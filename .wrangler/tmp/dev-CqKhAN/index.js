var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });

// src/modules/subjects/bangla/definition.ts
var banglaTemplate = {
  slug: "bangla",
  name: "Bangla",
  structure: {
    hasChapters: true,
    hasTopics: false,
    contentScope: "chapter"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "core",
      isOptional: false
    },
    {
      slug: "11-12",
      stream: "core",
      isOptional: false
    }
  ]
};

// src/modules/subjects/english/definition.ts
var englishTemplate = {
  slug: "english",
  name: "English",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "core",
      isOptional: false
    },
    {
      slug: "11-12",
      stream: "core",
      isOptional: false
    }
  ]
};

// src/modules/subjects/mathematics/definition.ts
var mathematicsTemplate = {
  slug: "mathematics",
  name: "Mathematics",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "core",
      isOptional: false
    }
  ]
};

// src/modules/subjects/bangladesh-and-global-studies/definition.ts
var bangladesh_and_global_studiesTemplate = {
  slug: "bangladesh-and-global-studies",
  name: "Bangladesh and Global Studies",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "core",
      isOptional: false
    }
  ]
};

// src/modules/subjects/information-and-communication-technology/definition.ts
var information_and_communication_technologyTemplate = {
  slug: "information-and-communication-technology",
  name: "Information and Communication Technology",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "core",
      isOptional: false
    },
    {
      slug: "11-12",
      stream: "core",
      isOptional: false
    }
  ]
};

// src/modules/subjects/islam-and-moral-education/definition.ts
var islam_and_moral_educationTemplate = {
  slug: "islam-and-moral-education",
  name: "Islam and Moral Education",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "core",
      isOptional: false
    }
  ]
};

// src/modules/subjects/hindu-religion-and-moral-education/definition.ts
var hindu_religion_and_moral_educationTemplate = {
  slug: "hindu-religion-and-moral-education",
  name: "Hindu Religion and Moral Education",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "core",
      isOptional: false
    }
  ]
};

// src/modules/subjects/buddhist-religion-and-moral-education/definition.ts
var buddhist_religion_and_moral_educationTemplate = {
  slug: "buddhist-religion-and-moral-education",
  name: "Buddhist Religion and Moral Education",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "core",
      isOptional: false
    }
  ]
};

// src/modules/subjects/christian-religion-and-moral-education/definition.ts
var christian_religion_and_moral_educationTemplate = {
  slug: "christian-religion-and-moral-education",
  name: "Christian Religion and Moral Education",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "core",
      isOptional: false
    }
  ]
};

// src/modules/subjects/physics/definition.ts
var physicsTemplate = {
  slug: "physics",
  name: "Physics",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "science",
      isOptional: false
    },
    {
      slug: "11-12",
      stream: "science",
      isOptional: false
    }
  ]
};

// src/modules/subjects/chemistry/definition.ts
var chemistryTemplate = {
  slug: "chemistry",
  name: "Chemistry",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "science",
      isOptional: false
    },
    {
      slug: "11-12",
      stream: "science",
      isOptional: false
    }
  ]
};

// src/modules/subjects/biology/definition.ts
var biologyTemplate = {
  slug: "biology",
  name: "Biology",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "science",
      isOptional: false
    },
    {
      slug: "11-12",
      stream: "science",
      isOptional: false
    }
  ]
};

// src/modules/subjects/higher-mathematics/definition.ts
var higher_mathematicsTemplate = {
  slug: "higher-mathematics",
  name: "Higher Mathematics",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "science",
      isOptional: false
    },
    {
      slug: "11-12",
      stream: "science",
      isOptional: false
    }
  ]
};

// src/modules/subjects/accounting/definition.ts
var accountingTemplate = {
  slug: "accounting",
  name: "Accounting",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "business",
      isOptional: false
    },
    {
      slug: "11-12",
      stream: "business",
      isOptional: false
    }
  ]
};

// src/modules/subjects/finance-and-banking/definition.ts
var finance_and_bankingTemplate = {
  slug: "finance-and-banking",
  name: "Finance and Banking",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "business",
      isOptional: false
    }
  ]
};

// src/modules/subjects/business-entrepreneurship/definition.ts
var business_entrepreneurshipTemplate = {
  slug: "business-entrepreneurship",
  name: "Business Entrepreneurship",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "business",
      isOptional: false
    }
  ]
};

// src/modules/subjects/business-organization-and-management/definition.ts
var business_organization_and_managementTemplate = {
  slug: "business-organization-and-management",
  name: "Business Organization and Management",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "business",
      isOptional: false
    }
  ]
};

// src/modules/subjects/production-management-and-marketing/definition.ts
var production_management_and_marketingTemplate = {
  slug: "production-management-and-marketing",
  name: "Production Management and Marketing",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "business",
      isOptional: false
    }
  ]
};

// src/modules/subjects/finance-banking-and-insurance/definition.ts
var finance_banking_and_insuranceTemplate = {
  slug: "finance-banking-and-insurance",
  name: "Finance, Banking and Insurance",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "business",
      isOptional: false
    }
  ]
};

// src/modules/subjects/geography-and-environment/definition.ts
var geography_and_environmentTemplate = {
  slug: "geography-and-environment",
  name: "Geography and Environment",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/civics-and-citizenship/definition.ts
var civics_and_citizenshipTemplate = {
  slug: "civics-and-citizenship",
  name: "Civics and Citizenship",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/economics/definition.ts
var economicsTemplate = {
  slug: "economics",
  name: "Economics",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "humanities",
      isOptional: false
    },
    {
      slug: "11-12",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/history-of-bangladesh-and-world-civilization/definition.ts
var history_of_bangladesh_and_world_civilizationTemplate = {
  slug: "history-of-bangladesh-and-world-civilization",
  name: "History of Bangladesh and World Civilization",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/agriculture-studies/definition.ts
var agriculture_studiesTemplate = {
  slug: "agriculture-studies",
  name: "Agriculture Studies",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/home-science/definition.ts
var home_scienceTemplate = {
  slug: "home-science",
  name: "Home Science",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "9-10",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/civics-and-good-governance/definition.ts
var civics_and_good_governanceTemplate = {
  slug: "civics-and-good-governance",
  name: "Civics and Good Governance",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/history/definition.ts
var historyTemplate = {
  slug: "history",
  name: "History",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/islamic-history-and-culture/definition.ts
var islamic_history_and_cultureTemplate = {
  slug: "islamic-history-and-culture",
  name: "Islamic History and Culture",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/sociology/definition.ts
var sociologyTemplate = {
  slug: "sociology",
  name: "Sociology",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/social-work/definition.ts
var social_workTemplate = {
  slug: "social-work",
  name: "Social Work",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/geography/definition.ts
var geographyTemplate = {
  slug: "geography",
  name: "Geography",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/logic/definition.ts
var logicTemplate = {
  slug: "logic",
  name: "Logic",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/psychology/definition.ts
var psychologyTemplate = {
  slug: "psychology",
  name: "Psychology",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "humanities",
      isOptional: false
    }
  ]
};

// src/modules/subjects/statistics/definition.ts
var statisticsTemplate = {
  slug: "statistics",
  name: "Statistics",
  structure: {
    hasChapters: true,
    hasTopics: true,
    contentScope: "topic"
  },
  classGroups: [
    {
      slug: "11-12",
      stream: "science",
      isOptional: false
    }
  ]
};

// src/ui/components/content-shell/desktop/shell.ts
var renderContentShellDesktop = /* @__PURE__ */ __name(({ header, sidebar, main, footer }) => `
  <div class="app app--desktop">
    <input class="sidebar-toggle" type="checkbox" id="sidebar-toggle" />
    <div class="app-shell">
      ${header}
      ${sidebar}
      <main class="app-main">
        <div class="app-main__breadcrumb" id="breadcrumb" role="navigation" aria-label="Breadcrumb"></div>
        <div class="app-main__content">${main}</div>
      </main>
      ${footer}
    </div>
  </div>
`, "renderContentShellDesktop");
var desktopShellStyles = `
  /* Layout System */
  .app { 
    min-height: 100vh; 
    background: var(--color-bg); 
    color: var(--color-text); 
    font-family: var(--font-body); 
  }
  
  .sidebar-toggle { 
    position: absolute; 
    opacity: 0; 
    pointer-events: none; 
  }
  
  /* Toggle icon states for desktop */
  .sidebar-toggle__icon { 
    display: inline-flex; 
    transition: opacity 0.2s ease, transform 0.2s ease;
  }
  
  .sidebar-toggle__icon--open { 
    display: none; 
    opacity: 0;
    transform: rotate(180deg);
  }
  
  .sidebar-toggle__icon--close { 
    display: inline-flex; 
  }
  
  .app-shell { 
    display: grid; 
    grid-template-columns: 240px 1fr; 
    grid-template-rows: auto 1fr auto; 
    min-height: 100vh; 
    min-height: 100dvh; 
    height: 100vh; 
    height: 100dvh; 
    background: var(--color-bg); 
    transition: grid-template-columns 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Layout controls spacing between sidebar and main content */
  .sidebar-toggle:checked + .app-shell { 
    grid-template-columns: 60px 1fr; 
  }
  
  /* Toggle icon states when checked */
  .sidebar-toggle:checked + .app-shell .sidebar-toggle__icon--open { 
    display: inline-flex; 
    opacity: 1;
    transform: rotate(0deg);
  }
  
  .sidebar-toggle:checked + .app-shell .sidebar-toggle__icon--close { 
    display: none; 
    opacity: 0;
    transform: rotate(-180deg);
  }

  /* Header Component */
  .app-header {
    grid-column: 1 / -1;
    position: relative;
    z-index: 30;
    display: grid;
    grid-template-columns: 80px 1fr 200px;
    align-items: center;
    padding: 6px 12px;
    border-bottom: 1px solid var(--color-border);
    background: var(--color-surface);
    box-shadow: var(--shadow-sm);
  }
  
  .app-header__center { 
    display: flex; 
    justify-content: center; 
  }
  
  .app-header__right { 
    display: flex; 
    justify-content: flex-end; 
    gap: 12px; 
  }
  
  .logo { 
    font-family: var(--font-display); 
    font-weight: 600; 
    letter-spacing: 0.4px; 
    color: var(--color-text); 
    text-transform: lowercase; 
  }

  /* Interactive Elements */
  .icon-button {
    cursor: pointer;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: 0;
    width: 36px;
    height: 36px;
    background: var(--color-surface);
    color: var(--color-text);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  .icon-button:hover { 
    border-color: var(--color-border-strong); 
    background: var(--color-surface-muted); 
    transform: translateY(-1px); 
  }

  /* Main Content Area - gets more/less space based on sidebar */
  .app-main {
    display: grid;
    grid-template-rows: auto 1fr;
    overflow: hidden;
    min-height: 0;
    background: var(--color-bg);
    position: relative;
    z-index: 1;
  }
  
  .app-main__content {
    padding: 12px 16px 16px;
    overflow-y: auto;
    overscroll-behavior: contain;
    min-height: 0;
    transition: all 0.2s ease;
    animation: page-enter 0.35s ease;
  }

  /* Footer Component */
  .app-footer { 
    grid-column: 1 / -1; 
    padding: 6px 12px; 
    border-top: 1px solid var(--color-border); 
    background: var(--color-surface-muted); 
    color: var(--color-text-muted); 
    text-align: center; 
  }

  /* Dropdown Navigation */
  .profile-menu, .notification-menu { 
    position: relative; 
  }
  
  .profile-menu summary, .notification-menu summary { 
    list-style: none; 
    cursor: pointer; 
  }
  
  .profile-menu summary::-webkit-details-marker, 
  .notification-menu summary::-webkit-details-marker { 
    display: none; 
  }
  
  .profile-menu, .notification-menu { 
    z-index: 50; 
  }
  
  .dropdown {
    position: absolute;
    right: 0;
    top: 40px;
    min-width: 220px;
    padding: 12px;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-sm);
    z-index: 60;
    animation: dropdown-fade 0.2s ease;
  }
  
  .dropdown p { 
    margin: 0 0 8px; 
    color: var(--color-text-muted); 
  }
  
  .dropdown p strong { 
    color: var(--color-text); 
  }
  
  .dropdown .button-link { 
    width: 100%; 
  }

  /* User Avatar */
  .avatar {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--color-accent);
    color: #fff;
    font-size: 14px;
    font-weight: 600;
  }
  
  .profile-notifications { 
    display: none; 
  }

  /* Glassy UI and Bouncy Animations */
  .app-shell {
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.05);
  }
  
  :root[data-theme="dark"] .app-shell {
    background: rgba(0, 0, 0, 0.05);
  }
  
  .app-header {
    backdrop-filter: blur(20px);
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }
  
  :root[data-theme="dark"] .app-header {
    background: rgba(0, 0, 0, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
  
  .app-sidebar {
    backdrop-filter: blur(15px);
    background: rgba(255, 255, 255, 0.08);
    border-right: 1px solid rgba(255, 255, 255, 0.15);
  }
  
  :root[data-theme="dark"] .app-sidebar {
    background: rgba(0, 0, 0, 0.08);
    border-right: 1px solid rgba(255, 255, 255, 0.08);
  }
  
  .app-main {
    backdrop-filter: blur(5px);
  }
  
  .icon-button {
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.3);
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  :root[data-theme="dark"] .icon-button {
    background: rgba(0, 0, 0, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.15);
  }
  
  .icon-button:hover { 
    background: rgba(255, 255, 255, 0.25);
    border-color: rgba(255, 255, 255, 0.4);
    transform: translateY(-2px) scale(1.05);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
  }
  
  :root[data-theme="dark"] .icon-button:hover {
    background: rgba(0, 0, 0, 0.25);
    border-color: rgba(255, 255, 255, 0.25);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
  }
  
  /* Bouncy sidebar transitions */
  .app-shell {
    transition: grid-template-columns 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .app-sidebar {
    transition: all 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .menu-item {
    transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .menu-item:hover { 
    transform: translateX(4px) scale(1.02);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  }
  
  .menu-icon {
    transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .menu-label {
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  /* Bouncy theme toggle */
  .theme-toggle {
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.25);
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  :root[data-theme="dark"] .theme-toggle {
    background: rgba(0, 0, 0, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.12);
  }
  
  .theme-toggle:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.35);
    transform: translateY(-2px) scale(1.05);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.12);
  }
  
  :root[data-theme="dark"] .theme-toggle:hover {
    background: rgba(0, 0, 0, 0.2);
    border-color: rgba(255, 255, 255, 0.2);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.25);
  }
  
  .theme-toggle:active {
    transform: translateY(-1px) scale(0.98);
  }

  /* Desktop only - no responsive styles */
`;

// src/ui/components/content-shell/mobile/shell.ts
var renderContentShellMobile = /* @__PURE__ */ __name(({ header, sidebar, main, footer }) => `
  <div class="app app--mobile">
    <input class="sidebar-toggle" type="checkbox" id="sidebar-toggle" />
    <div class="app-shell">
      ${header}
      ${sidebar}
      <main class="app-main">
        <div class="app-main__breadcrumb" id="breadcrumb" role="navigation" aria-label="Breadcrumb"></div>
        <div class="app-main__content">${main}</div>
      </main>
      ${footer}
    </div>
  </div>
`, "renderContentShellMobile");
var mobileShellStyles = `
  .app { min-height: 100vh; min-height: 100dvh; background: var(--color-bg); color: var(--color-text); font-family: var(--font-body); }
  .sidebar-toggle { position: absolute; opacity: 0; pointer-events: none; }
  .app-shell { display: grid; grid-template-rows: auto 1fr auto; min-height: 100vh; min-height: 100dvh; height: 100vh; height: 100dvh; background: var(--color-bg); }
  .app-header {
    position: relative;
    z-index: 30;
    display: grid;
    grid-template-columns: 60px 1fr 80px;
    align-items: center;
    padding: 6px 12px;
    border-bottom: 1px solid var(--color-border);
    background: var(--color-surface);
    box-shadow: var(--shadow-sm);
  }
  .app-header__center { display: flex; justify-content: center; }
  .app-header__right { display: flex; justify-content: flex-end; }
  .logo { font-family: var(--font-display); font-weight: 600; letter-spacing: 0.4px; color: var(--color-text); text-transform: lowercase; }
  .icon-button {
    cursor: pointer;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: 0;
    width: 36px;
    height: 36px;
    background: var(--color-surface);
    color: var(--color-text);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: border-color 0.2s ease, background-color 0.2s ease, transform 0.2s ease;
  }
  .icon-button:hover { border-color: var(--color-border-strong); background: var(--color-surface-muted); transform: translateY(-1px); }
  .sidebar-toggle__icon { display: inline-flex; }
  .sidebar-toggle__icon--close { display: none; }
  .app-sidebar {
    position: fixed;
    top: 0;
    left: 0;
    bottom: 0;
    width: 80%;
    max-width: 320px;
    background: var(--color-surface-muted);
    border-right: 1px solid var(--color-border);
    padding: 16px;
    transform: translateX(-110%);
    transition: transform 0.25s cubic-bezier(0.2, 0.8, 0.2, 1);
    z-index: 60;
    overflow: auto;
    box-shadow: var(--shadow-sm);
  }
  .sidebar-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px; font-weight: 600; color: var(--color-text); }
  .menu { list-style: none; padding: 0; margin: 0; }
  .menu-item {
    display: flex;
    gap: 10px;
    align-items: center;
    padding: 8px 6px;
    border-radius: var(--radius-sm);
    color: var(--color-text);
    border: 1px solid transparent;
    transition: background 0.2s ease, border-color 0.2s ease, transform 0.2s ease;
  }
  .menu-item:hover { background: var(--color-surface-elevated); border-color: var(--color-border); transform: translateX(2px); }
  .menu-icon { display: inline-flex; align-items: center; justify-content: center; color: var(--color-text-muted); }
  .app-main {
    display: grid;
    grid-template-rows: auto 1fr;
    overflow: hidden;
    min-height: 0;
    background: var(--color-bg);
    border: none;
    border-radius: 0;
    position: relative;
    z-index: 1;
  }
  .app-main__content {
    padding: 12px;
    overflow: auto;
    overscroll-behavior: contain;
    min-height: 0;
    transition: opacity 0.2s ease, transform 0.2s ease;
    animation: page-enter 0.35s ease;
  }
  .app-footer { padding: 6px 12px; border-top: 1px solid var(--color-border); background: var(--color-surface-muted); color: var(--color-text-muted); text-align: center; }
  .profile-menu summary { list-style: none; cursor: pointer; }
  .profile-menu summary::-webkit-details-marker { display: none; }
  .profile-menu { position: relative; z-index: 50; }
  .dropdown {
    position: absolute;
    right: 12px;
    top: 52px;
    min-width: 220px;
    padding: 12px;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-sm);
    z-index: 60;
    animation: dropdown-fade 0.2s ease;
  }
  .dropdown p { margin: 0 0 8px; color: var(--color-text-muted); }
  .dropdown p strong { color: var(--color-text); }
  .dropdown .button-link { width: 100%; }
  .avatar {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--color-accent);
    color: #fff;
    font-size: 14px;
    font-weight: 600;
  }
  .sidebar-toggle:checked + .app-shell .app-sidebar { transform: translateX(0); }
  .sidebar-toggle:checked + .app-shell .sidebar-toggle__icon--open { display: none; }
  .sidebar-toggle:checked + .app-shell .sidebar-toggle__icon--close { display: inline-flex; }
  
  /* Glassy UI and Bouncy Animations */
  .app-shell {
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.05);
  }
  
  :root[data-theme="dark"] .app-shell {
    background: rgba(0, 0, 0, 0.05);
  }
  
  .app-header {
    backdrop-filter: blur(20px);
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }
  
  :root[data-theme="dark"] .app-header {
    background: rgba(0, 0, 0, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
  
  .app-sidebar {
    backdrop-filter: blur(15px);
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.15);
  }
  
  :root[data-theme="dark"] .app-sidebar {
    background: rgba(0, 0, 0, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.08);
  }
  
  .app-main {
    backdrop-filter: blur(5px);
  }
  
  .icon-button {
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.3);
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  :root[data-theme="dark"] .icon-button {
    background: rgba(0, 0, 0, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.15);
  }
  
  .icon-button:hover { 
    background: rgba(255, 255, 255, 0.25);
    border-color: rgba(255, 255, 255, 0.4);
    transform: translateY(-2px) scale(1.05);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
  }
  
  :root[data-theme="dark"] .icon-button:hover {
    background: rgba(0, 0, 0, 0.25);
    border-color: rgba(255, 255, 255, 0.25);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
  }
  
  /* Bouncy sidebar slide */
  .app-sidebar {
    transition: transform 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .menu-item {
    transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .menu-item:hover { 
    transform: translateX(4px) scale(1.02);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  }
  
  .menu-icon {
    transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .menu-label {
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  /* Bouncy theme toggle */
  .theme-toggle {
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.25);
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  :root[data-theme="dark"] .theme-toggle {
    background: rgba(0, 0, 0, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.12);
  }
  
  .theme-toggle:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.35);
    transform: translateY(-2px) scale(1.05);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.12);
  }
  
  :root[data-theme="dark"] .theme-toggle:hover {
    background: rgba(0, 0, 0, 0.2);
    border-color: rgba(255, 255, 255, 0.2);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.25);
  }
  
  .theme-toggle:active {
    transform: translateY(-1px) scale(0.98);
  }
`;

// src/ui/components/content-shell/tablet/shell.ts
var renderContentShellTablet = /* @__PURE__ */ __name(({ header, sidebar, main, footer }) => `
  <div class="app app--tablet">
    <input class="sidebar-toggle" type="checkbox" id="sidebar-toggle" />
    <div class="app-shell">
      ${header}
      ${sidebar}
      <main class="app-main">
        <div class="app-main__breadcrumb" id="breadcrumb" role="navigation" aria-label="Breadcrumb"></div>
        <div class="app-main__content">${main}</div>
      </main>
      ${footer}
    </div>
  </div>
`, "renderContentShellTablet");
var tabletShellStyles = `
  /* Tablet Layout System */
  .app { 
    min-height: 100vh; 
    background: var(--color-bg); 
    color: var(--color-text); 
    font-family: var(--font-body); 
  }
  
  .sidebar-toggle { 
    position: absolute; 
    opacity: 0; 
    pointer-events: none; 
  }
  
  .app-shell { 
    display: grid; 
    grid-template-columns: 200px 1fr; 
    grid-template-rows: auto 1fr auto; 
    min-height: 100vh; 
    min-height: 100dvh; 
    height: 100vh; 
    height: 100dvh; 
    background: var(--color-bg); 
    transition: grid-template-columns 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Tablet controls layout spacing */
  .sidebar-toggle:checked + .app-shell { 
    grid-template-columns: 60px 1fr; 
  }
  
  /* Toggle icon states for tablet */
  .sidebar-toggle__icon { 
    display: inline-flex; 
    transition: opacity 0.2s ease, transform 0.2s ease;
  }
  
  .sidebar-toggle__icon--open { 
    display: none; 
    opacity: 0;
    transform: rotate(180deg);
  }
  
  .sidebar-toggle__icon--close { 
    display: inline-flex; 
  }
  
  /* Toggle icon states when checked */
  .sidebar-toggle:checked + .app-shell .sidebar-toggle__icon--open { 
    display: inline-flex; 
    opacity: 1;
    transform: rotate(0deg);
  }
  
  .sidebar-toggle:checked + .app-shell .sidebar-toggle__icon--close { 
    display: none; 
    opacity: 0;
    transform: rotate(-180deg);
  }

  /* Header Component */
  .app-header {
    grid-column: 1 / -1;
    position: relative;
    z-index: 30;
    display: grid;
    grid-template-columns: 60px 1fr 120px;
    align-items: center;
    padding: 6px 12px;
    border-bottom: 1px solid var(--color-border);
    background: var(--color-surface);
    box-shadow: var(--shadow-sm);
  }
  
  .app-header__center { 
    display: flex; 
    justify-content: center; 
  }
  
  .app-header__right { 
    display: flex; 
    justify-content: flex-end; 
    gap: 12px; 
  }
  
  .logo { 
    font-family: var(--font-display); 
    font-weight: 600; 
    letter-spacing: 0.4px; 
    color: var(--color-text); 
    text-transform: lowercase; 
  }

  /* Interactive Elements */
  .icon-button {
    cursor: pointer;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: 0;
    width: 36px;
    height: 36px;
    background: var(--color-surface);
    color: var(--color-text);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  .icon-button:hover { 
    border-color: var(--color-border-strong); 
    background: var(--color-surface-muted); 
    transform: translateY(-1px); 
  }

  /* Main Content Area - tablet specific */
  .app-main {
    display: grid;
    grid-template-rows: auto 1fr;
    overflow: hidden;
    min-height: 0;
    background: var(--color-bg);
    position: relative;
    z-index: 1;
  }
  
  .app-main__content {
    padding: 10px 14px 14px;
    overflow-y: auto;
    overscroll-behavior: contain;
    min-height: 0;
    transition: all 0.2s ease;
    animation: page-enter 0.35s ease;
  }

  /* Footer Component */
  .app-footer { 
    grid-column: 1 / -1; 
    padding: 6px 12px; 
    border-top: 1px solid var(--color-border); 
    background: var(--color-surface-muted); 
    color: var(--color-text-muted); 
    text-align: center; 
  }

  /* Tablet specific - no responsive styles */
  
  /* Glassy UI and Bouncy Animations */
  .app-shell {
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.05);
  }
  
  :root[data-theme="dark"] .app-shell {
    background: rgba(0, 0, 0, 0.05);
  }
  
  .app-header {
    backdrop-filter: blur(20px);
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }
  
  :root[data-theme="dark"] .app-header {
    background: rgba(0, 0, 0, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
  
  .app-sidebar {
    backdrop-filter: blur(15px);
    background: rgba(255, 255, 255, 0.08);
    border-right: 1px solid rgba(255, 255, 255, 0.15);
  }
  
  :root[data-theme="dark"] .app-sidebar {
    background: rgba(0, 0, 0, 0.08);
    border-right: 1px solid rgba(255, 255, 255, 0.08);
  }
  
  .app-main {
    backdrop-filter: blur(5px);
  }
  
  .icon-button {
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.3);
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  :root[data-theme="dark"] .icon-button {
    background: rgba(0, 0, 0, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.15);
  }
  
  .icon-button:hover { 
    background: rgba(255, 255, 255, 0.25);
    border-color: rgba(255, 255, 255, 0.4);
    transform: translateY(-2px) scale(1.05);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
  }
  
  :root[data-theme="dark"] .icon-button:hover {
    background: rgba(0, 0, 0, 0.25);
    border-color: rgba(255, 255, 255, 0.25);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
  }
  
  /* Bouncy sidebar transitions */
  .app-shell {
    transition: grid-template-columns 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .app-sidebar {
    transition: all 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .menu-item {
    transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .menu-item:hover { 
    transform: translateX(4px) scale(1.02);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  }
  
  .menu-icon {
    transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  .menu-label {
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  /* Bouncy theme toggle */
  .theme-toggle {
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.25);
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }
  
  :root[data-theme="dark"] .theme-toggle {
    background: rgba(0, 0, 0, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.12);
  }
  
  .theme-toggle:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.35);
    transform: translateY(-2px) scale(1.05);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.12);
  }
  
  :root[data-theme="dark"] .theme-toggle:hover {
    background: rgba(0, 0, 0, 0.2);
    border-color: rgba(255, 255, 255, 0.2);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.25);
  }
  
  .theme-toggle:active {
    transform: translateY(-1px) scale(0.98);
  }
`;

// src/ui/components/footer/desktop/footer.ts
var renderFooterDesktop = /* @__PURE__ */ __name(() => `
  <footer class="app-footer">
    <span>Freeducation</span>
  </footer>
`, "renderFooterDesktop");

// src/ui/components/icons.ts
var renderIcon = /* @__PURE__ */ __name((path) => `
  <svg class="icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false" role="img">
    ${path}
  </svg>
`, "renderIcon");
var renderMenuIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<path d="M4 6h16M4 12h16M4 18h16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" />'
), "renderMenuIcon");
var renderCloseIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<path d="M6 6l12 12M18 6l-12 12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" />'
), "renderCloseIcon");
var renderHomeIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<path d="M4 11.5L12 5l8 6.5V20a1 1 0 0 1-1 1h-5v-6H10v6H5a1 1 0 0 1-1-1v-8.5Z" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round" />'
), "renderHomeIcon");
var renderUserIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<path d="M12 12a4 4 0 1 0-4-4 4 4 0 0 0 4 4Zm7 8a7 7 0 0 0-14 0" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" />'
), "renderUserIcon");
var renderUsersIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<path d="M8 12a3 3 0 1 0-3-3 3 3 0 0 0 3 3Zm8 0a3 3 0 1 0-3-3 3 3 0 0 0 3 3Zm-8 2c-2.8 0-5 1.8-5 4v2m13-6c2.8 0 5 1.8 5 4v2M8 16h8" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />'
), "renderUsersIcon");
var renderModulesIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<path d="M4 4h7v7H4V4Zm9 0h7v7h-7V4ZM4 13h7v7H4v-7Zm9 0h7v7h-7v-7Z" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round" />'
), "renderModulesIcon");
var renderBellIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<path d="M18 9a6 6 0 1 0-12 0c0 4-2 5-2 5h16s-2-1-2-5Zm-5 9a2 2 0 0 1-4 0" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />'
), "renderBellIcon");
var renderSunIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<circle cx="12" cy="12" r="4" fill="none" stroke="currentColor" stroke-width="2" /><path d="M12 2v3M12 19v3M4.2 4.2l2.1 2.1M17.7 17.7l2.1 2.1M2 12h3M19 12h3M4.2 19.8l2.1-2.1M17.7 6.3l2.1-2.1" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" />'
), "renderSunIcon");
var renderMoonIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<path d="M21 12.8A8.5 8.5 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8Z" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />'
), "renderMoonIcon");
var renderMinimizeIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<path d="M4 14h6v6M14 4h6v6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />'
), "renderMinimizeIcon");
var renderMaximizeIcon = /* @__PURE__ */ __name(() => renderIcon(
  '<path d="M4 10h6V4M14 20h6v-6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />'
), "renderMaximizeIcon");

// src/ui/components/header/desktop/header.ts
var renderHeaderDesktop = /* @__PURE__ */ __name(({ siteName, profileMenu, notificationMenu }) => `
  <header class="app-header app-header--desktop">
    <div class="app-header__left">
      <label class="icon-button" for="sidebar-toggle" aria-label="Toggle sidebar">
        <span class="sidebar-toggle__icon sidebar-toggle__icon--open">${renderMinimizeIcon()}</span>
        <span class="sidebar-toggle__icon sidebar-toggle__icon--close">${renderMaximizeIcon()}</span>
      </label>
    </div>
    <div class="app-header__center">
      <div class="logo">${siteName}</div>
    </div>
    <div class="app-header__right">
      ${notificationMenu}
      ${profileMenu}
    </div>
  </header>
`, "renderHeaderDesktop");

// src/ui/components/header/mobile/header.ts
var renderHeaderMobile = /* @__PURE__ */ __name(({ siteName, profileMenu }) => `
  <header class="app-header app-header--mobile">
    <div class="app-header__left">
      <label class="icon-button" for="sidebar-toggle" aria-label="Toggle sidebar">
        <span class="sidebar-toggle__icon sidebar-toggle__icon--open">${renderMenuIcon()}</span>
        <span class="sidebar-toggle__icon sidebar-toggle__icon--close">${renderCloseIcon()}</span>
      </label>
    </div>
    <div class="app-header__center">
      <div class="logo">${siteName}</div>
    </div>
    <div class="app-header__right">
      ${profileMenu}
    </div>
  </header>
`, "renderHeaderMobile");

// src/ui/components/header/tablet/header.ts
var renderHeaderTablet = /* @__PURE__ */ __name(({ siteName, profileMenu, notificationMenu }) => `
  <header class="app-header app-header--tablet">
    <div class="app-header__left">
      <label class="icon-button" for="sidebar-toggle" aria-label="Toggle sidebar">
        <span class="sidebar-toggle__icon sidebar-toggle__icon--open">${renderMinimizeIcon()}</span>
        <span class="sidebar-toggle__icon sidebar-toggle__icon--close">${renderMaximizeIcon()}</span>
      </label>
    </div>
    <div class="app-header__center">
      <div class="logo">${siteName}</div>
    </div>
    <div class="app-header__right">
      ${notificationMenu}
      ${profileMenu}
    </div>
  </header>
`, "renderHeaderTablet");

// src/ui/components/sidebar/desktop/sidebar.ts
var createMenuItem = /* @__PURE__ */ __name((href, icon, label) => `
  <li>
    <a class="menu-item" href="${href}">
      <span class="menu-icon">${icon}</span>
      <span class="menu-label">${label}</span>
    </a>
  </li>
`, "createMenuItem");
var renderNavigationItems = /* @__PURE__ */ __name((session) => {
  const items = [
    createMenuItem("/", renderHomeIcon(), "Home")
  ];
  if (session) {
    items.push(
      createMenuItem("/admin/users", renderUsersIcon(), "User management"),
      createMenuItem("/admin/modules", renderModulesIcon(), "Modules")
    );
  }
  return items.join("");
}, "renderNavigationItems");
var renderThemeToggle = /* @__PURE__ */ __name(() => `
  <button class="theme-toggle" type="button" data-theme-toggle aria-pressed="false" aria-label="Toggle theme">
    <span class="theme-toggle__icon theme-toggle__icon--sun">${renderSunIcon()}</span>
    <span class="theme-toggle__icon theme-toggle__icon--moon">${renderMoonIcon()}</span>
    <span class="theme-toggle__label">Theme</span>
  </button>
`, "renderThemeToggle");
var renderSidebarDesktop = /* @__PURE__ */ __name(({ session }) => `
  <aside class="app-sidebar app-sidebar--desktop">
    <nav>
      <ul class="menu">
        ${renderNavigationItems(session)}
      </ul>
    </nav>
    <div class="sidebar-footer">
      ${renderThemeToggle()}
    </div>
  </aside>
`, "renderSidebarDesktop");
var sidebarDesktopStyles = `
  /* Sidebar controls its own appearance */
  .app-sidebar {
    grid-row: 2 / 3;
    padding: 10px;
    border-right: 1px solid var(--color-border);
    background: var(--color-surface-muted);
    overflow-y: auto;
    box-shadow: var(--shadow-sm);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Navigation Menu */
  .menu { 
    list-style: none; 
    padding: 0; 
    margin: 0; 
  }
  
  .menu-item {
    display: flex;
    gap: 10px;
    align-items: center;
    padding: 8px 6px;
    border-radius: var(--radius-sm);
    color: var(--color-text);
    border: 1px solid transparent;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
  }
  
  .menu-item:hover { 
    background: var(--color-surface-elevated); 
    border-color: var(--color-border); 
    transform: translateX(2px); 
  }
  
  /* Sidebar appearance when shrunk */
  .sidebar-toggle:checked + .app-shell .menu-item:hover {
    transform: translateX(0);
    background: var(--color-surface-elevated); 
  }
  
  .menu-icon { 
    display: inline-flex; 
    align-items: center; 
    justify-content: center; 
    color: var(--color-text-muted); 
    flex-shrink: 0;
  }
  
  .menu-label { 
    transition: all 0.2s ease; 
    white-space: nowrap;
    flex-shrink: 0;
  }

  /* Sidebar controls its own full/shrunk appearance */
  .sidebar-toggle:checked + .app-shell .menu-label { 
    opacity: 0; 
    visibility: hidden;
    position: absolute;
    pointer-events: none;
  }
  
  .sidebar-toggle:checked + .app-shell .menu-item {
    justify-content: center;
    width: 100%;
    min-width: 0; /* Prevent flex items from overflowing */
  }
  
  .sidebar-toggle:checked + .app-shell .menu-icon {
    flex-shrink: 0;
    min-width: 0;
    overflow: hidden;
  }
  
  .sidebar-toggle:checked + .app-shell .app-sidebar { 
    padding: 10px 6px; 
    overflow-x: hidden; /* Prevent horizontal scrolling */
    overflow-y: auto; /* Allow vertical scrolling only */
  }
  
  /* Hide theme button label when sidebar is minimized */
  .sidebar-toggle:checked + .app-shell .theme-toggle__label {
    opacity: 0;
    visibility: hidden;
    position: absolute;
    pointer-events: none;
  }
  
  .sidebar-toggle:checked + .app-shell .theme-toggle {
    justify-content: center;
    width: 100%;
    min-width: 0;
  }

  /* Sidebar Footer */
  .sidebar-footer {
    margin-top: auto;
    padding-top: 12px;
    border-top: 1px solid var(--color-border);
  }

  /* Theme Toggle */
  .theme-toggle {
    width: 100%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 8px 10px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-border);
    background: var(--color-surface);
    color: var(--color-text);
    cursor: pointer;
    transition: background-color 0.2s ease, border-color 0.2s ease, transform 0.2s ease;
  }
  .theme-toggle:hover {
    background: var(--color-surface-muted);
    border-color: var(--color-border-strong);
    transform: translateY(-1px);
  }
  .theme-toggle__icon { display: inline-flex; align-items: center; }
  .theme-toggle__icon--moon { display: none; }
  .theme-toggle__label { font-size: 13px; font-weight: 600; }
  :root[data-theme="dark"] .theme-toggle__icon--sun { display: none; }
  :root[data-theme="dark"] .theme-toggle__icon--moon { display: inline-flex; }
`;

// src/ui/components/sidebar/mobile/sidebar.ts
var renderSidebarMobile = /* @__PURE__ */ __name(({ session }) => `
  <aside class="app-sidebar app-sidebar--mobile">
    <div class="sidebar-header">
      <span>Menu</span>
      <label class="icon-button" for="sidebar-toggle" aria-label="Close sidebar">${renderCloseIcon()}</label>
    </div>
    <nav>
      <ul class="menu">
        <li>
          <a class="menu-item" href="/">
            <span class="menu-icon">${renderHomeIcon()}</span>
            <span class="menu-label">Home</span>
          </a>
        </li>
        ${session ? `<li>
          <a class="menu-item" href="/admin/users">
            <span class="menu-icon">${renderUsersIcon()}</span>
            <span class="menu-label">User management</span>
          </a>
        </li>` : ""}
        ${session ? `<li>
          <a class="menu-item" href="/admin/modules">
            <span class="menu-icon">${renderModulesIcon()}</span>
            <span class="menu-label">Modules</span>
          </a>
        </li>` : ""}
      </ul>
    </nav>
    <div class="sidebar-footer">
      <button class="theme-toggle" type="button" data-theme-toggle aria-pressed="false" aria-label="Toggle theme">
        <span class="theme-toggle__icon theme-toggle__icon--sun">${renderSunIcon()}</span>
        <span class="theme-toggle__icon theme-toggle__icon--moon">${renderMoonIcon()}</span>
        <span class="theme-toggle__label">Theme</span>
      </button>
    </div>
  </aside>
`, "renderSidebarMobile");

// src/ui/components/sidebar/tablet/sidebar.ts
var createMenuItem2 = /* @__PURE__ */ __name((href, icon, label) => `
  <li>
    <a class="menu-item" href="${href}">
      <span class="menu-icon">${icon}</span>
      <span class="menu-label">${label}</span>
    </a>
  </li>
`, "createMenuItem");
var renderNavigationItems2 = /* @__PURE__ */ __name((session) => {
  const items = [
    createMenuItem2("/", renderHomeIcon(), "Home")
  ];
  if (session) {
    items.push(
      createMenuItem2("/admin/users", renderUsersIcon(), "User management"),
      createMenuItem2("/admin/modules", renderModulesIcon(), "Modules")
    );
  }
  return items.join("");
}, "renderNavigationItems");
var renderThemeToggle2 = /* @__PURE__ */ __name(() => `
  <button class="theme-toggle" type="button" data-theme-toggle aria-pressed="false" aria-label="Toggle theme">
    <span class="theme-toggle__icon theme-toggle__icon--sun">${renderSunIcon()}</span>
    <span class="theme-toggle__icon theme-toggle__icon--moon">${renderMoonIcon()}</span>
    <span class="theme-toggle__label">Theme</span>
  </button>
`, "renderThemeToggle");
var renderSidebarTablet = /* @__PURE__ */ __name(({ session }) => `
  <aside class="app-sidebar app-sidebar--tablet">
    <nav>
      <ul class="menu">
        ${renderNavigationItems2(session)}
      </ul>
    </nav>
    <div class="sidebar-footer">
      ${renderThemeToggle2()}
    </div>
  </aside>
`, "renderSidebarTablet");
var sidebarTabletStyles = `
  /* Tablet sidebar controls its own appearance */
  .app-sidebar {
    grid-row: 2 / 3;
    padding: 8px;
    border-right: 1px solid var(--color-border);
    background: var(--color-surface-muted);
    overflow-y: auto;
    box-shadow: var(--shadow-sm);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Navigation Menu */
  .menu { 
    list-style: none; 
    padding: 0; 
    margin: 0; 
  }
  
  .menu-item {
    display: flex;
    gap: 8px;
    align-items: center;
    padding: 6px 4px;
    border-radius: var(--radius-sm);
    color: var(--color-text);
    border: 1px solid transparent;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
  }
  
  .menu-item:hover { 
    background: var(--color-surface-elevated); 
    border-color: var(--color-border); 
    transform: translateX(2px); 
  }
  
  /* Tablet sidebar appearance when shrunk */
  .sidebar-toggle:checked + .app-shell .menu-item:hover {
    transform: translateX(0);
    background: var(--color-surface-elevated); 
  }
  
  .menu-icon { 
    display: inline-flex; 
    align-items: center; 
    justify-content: center; 
    color: var(--color-text-muted); 
    flex-shrink: 0;
  }
  
  .menu-label { 
    transition: all 0.2s ease; 
    white-space: nowrap;
    flex-shrink: 0;
  }

  /* Tablet sidebar controls its own full/shrunk appearance */
  .sidebar-toggle:checked + .app-shell .menu-label { 
    opacity: 0; 
    visibility: hidden;
    position: absolute;
    pointer-events: none;
  }
  
  .sidebar-toggle:checked + .app-shell .menu-item {
    justify-content: center;
    width: 100%;
    min-width: 0; /* Prevent flex items from overflowing */
  }
  
  .sidebar-toggle:checked + .app-shell .menu-icon {
    flex-shrink: 0;
    min-width: 0;
    overflow: hidden;
  }
  
  .sidebar-toggle:checked + .app-shell .app-sidebar { 
    padding: 8px 4px; 
    overflow-x: hidden; /* Prevent horizontal scrolling */
    overflow-y: auto; /* Allow vertical scrolling only */
  }
  
  /* Hide theme button label when tablet sidebar is minimized */
  .sidebar-toggle:checked + .app-shell .theme-toggle__label {
    opacity: 0;
    visibility: hidden;
    position: absolute;
    pointer-events: none;
  }
  
  .sidebar-toggle:checked + .app-shell .theme-toggle {
    justify-content: center;
    width: 100%;
    min-width: 0;
  }

  /* Sidebar Footer */
  .sidebar-footer {
    margin-top: auto;
    padding-top: 10px;
    border-top: 1px solid var(--color-border);
  }

  /* Theme Toggle - tablet specific */
  .theme-toggle {
    width: 100%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    padding: 6px 8px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-border);
    background: var(--color-surface);
    color: var(--color-text);
    cursor: pointer;
    transition: background-color 0.2s ease, border-color 0.2s ease, transform 0.2s ease;
  }
  .theme-toggle:hover {
    background: var(--color-surface-muted);
    border-color: var(--color-border-strong);
    transform: translateY(-1px);
  }
  .theme-toggle__icon { display: inline-flex; align-items: center; }
  .theme-toggle__icon--moon { display: none; }
  .theme-toggle__label { font-size: 12px; font-weight: 600; }
  :root[data-theme="dark"] .theme-toggle__icon--sun { display: none; }
  :root[data-theme="dark"] .theme-toggle__icon--moon { display: inline-flex; }
`;

// src/ui/components/notification-menu.ts
var renderNotificationMenu = /* @__PURE__ */ __name(({ session }) => {
  const trigger = `<span class="icon-button" aria-label="Notifications">${renderBellIcon()}</span>`;
  const content = session ? "<p>No notifications yet.</p>" : '<p><a href="/login">Sign in</a> to view notifications.</p>';
  return `
    <details class="notification-menu">
      <summary>${trigger}</summary>
      <div class="dropdown">
        <p><strong>Notifications</strong></p>
        ${content}
      </div>
    </details>
  `;
}, "renderNotificationMenu");

// src/ui/components/profile-menu.ts
var getInitials = /* @__PURE__ */ __name((name) => name.split(" ").filter(Boolean).slice(0, 2).map((part) => part[0]?.toUpperCase()).join("") || "A", "getInitials");
var renderProfileMenu = /* @__PURE__ */ __name(({ session }) => {
  const trigger = session ? `<span class="avatar" aria-label="Profile">${getInitials(session.name)}</span>` : `<span class="icon-button" aria-label="Profile">${renderUserIcon()}</span>`;
  const profileContent = session ? `
      <p><strong>${session.name}</strong></p>
      <p>${session.email}</p>
      <a class="button-link" href="/logout">Logout</a>
    ` : `
      <p>Sign in to continue.</p>
      <a class="button-link button-link--primary" href="/login">Sign in</a>
      <a class="button-link" href="/signup">Sign up</a>
    `;
  const notificationContent = `
      <hr />
      <p><strong>Notifications</strong></p>
      <p>No notifications yet.</p>
    `;
  return `
    <details class="profile-menu">
      <summary>${trigger}</summary>
      <div class="dropdown">
        ${profileContent}
        <div class="profile-notifications">
          ${notificationContent}
        </div>
      </div>
    </details>
  `;
}, "renderProfileMenu");

// src/ui/styles/base/reset.ts
var baseStyles = `
  @import url("../assets/fonts/fonts.css");

  :root {
    color-scheme: light;
    --font-body: "Source Sans 3", "Segoe UI", sans-serif;
    --font-display: "Source Serif 4", "Georgia", serif;
    --color-bg: #f5f3ee;
    --color-bg-alt: #eef3f9;
    --color-surface: #ffffff;
    --color-surface-muted: #f1f4f8;
    --color-surface-elevated: #e7ecf4;
    --color-text: #1b2533;
    --color-text-muted: #5b6676;
    --color-border: #d1d9e6;
    --color-border-strong: #b8c3d6;
    --color-link: #1f5fa5;
    --color-link-hover: #133f6c;
    --color-accent: #1f7ea5;
    --color-accent-strong: #165d7e;
    --color-danger: #8b1f3c;
    --color-danger-bg: #ffe8ed;
    --color-success: #2d5016;
    --color-success-bg: #e7f2dc;
    --color-overlay: rgba(255, 255, 255, 0.88);
    --shadow-sm: 0 1px 3px rgba(15, 23, 42, 0.05);
    --shadow-md: 0 2px 6px rgba(15, 23, 42, 0.08);
    --radius-sm: 4px;
    --radius-md: 4px;
    --radius-lg: 4px;
  }

  :root[data-theme="dark"] {
    color-scheme: dark;
    --color-bg: #0e141b;
    --color-bg-alt: #141c27;
    --color-surface: #18212d;
    --color-surface-muted: #1d2736;
    --color-surface-elevated: #233145;
    --color-text: #e4edf9;
    --color-text-muted: #98a7bb;
    --color-border: #2b374b;
    --color-border-strong: #3a4a63;
    --color-link: #8cc7ff;
    --color-link-hover: #c1e2ff;
    --color-accent: #5fc8ff;
    --color-accent-strong: #2ea9e0;
    --color-danger: #f19cb3;
    --color-danger-bg: #3a1f2a;
    --color-success: #8dd17b;
    --color-success-bg: #1f2c1b;
    --color-overlay: rgba(10, 14, 19, 0.8);
    --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.15);
    --shadow-md: 0 2px 6px rgba(0, 0, 0, 0.2);
  }

  body {
    background: var(--color-bg);
    color: var(--color-text);
    font-family: var(--font-body);
  }

  button,
  input,
  select,
  textarea {
    font: inherit;
    color: inherit;
  }
`;

// src/ui/styles/components/forms.ts
var componentStyles = `
  button {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 4px;
    padding: 8px 12px;
    cursor: pointer;
    transition: background-color 0.2s ease, border-color 0.2s ease;
  }

  button:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }

  button:hover { 
    background: var(--color-surface-muted); 
    border-color: var(--color-border-strong); 
  }
  
  button:active { 
    background: var(--color-surface-elevated); 
  }

  input,
  select,
  textarea {
    border: 1px solid var(--color-border);
    border-radius: 4px;
    padding: 8px 10px;
    background: var(--color-surface);
  }

  input:focus,
  select:focus,
  textarea:focus {
    outline: 2px solid var(--color-accent);
    outline-offset: 1px;
    border-color: var(--color-accent);
  }
`;

// src/ui/styles/pages/global.ts
var pageStyles = `
  /* Page-specific styles can be added here */
`;

// src/ui/styles/theme.ts
var themeStyles = `
  ${baseStyles}
  ${componentStyles}
  ${pageStyles}
`;

// src/ui/layouts/styles.ts
var baseStyles2 = `
  ${themeStyles}
  
  /* Critical performance optimizations */
  * { 
    box-sizing: border-box; 
    will-change: auto; /* Optimize animations */
  }
  
  html, body { 
    height: 100%; 
    scroll-behavior: smooth;
  }
  
  body { 
    margin: 0; 
    overflow: hidden;
    font-display: swap; /* Faster font loading */
    text-rendering: optimizeSpeed; /* Faster text rendering */
    contain: layout style paint; /* Performance optimization */
  }
  
  a { 
    color: var(--color-link); 
    text-decoration: none; 
    transition: color 0.2s ease; /* Hardware accelerated */
  }
  
  a:hover { 
    color: var(--color-link-hover); 
  }
  
  hr { 
    border: none; 
    border-top: 1px solid var(--color-border); 
    margin: 12px 0; 
  }
  
  h1, h2, h3, h4 { 
    font-family: var(--font-display);
    font-display: swap; /* Faster heading rendering */
  }
  
  .icon { 
    width: 18px; 
    height: 18px; 
    display: inline-block; 
    transform: translateZ(0); /* Hardware acceleration */
  }
  .button-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    padding: 6px 10px;
    border-radius: 4px;
    border: 1px solid var(--color-border);
    background: transparent;
    color: var(--color-text);
    text-decoration: none;
    transition: background-color 0.2s ease, color 0.2s ease, border-color 0.2s ease;
  }
  .button-link:hover {
    background: var(--color-surface-muted);
    border-color: var(--color-border-strong);
  }
  .button-link:active { background: var(--color-surface-elevated); }
  .button-link--primary {
    background: var(--color-accent);
    color: #fff;
    border-color: var(--color-accent);
  }
  .button-link--primary:hover {
    background: var(--color-accent-strong);
    border-color: var(--color-accent-strong);
  }
  .button-link--danger {
    background: var(--color-danger);
    color: #fff;
    border-color: var(--color-danger);
  }
  .button-link--danger:hover {
    background: var(--color-danger-bg);
    color: var(--color-danger);
    border-color: var(--color-border-strong);
  }
  .page { max-width: 720px; margin: 0 auto; display: grid; gap: 12px; animation: page-enter 0.35s ease; }
  .page-header { display: grid; gap: 6px; text-align: center; align-items: center; }
  .page-title { margin: 0; font-size: 28px; letter-spacing: 0.3px; font-weight: 600; }
  .page-subtitle { margin: 0; color: var(--color-text-muted); }
  .app-main { transition: opacity 0.2s ease, transform 0.2s ease; }
  body.is-transitioning .app-main { opacity: 0.7; transform: translateY(4px); }
  .app-main__breadcrumb {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 6px 16px;
    border-bottom: 1px solid var(--color-border);
    background: var(--color-surface);
    color: var(--color-text-muted);
    font-size: 13px;
    overflow-x: auto;
    white-space: nowrap;
    scrollbar-width: thin;
    -webkit-overflow-scrolling: touch;
  }
  .app-main__breadcrumb:empty { display: none; }
  .breadcrumb {
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }
  .breadcrumb a { color: var(--color-text-muted); }
  .breadcrumb a:hover { color: var(--color-text); }
  .breadcrumb__current { color: var(--color-text); font-weight: 600; }
  .breadcrumb__separator { color: var(--color-text-muted); }
  .page-section { display: grid; gap: 10px; animation: fade-in 0.3s ease; }
  .page-actions { display: flex; justify-content: center; flex-wrap: wrap; gap: 8px; }
  .section-title { margin: 0; font-size: 20px; text-align: center; font-weight: 600; }
  .card-grid { display: grid; gap: 10px; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); }
  .card-link {
    display: grid;
    gap: 4px;
    padding: 10px;
    border-radius: 4px;
    border: 1px solid var(--color-border);
    background: transparent;
    color: var(--color-text);
    text-decoration: none;
    transition: background-color 0.2s ease, color 0.2s ease, border-color 0.2s ease;
  }
  .card-link:hover {
    background: var(--color-surface-muted);
    border-color: var(--color-border-strong);
  }
  .card-link:active { background: var(--color-surface-elevated); }
  .card-link__title {
    font-size: 14px;
    font-weight: 600;
    line-height: 1.4;
    margin: 0;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .card-link__description {
    font-size: 12px;
    color: var(--color-text-muted);
    line-height: 1.3;
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .card-link__meta {
    font-size: 11px;
    color: var(--color-text-muted);
    margin: 0;
  }
  .form-card {
    background: transparent;
    border: 1px solid var(--color-border);
    border-radius: 4px;
    padding: 16px;
  }
  .form-grid { display: grid; gap: 12px; }
  .form-field {
    display: grid;
    gap: 6px;
    font-size: 13px;
    color: var(--color-text);
  }
  .form {
    display: grid;
    gap: 12px;
    max-width: 400px;
    margin: 0 auto;
    animation: fade-in 0.3s ease;
  }
  .form-group {
    display: grid;
    gap: 4px;
  }
  .form-label {
    font-size: 13px;
    font-weight: 600;
    color: var(--color-text);
  }
  .form-input {
    padding: 8px 10px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background: var(--color-surface);
    color: var(--color-text);
    font-size: 14px;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }
  .form-input:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(31, 126, 165, 0.18);
  }
  .form-input::placeholder { color: var(--color-text-muted); }
  .form-error {
    font-size: 12px;
    color: var(--color-danger);
    margin: 0;
  }
  .form-success {
    font-size: 12px;
    color: var(--color-success);
    margin: 0;
  }
  .form-actions {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 8px;
  }
  .helper-text { font-size: 13px; color: var(--color-text-muted); margin: 0; }
  .alert {
    padding: 8px 10px;
    border-radius: 4px;
    border: 1px solid var(--color-border);
    background: var(--color-surface-elevated);
    color: var(--color-text);
    font-size: 13px;
  }
  .alert--error {
    background: var(--color-danger-bg);
    color: var(--color-danger);
    border-color: var(--color-danger);
  }
  .table-responsive {
    overflow-x: auto;
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-border);
    background: var(--color-surface);
  }
  .data-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 13px;
  }
  .data-table th {
    text-align: left;
    padding: 8px 12px;
    border-bottom: 1px solid var(--color-border);
    background: var(--color-surface-elevated);
    font-weight: 600;
    color: var(--color-text);
  }
  .data-table td {
    padding: 8px 12px;
    border-bottom: 1px solid var(--color-border);
    color: var(--color-text);
  }
  .data-table tr:last-child td { border-bottom: none; }
  .data-table a { color: var(--color-link); }
  .data-table a:hover { color: var(--color-link-hover); }
  .app-sidebar { display: flex; flex-direction: column; gap: 12px; }
  .sidebar-footer {
    margin-top: auto;
    padding-top: 12px;
    border-top: 1px solid var(--color-border);
  }
  .theme-toggle {
    width: 100%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 8px 10px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-border);
    background: var(--color-surface);
    color: var(--color-text);
    cursor: pointer;
    transition: background-color 0.2s ease, border-color 0.2s ease, transform 0.2s ease;
  }
  .theme-toggle:hover {
    background: var(--color-surface-muted);
    border-color: var(--color-border-strong);
    transform: translateY(-1px);
  }
  .theme-toggle__icon { display: inline-flex; align-items: center; }
  .theme-toggle__icon--moon { display: none; }
  .theme-toggle__label { font-size: 13px; font-weight: 600; }
  :root[data-theme="dark"] .theme-toggle__icon--sun { display: none; }
  :root[data-theme="dark"] .theme-toggle__icon--moon { display: inline-flex; }
  .loader {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--color-overlay);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.2s ease, visibility 0.2s ease;
  }
  .loader.active {
    opacity: 1;
    visibility: visible;
  }
  .loader__spinner {
    width: 32px;
    height: 32px;
    border: 3px solid var(--color-border);
    border-top-color: var(--color-accent);
    border-radius: 50%;
    animation: spin 1s linear infinite;
  }
  @keyframes page-enter {
    from { opacity: 0; transform: translateY(8px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes fade-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  @keyframes spin {
    to { transform: rotate(360deg); }
  }
  .user-management-toolbar {
    display: flex;
    flex-direction: column;
    gap: 12px;
    border: 1px solid var(--color-border);
    border-radius: 4px;
    padding: 16px;
    background: transparent;
  }
  .filter-form {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .filter-fields {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 12px;
  }
  .filter-actions {
    display: flex;
    justify-content: flex-start;
    gap: 8px;
    flex-wrap: wrap;
  }
  .toolbar-actions {
    display: flex;
    justify-content: flex-end;
    padding-top: 8px;
    border-top: 1px solid var(--color-border);
  }
  .table-scroll {
    overflow-x: auto;
    border-radius: 4px;
    border: 1px solid var(--color-border);
    background: transparent;
  }
  .filter-field {
    display: grid;
    gap: 6px;
    font-size: 13px;
    color: var(--color-text);
  }
  .filter-field span {
    font-weight: 600;
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: var(--color-text-muted);
  }
  .filter-field select,
  .filter-field input {
    padding: 8px 12px;
    border: 1px solid var(--color-border);
    border-radius: 4px;
    background: var(--color-surface);
    color: var(--color-text);
    font-size: 14px;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }
  .filter-field select:focus,
  .filter-field input:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(31, 126, 165, 0.18);
  }
  .filter-actions {
    display: flex;
    justify-content: flex-end;
    gap: 8px;
    flex-wrap: wrap;
  }
  .confirm-delete {
    position: relative;
    display: inline-block;
  }
  .confirm-delete__toggle {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }
  .confirm-delete__modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--color-overlay);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s ease;
  }
  .confirm-delete__toggle:checked ~ .confirm-delete__modal {
    opacity: 1;
    visibility: visible;
  }
  .confirm-delete__panel {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 4px;
    padding: 20px;
    max-width: 400px;
    width: 90%;
    max-height: 90vh;
    overflow-y: auto;
    transform: scale(0.9);
    transition: transform 0.3s ease;
  }
  .confirm-delete__toggle:checked ~ .confirm-delete__modal .confirm-delete__panel {
    transform: scale(1);
  }
  .confirm-delete__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
  }
  .confirm-delete__title {
    margin: 0;
    font-size: 18px;
    font-weight: 600;
    color: var(--color-text);
  }
  .confirm-delete__close {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border-radius: 4px;
    background: var(--color-surface-muted);
    color: var(--color-text-muted);
    cursor: pointer;
    font-size: 18px;
    font-weight: 600;
    transition: background-color 0.2s ease, color 0.2s ease;
  }
  .confirm-delete__close:hover {
    background: var(--color-surface-elevated);
    color: var(--color-text);
  }
  .confirm-delete__form {
    display: grid;
    gap: 16px;
  }
  .confirm-delete__field {
    display: grid;
    gap: 6px;
  }
  .confirm-delete__field span {
    font-size: 13px;
    font-weight: 600;
    color: var(--color-text);
  }
  .confirm-delete__field input {
    padding: 10px 12px;
    border: 1px solid var(--color-border);
    border-radius: 4px;
    background: var(--color-surface);
    color: var(--color-text);
    font-size: 14px;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }
  .confirm-delete__field input:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(31, 126, 165, 0.18);
  }
  .confirm-delete__actions {
    display: flex;
    justify-content: flex-end;
    gap: 8px;
    flex-wrap: wrap;
  }
  .data-table tr {
    transition: background-color 0.2s ease;
  }
  .data-table tbody tr:hover {
    background: var(--color-surface-muted);
    cursor: pointer;
  }
  .data-table tbody tr:hover td {
    color: var(--color-link);
  }
  .table-row-link {
    display: block;
    color: inherit;
    text-decoration: none;
    padding: 8px 12px;
    border: none;
    background: none;
    width: 100%;
    height: 100%;
    text-align: left;
    font: inherit;
  }
  .table-row-link:hover {
    color: var(--color-link);
  }
  .data-table td {
    max-width: 0;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    padding: 0;
    border-bottom: 1px solid var(--color-border);
    color: var(--color-text);
    position: relative;
  }
  .data-table td > * {
    padding: 8px 12px;
    display: block;
  }
  .data-table td:nth-child(1) { /* Role */
    max-width: 80px;
    text-align: center;
    font-weight: 600;
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }
  .data-table td:nth-child(2) { /* Name */
    max-width: 150px;
    font-weight: 500;
  }
  .data-table td:nth-child(3) { /* Email */
    max-width: 200px;
    font-family: monospace;
    font-size: 12px;
  }
  .data-table td:nth-child(4) { /* Created */
    max-width: 100px;
    font-size: 12px;
    color: var(--color-text-muted);
  }
  .data-table td:nth-child(5) { /* Actions */
    max-width: 80px;
    text-align: center;
  }
  .data-table td.table-actions {
    text-align: center;
    max-width: 120px;
  }
  .table-meta {
    font-size: 11px;
    color: var(--color-text-muted);
    font-weight: 400;
  }
  .table-link {
    color: var(--color-link);
    text-decoration: none;
    font-weight: 500;
  }
  .table-link:hover {
    color: var(--color-link-hover);
    text-decoration: underline;
  }
`;

// src/ui/layouts/client-script.ts
var clientScript = `
  // Advanced performance optimizations
  let rafId = null;
  
  // Performance monitoring
  const measurePerformance = () => {
    if ('performance' in window) {
      const navigation = performance.getEntriesByType('navigation')[0];
      console.log('Page load time:', navigation.loadEventEnd - navigation.loadEventStart);
      
      // Largest Contentful Paint
      new PerformanceObserver((list) => {
        const entries = list.getEntries();
        const lastEntry = entries[entries.length - 1];
        console.log('LCP:', lastEntry.startTime);
      }).observe({ entryTypes: ['largest-contentful-paint'] });
    }
  };
  
  // Network-aware loading
  const optimizeForNetwork = () => {
    if ('connection' in navigator) {
      const connection = (navigator as any).connection;
      if (connection.saveData || connection.effectiveType === 'slow-2g') {
        document.body.classList.add('reduced-data');
        // Disable animations for slow connections
        document.documentElement.style.setProperty('--animation-duration', '0s');
      }
    }
  };
  
  // Predictive prefetching
  const setupPrefetching = () => {
    let prefetchTimeout;
    document.addEventListener('mouseover', (e) => {
      const link = e.target.closest('a');
      if (link && link.href) {
        prefetchTimeout = setTimeout(() => {
          const prefetchLink = document.createElement('link');
          prefetchLink.rel = 'prefetch';
          prefetchLink.href = link.href;
          document.head.appendChild(prefetchLink);
        }, 100);
      }
    });
    document.addEventListener('mouseout', () => {
      clearTimeout(prefetchTimeout);
    });
  };
  
  // Lazy loading for images
  const setupLazyLoading = () => {
    const imageObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const img = entry.target;
          if (img.dataset.src) {
            img.src = img.dataset.src;
            img.classList.remove('lazy');
            imageObserver.unobserve(img);
          }
        }
      });
    });
    
    document.querySelectorAll('img[data-src]').forEach(img => {
      imageObserver.observe(img);
    });
  };
  
  // Request batching
  class RequestBatcher {
    constructor(delay = 100) {
      this.queue = [];
      this.delay = delay;
      this.timeout = null;
    }
    
    add(url, options) {
      return new Promise((resolve) => {
        this.queue.push({ url, options: options || {}, resolve });
        
        if (!this.timeout) {
          this.timeout = setTimeout(() => this.flush(), this.delay);
        }
      });
    }
    
    async flush() {
      const requests = this.queue.splice(0);
      this.timeout = null;
      
      await Promise.all(
        requests.map(({ url, options, resolve }) =>
          fetch(url, options).then(resolve)
        )
      );
    }
  }
  
  const debounce = (func, wait) => {
    let timeout;
    return function executedFunction(...args) {
      const later = () => {
        clearTimeout(timeout);
        func(...args);
      };
      clearTimeout(timeout);
      timeout = setTimeout(later, wait);
    };
  };
  
  const throttle = (func, limit) => {
    let inThrottle;
    return function() {
      const args = arguments;
      const context = this;
      if (!inThrottle) {
        func.apply(context, args);
        inThrottle = true;
        setTimeout(() => inThrottle = false, limit);
      }
    };
  };
  
  const sidebarToggle = document.getElementById("sidebar-toggle");
  const sidebarToggleLabel = document.querySelector(".sidebar-toggle__label");
  const sidebarViewportQuery = window.matchMedia("(max-width: 768px)");
  const loader = document.querySelector(".loader");
  const breadcrumbRoot = document.getElementById("breadcrumb");
  const themeToggles = document.querySelectorAll("[data-theme-toggle]");
  const csrfMeta = document.querySelector("meta[name='csrf-token']");
  const csrfToken = csrfMeta?.getAttribute("content") || null;
  
  const activateLoader = () => {
    loader?.classList.add("active");
  };
  
  const deactivateLoader = () => {
    loader?.classList.remove("active");
  };
  
  const resetTransition = () => {
    document.body.classList.remove("is-transitioning");
  };
  
  const applySidebarState = async () => {
    if (!sidebarToggle || !sidebarToggleLabel) return;
    const isMobile = sidebarViewportQuery.matches;
    sidebarToggleLabel.style.display = isMobile ? "flex" : "none";
    
    // Load sidebar state from user account or localStorage (only for desktop/tablet)
    if (!isMobile) {
      const savedState = await loadSidebarState();
      if (savedState === "minimized") {
        sidebarToggle.checked = true;
      } else if (savedState === "expanded") {
        sidebarToggle.checked = false;
      }
    }
    
    if (isMobile && sidebarToggle.checked) {
      sidebarToggle.checked = false;
    }
  };

  const applyTheme = (theme) => {
    const root = document.documentElement;
    root.setAttribute("data-theme", theme);
    themeToggles.forEach((toggle) => {
      toggle.setAttribute("aria-pressed", theme === "dark" ? "true" : "false");
    });
  };

  const resolveTheme = () => {
    const stored = window.localStorage?.getItem("theme");
    if (stored === "dark" || stored === "light") {
      return stored;
    }
    return "light";
  };

  const toggleTheme = () => {
    const current = document.documentElement.getAttribute("data-theme") || "light";
    const next = current === "dark" ? "light" : "dark";
    window.localStorage?.setItem("theme", next);
    applyTheme(next);
  };

  const ensureCsrfInput = (form) => {
    if (!csrfToken) return;
    if (form.querySelector("input[name='csrf_token']")) return;
    const input = document.createElement("input");
    input.type = "hidden";
    input.name = "csrf_token";
    input.value = csrfToken;
    form.appendChild(input);
  };
  
  const saveSidebarState = async (state: "minimized" | "expanded") => {
    // Always save to localStorage as fallback
    window.localStorage?.setItem("sidebar-state", state);
    
    // Try to save to user account if logged in
    try {
      const response = await fetch("/api/user/preferences", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          ...(csrfToken && { "X-CSRF-Token": csrfToken })
        },
        body: JSON.stringify({ sidebarState: state })
      });
      
      // If user is not logged in, that's okay - we'll use localStorage
      if (response.status === 401) {
        console.log("User not logged in, using localStorage only");
      }
    } catch (error) {
      console.log("Failed to save to user account, using localStorage:", error);
    }
  };

  const loadSidebarState = async (): Promise<"minimized" | "expanded"> => {
    // First try to load from user account if logged in
    try {
      const response = await fetch("/api/user/preferences");
      if (response.ok) {
        const data = await response.json();
        if (data.sidebarState) {
          return data.sidebarState;
        }
      }
    } catch (error) {
      console.log("Failed to load from user account, using localStorage:", error);
    }
    
    // Fallback to localStorage
    const savedState = window.localStorage?.getItem("sidebar-state");
    return savedState === "minimized" ? "minimized" : "expanded";
  };

  const handleSidebarChange = async () => {
    if (!sidebarToggle) return;
    const isChecked = sidebarToggle.checked;
    const state = isChecked ? "minimized" : "expanded";
    
    // Save to both user account and localStorage
    await saveSidebarState(state);
    
    if (isChecked) {
      document.body.style.overflow = "hidden";
    } else {
      document.body.style.overflow = "";
    }
  };
  
  const renderBreadcrumbs = () => {
    if (!breadcrumbRoot) return;
    const crumbs = window.breadcrumbCrumbs || [];
    if (!crumbs.length) {
      breadcrumbRoot.textContent = "";
      return;
    }
    
    // Clear existing content
    breadcrumbRoot.textContent = "";
    
    // Create breadcrumb container
    const breadcrumbContainer = document.createElement("div");
    breadcrumbContainer.className = "breadcrumb";
    
    crumbs.forEach((item, index) => {
      const isLast = index === crumbs.length - 1;
      
      if (isLast) {
        const current = document.createElement("span");
        current.className = "breadcrumb__current";
        current.setAttribute("aria-current", "page");
        current.textContent = item.label;
        breadcrumbContainer.appendChild(current);
      } else {
        const link = document.createElement("a");
        link.href = item.href;
        link.textContent = item.label;
        breadcrumbContainer.appendChild(link);
        
        const separator = document.createElement("span");
        separator.className = "breadcrumb__separator";
        separator.textContent = "\u203A";
        breadcrumbContainer.appendChild(separator);
      }
    });
    
    breadcrumbRoot.appendChild(breadcrumbContainer);
    window.requestAnimationFrame(() => {
      breadcrumbRoot.scrollLeft = breadcrumbRoot.scrollWidth;
    });
  };

  window.addEventListener("pageshow", async () => {
    deactivateLoader();
    resetTransition();
    await applySidebarState();
    renderBreadcrumbs();
    applyTheme(resolveTheme());
    
    // Initialize performance optimizations
    measurePerformance();
    optimizeForNetwork();
    setupPrefetching();
    setupLazyLoading();
  });
  
  await applySidebarState();
  renderBreadcrumbs();
  applyTheme(resolveTheme());
  
  // Initialize performance features early
  measurePerformance();
  optimizeForNetwork();
  setupPrefetching();
  setupLazyLoading();
  sidebarViewportQuery.addEventListener("change", applySidebarState);
  sidebarToggle?.addEventListener("change", handleSidebarChange);
  themeToggles.forEach((toggle) => {
    toggle.addEventListener("click", () => {
      toggleTheme();
    });
  });
  document.addEventListener("submit", (event) => {
    const target = event.target;
    if (target instanceof HTMLFormElement && target.target !== "_blank") {
      ensureCsrfInput(target);
      activateLoader();
      document.body.classList.add("is-transitioning");
    }
  });
  document.addEventListener("click", (event) => {
    const target = event.target;
    if (!(target instanceof Element)) return;
    const link = target.closest("a");
    if (!link || link.target === "_blank") return;
    const href = link.getAttribute("href");
    if (!href || href.startsWith("#") || href.startsWith("mailto:") || href.startsWith("tel:")) return;
    const url = new URL(href, window.location.href);
    if (url.origin !== window.location.origin) return;
    activateLoader();
    document.body.classList.add("is-transitioning");
  });
`;

// src/ui/layouts/pageLayout.ts
var renderShell = /* @__PURE__ */ __name((device, header, sidebar, content, footer) => {
  if (device === "mobile") {
    return renderContentShellMobile({ header, sidebar, main: content, footer });
  }
  if (device === "tablet") {
    return renderContentShellTablet({ header, sidebar, main: content, footer });
  }
  return renderContentShellDesktop({ header, sidebar, main: content, footer });
}, "renderShell");
var renderShellStyles = /* @__PURE__ */ __name((device) => {
  if (device === "mobile") {
    return mobileShellStyles;
  }
  if (device === "tablet") {
    return `${tabletShellStyles}
${sidebarTabletStyles}`;
  }
  return `${desktopShellStyles}
${sidebarDesktopStyles}`;
}, "renderShellStyles");
var renderPageLayout = /* @__PURE__ */ __name(({ device, content, session, csrfToken, nonce }) => {
  const profileMenu = renderProfileMenu({ session });
  const notificationMenu = renderNotificationMenu({ session });
  const header = device === "mobile" ? renderHeaderMobile({ siteName: "freeducation", profileMenu }) : device === "tablet" ? renderHeaderTablet({ siteName: "freeducation", profileMenu, notificationMenu }) : renderHeaderDesktop({ siteName: "freeducation", profileMenu, notificationMenu });
  const sidebar = device === "mobile" ? renderSidebarMobile({ session }) : device === "tablet" ? renderSidebarTablet({ session }) : renderSidebarDesktop({ session });
  const footer = device === "mobile" ? renderFooterDesktop() : device === "tablet" ? renderFooterDesktop() : renderFooterDesktop();
  const body = renderShell(device, header, sidebar, content, footer);
  const styles = `${baseStyles2}
${renderShellStyles(device)}`;
  const csrfMeta = csrfToken ? `<meta name="csrf-token" content="${csrfToken}" />` : "";
  const scriptNonce = nonce ? ` nonce="${nonce}"` : "";
  return `<!DOCTYPE html>
<html lang="en" data-theme="light">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    ${csrfMeta}
    <title>freeducation</title>
    <style>${styles}</style>
  </head>
  <body>
    ${body}
    <div class="loader" id="page-loader" aria-hidden="true">
      <div class="loader__spinner"></div>
    </div>
    <script${scriptNonce}>${clientScript}<\/script>
  </body>
</html>`;
}, "renderPageLayout");

// src/core/config/api.ts
var CONTENT_TYPES = {
  JSON: "application/json",
  HTML: "text/html; charset=utf-8",
  PLAIN: "text/plain; charset=utf-8",
  FORM: "application/x-www-form-urlencoded"
};

// src/core/config/constants/http.ts
var HTTP_STATUS = {
  OK: 200,
  CREATED: 201,
  BAD_REQUEST: 400,
  UNAUTHORIZED: 401,
  FORBIDDEN: 403,
  NOT_FOUND: 404,
  INTERNAL_SERVER_ERROR: 500
};

// src/core/config/constants/session.ts
var SESSION_CONFIG = {
  COOKIE_NAME: "admin_session",
  MAX_AGE: 604800,
  // 7 days in seconds
  PATH: "/"
};

// src/core/http/response.ts
var jsonResponse = /* @__PURE__ */ __name((body, status = HTTP_STATUS.OK) => new Response(JSON.stringify(body), {
  status,
  headers: { "content-type": CONTENT_TYPES.JSON }
}), "jsonResponse");
var htmlResponse = /* @__PURE__ */ __name((html, status = HTTP_STATUS.OK, headers) => new Response(html, {
  status,
  headers: { "content-type": CONTENT_TYPES.HTML, ...headers }
}), "htmlResponse");
var redirectResponse = /* @__PURE__ */ __name((location, headers) => new Response(null, {
  status: 302,
  headers: { Location: location, ...headers }
}), "redirectResponse");
var badRequest = /* @__PURE__ */ __name((message) => jsonResponse({ error: message }, HTTP_STATUS.BAD_REQUEST), "badRequest");
var serviceError = /* @__PURE__ */ __name((message) => new Response(message, { status: HTTP_STATUS.INTERNAL_SERVER_ERROR, headers: { "content-type": CONTENT_TYPES.PLAIN } }), "serviceError");

// src/core/http/device.ts
var getDeviceType = /* @__PURE__ */ __name((userAgent) => {
  const agent = userAgent?.toLowerCase() ?? "";
  if (/mobi|android|iphone|ipod/.test(agent)) {
    return "mobile";
  }
  if (/ipad|tablet/.test(agent)) {
    return "tablet";
  }
  return "desktop";
}, "getDeviceType");

// src/core/middleware/csrf.ts
var CSRF_CONFIG = {
  tokenLength: 32,
  cookieName: "csrf_token",
  headerName: "x-csrf-token",
  formFieldName: "csrf_token",
  maxAgeSeconds: 60 * 60
  // 1 hour
};
var toHex = /* @__PURE__ */ __name((buffer) => Array.from(buffer, (byte) => byte.toString(16).padStart(2, "0")).join(""), "toHex");
var timingSafeEqual = /* @__PURE__ */ __name((a, b) => {
  if (a.length !== b.length) {
    return false;
  }
  let mismatch = 0;
  for (let index = 0; index < a.length; index += 1) {
    mismatch |= a.charCodeAt(index) ^ b.charCodeAt(index);
  }
  return mismatch === 0;
}, "timingSafeEqual");
var createCSRFToken = /* @__PURE__ */ __name(() => {
  const bytes = crypto.getRandomValues(new Uint8Array(CSRF_CONFIG.tokenLength));
  return toHex(bytes);
}, "createCSRFToken");
var getCSRFCookie = /* @__PURE__ */ __name((request) => {
  const cookieHeader = request.headers.get("cookie");
  if (!cookieHeader) {
    return null;
  }
  const cookies = cookieHeader.split(";");
  for (const cookie of cookies) {
    const [key, ...rest] = cookie.trim().split("=");
    if (key === CSRF_CONFIG.cookieName) {
      return rest.join("=");
    }
  }
  return null;
}, "getCSRFCookie");
var setCSRFCookie = /* @__PURE__ */ __name((token) => `${CSRF_CONFIG.cookieName}=${token}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=${CSRF_CONFIG.maxAgeSeconds}`, "setCSRFCookie");
var extractCSRFToken = /* @__PURE__ */ __name(async (request) => {
  const headerToken = request.headers.get(CSRF_CONFIG.headerName);
  if (headerToken) {
    return headerToken;
  }
  const contentType = request.headers.get("content-type") || "";
  if (contentType.includes("application/x-www-form-urlencoded") || contentType.includes("multipart/form-data")) {
    const cloned = request.clone();
    const formData = await cloned.formData().catch(() => null);
    const token = formData?.get(CSRF_CONFIG.formFieldName);
    return typeof token === "string" ? token : null;
  }
  return null;
}, "extractCSRFToken");
var validateCSRFToken = /* @__PURE__ */ __name((token, cookie) => {
  if (!token || !cookie) {
    return false;
  }
  return timingSafeEqual(token, cookie);
}, "validateCSRFToken");

// src/modules/subjects/bangla/9-10/first-paper/data.ts
var sahapathTable = "bangla_9_10_sahapath_items";
var literatureTable = "bangla_9_10_literature_items";
var listBanglaNineTenSahapathItems = /* @__PURE__ */ __name(async (db) => {
  const result = await db.prepare(`SELECT id, category, title, created_at as createdAt FROM ${sahapathTable} ORDER BY createdAt ASC`).all();
  return result.results;
}, "listBanglaNineTenSahapathItems");
var getBanglaNineTenSahapathItem = /* @__PURE__ */ __name(async (db, itemId) => {
  const result = await db.prepare(`SELECT id, category, title, created_at as createdAt FROM ${sahapathTable} WHERE id = ?`).bind(itemId).all();
  return result.results[0] ?? null;
}, "getBanglaNineTenSahapathItem");
var createBanglaNineTenSahapathItem = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(`INSERT INTO ${sahapathTable} (category, title, created_at) VALUES (?, ?, ?)`).bind(payload.category, payload.title, createdAt).run();
}, "createBanglaNineTenSahapathItem");
var updateBanglaNineTenSahapathItem = /* @__PURE__ */ __name(async (db, payload) => {
  await db.prepare(`UPDATE ${sahapathTable} SET category = ?, title = ? WHERE id = ?`).bind(payload.category, payload.title, payload.id).run();
}, "updateBanglaNineTenSahapathItem");
var updateBanglaNineTenSahapathItemTitle = /* @__PURE__ */ __name(async (db, payload) => {
  await db.prepare(`UPDATE ${sahapathTable} SET title = ? WHERE id = ?`).bind(payload.title, payload.id).run();
}, "updateBanglaNineTenSahapathItemTitle");
var updateBanglaNineTenSahapathItemCategory = /* @__PURE__ */ __name(async (db, payload) => {
  await db.prepare(`UPDATE ${sahapathTable} SET category = ? WHERE id = ?`).bind(payload.category, payload.id).run();
}, "updateBanglaNineTenSahapathItemCategory");
var deleteBanglaNineTenSahapathItem = /* @__PURE__ */ __name(async (db, itemId) => {
  await db.prepare(`DELETE FROM ${sahapathTable} WHERE id = ?`).bind(itemId).run();
}, "deleteBanglaNineTenSahapathItem");
var listBanglaNineTenLiteratureItems = /* @__PURE__ */ __name(async (db, category) => {
  const result = await db.prepare(
    `SELECT id, category, title, created_at as createdAt FROM ${literatureTable} WHERE category = ? ORDER BY createdAt ASC`
  ).bind(category).all();
  return result.results;
}, "listBanglaNineTenLiteratureItems");
var getBanglaNineTenLiteratureItem = /* @__PURE__ */ __name(async (db, itemId) => {
  const result = await db.prepare(`SELECT id, category, title, created_at as createdAt FROM ${literatureTable} WHERE id = ?`).bind(itemId).all();
  return result.results[0] ?? null;
}, "getBanglaNineTenLiteratureItem");
var createBanglaNineTenLiteratureItem = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(`INSERT INTO ${literatureTable} (category, title, created_at) VALUES (?, ?, ?)`).bind(payload.category, payload.title, createdAt).run();
}, "createBanglaNineTenLiteratureItem");
var updateBanglaNineTenLiteratureItem = /* @__PURE__ */ __name(async (db, payload) => {
  await db.prepare(`UPDATE ${literatureTable} SET title = ? WHERE id = ?`).bind(payload.title, payload.id).run();
}, "updateBanglaNineTenLiteratureItem");
var deleteBanglaNineTenLiteratureItem = /* @__PURE__ */ __name(async (db, itemId) => {
  await db.prepare(`DELETE FROM ${literatureTable} WHERE id = ?`).bind(itemId).run();
}, "deleteBanglaNineTenLiteratureItem");

// src/modules/subjects/bangla/9-10/first-paper/views.ts
var escapeValue = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var sahapathLabels = {
  natok: "\u09A8\u09BE\u099F\u0995",
  uponnash: "\u0989\u09AA\u09A8\u09CD\u09AF\u09BE\u09B8"
};
var literatureLabels = {
  goddo: "\u0997\u09A6\u09CD\u09AF",
  poddo: "\u09AA\u09A6\u09CD\u09AF"
};
var renderModal = /* @__PURE__ */ __name((id, title, body, triggerLabel, triggerClass = "button-link") => `
  <div class="modal">
    <input class="modal__toggle" type="checkbox" id="${id}" />
    <label class="${triggerClass}" for="${id}">${triggerLabel}</label>
    <div class="modal__overlay" role="dialog" aria-modal="true" aria-labelledby="${id}-title">
      <div class="modal__panel">
        <div class="modal__header">
          <h3 id="${id}-title" class="modal__title">${title}</h3>
          <label class="modal__close" for="${id}" aria-label="Close">\xD7</label>
        </div>
        ${body}
      </div>
    </div>
  </div>
`, "renderModal");
var renderSwapPrompt = /* @__PURE__ */ __name((prompt) => {
  if (!prompt) {
    return "";
  }
  return `
    <section class="form-card">
      <p class="helper-text">"${escapeValue(prompt.entryTitle)}" \u098F\u09B0 \u09A7\u09B0\u09A8 \u09AA\u09B0\u09BF\u09AC\u09B0\u09CD\u09A4\u09A8 \u0995\u09B0\u09BE \u09B9\u09B2\u09C7 \u09AC\u09BF\u09A6\u09CD\u09AF\u09AE\u09BE\u09A8 \u09A7\u09B0\u09A8 \u09AC\u09A6\u09B2\u09BE\u09AC\u09C7\u0964 \u0986\u09AA\u09A8\u09BF \u0995\u09BF \u09A7\u09B0\u09A8 \u0985\u09A6\u09B2\u09AC\u09A6\u09B2 \u0995\u09B0\u09A4\u09C7 \u099A\u09BE\u09A8?</p>
      <div class="form-actions">
        <form method="post" action="/admin/modules/subjects/bangla/9-10/first-paper/sahapath/switch">
          <input type="hidden" name="entryId" value="${prompt.entryId}" />
          <input type="hidden" name="targetId" value="${prompt.targetId}" />
          <input type="hidden" name="entryTitle" value="${escapeValue(prompt.entryTitle)}" />
          <input type="hidden" name="entryCategory" value="${escapeValue(prompt.entryCategory)}" />
          <input type="hidden" name="targetCategory" value="${escapeValue(prompt.targetCategory)}" />
          <button type="submit" class="button-link button-link--primary">\u0985\u09A6\u09B2\u09AC\u09A6\u09B2 \u09A8\u09BF\u09B6\u09CD\u099A\u09BF\u09A4 \u0995\u09B0\u09C1\u09A8</button>
        </form>
        <a class="button-link" href="/admin/modules/subjects/bangla/9-10/first-paper/sahapath">\u09AC\u09BE\u09A4\u09BF\u09B2</a>
      </div>
    </section>
  `;
}, "renderSwapPrompt");
var renderBanglaNineTenHome = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09E7\u09AE \u09AA\u09A4\u09CD\u09B0 (\u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09EF-\u09E7\u09E6)</h1>
      <p class="page-subtitle">\u098F\u0995\u099F\u09BF \u09AC\u09BF\u09AD\u09BE\u0997 \u09A8\u09BF\u09B0\u09CD\u09AC\u09BE\u099A\u09A8 \u0995\u09B0\u09C1\u09A8\u0964</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/bangla/9-10">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
        <a class="button-link" href="/admin/modules/subjects">Subjects</a>
      </div>
    </header>
    <section class="page-section">
      <div class="card-grid">
        <a class="card-link" href="/admin/modules/subjects/bangla/9-10/first-paper/sahapath">
          <span class="card-title">\u09B8\u09B9\u09AA\u09BE\u09A0</span>
        </a>
        <a class="card-link" href="/admin/modules/subjects/bangla/9-10/first-paper/literature">
          <span class="card-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09B8\u09BE\u09B9\u09BF\u09A4\u09CD\u09AF</span>
        </a>
      </div>
    </section>
  </section>
`, "renderBanglaNineTenHome");
var renderBanglaNineTenLiteratureHome = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09B8\u09BE\u09B9\u09BF\u09A4\u09CD\u09AF (\u09E7\u09AE \u09AA\u09A4\u09CD\u09B0, \u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09EF-\u09E7\u09E6)</h1>
      <p class="page-subtitle">\u098F\u0995\u099F\u09BF \u09A7\u09B0\u09A3 \u09A8\u09BF\u09B0\u09CD\u09AC\u09BE\u099A\u09A8 \u0995\u09B0\u09C1\u09A8\u0964</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/bangla/9-10/first-paper">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
      </div>
    </header>
    <section class="page-section">
      <div class="card-grid">
        <a class="card-link" href="/admin/modules/subjects/bangla/9-10/first-paper/literature/goddo">
          <span class="card-title">\u0997\u09A6\u09CD\u09AF</span>
        </a>
        <a class="card-link" href="/admin/modules/subjects/bangla/9-10/first-paper/literature/poddo">
          <span class="card-title">\u09AA\u09A6\u09CD\u09AF</span>
        </a>
      </div>
    </section>
  </section>
`, "renderBanglaNineTenLiteratureHome");
var renderBanglaNineTenSahapathList = /* @__PURE__ */ __name((props) => {
  const { items, availableCategories, successMessage, errorMessage, swapPrompt } = props;
  const addDisabled = availableCategories.length === 0;
  const modalBody = `
    <form class="form-card form-grid" method="post" action="/admin/modules/subjects/bangla/9-10/first-paper/sahapath/new">
      <label class="form-field">
        <span>\u09A7\u09B0\u09A3 \u09A8\u09BF\u09B0\u09CD\u09AC\u09BE\u099A\u09A8 \u0995\u09B0\u09C1\u09A8</span>
        <select name="category" required>
          ${availableCategories.map((category) => `<option value="${category}">${sahapathLabels[category] ?? category}</option>`).join("")}
        </select>
      </label>
      <label class="form-field">
        <span>\u09A8\u09BE\u09AE \u09B2\u09BF\u0996\u09C1\u09A8</span>
        <input name="title" type="text" required />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">\u09B8\u0982\u09B0\u0995\u09CD\u09B7\u09A3 \u0995\u09B0\u09C1\u09A8</button>
      </div>
    </form>
  `;
  const addButton = addDisabled ? `<button class="button-link" type="button" disabled>\u09AF\u09CB\u0997 \u0995\u09B0\u09BE \u09B9\u09DF\u09C7\u099B\u09C7</button>` : renderModal("add-sahapath-9-10", "\u09B8\u09B9\u09AA\u09BE\u09A0 \u09AF\u09CB\u0997 \u0995\u09B0\u09C1\u09A8", modalBody, "Add", "button-link button-link--primary");
  return `
    <section class="page">
      <header class="page-header">
        <h1 class="page-title">\u09B8\u09B9\u09AA\u09BE\u09A0 (\u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09EF-\u09E7\u09E6)</h1>
        <p class="page-subtitle">\u09A8\u09BE\u099F\u0995 \u0993 \u0989\u09AA\u09A8\u09CD\u09AF\u09BE\u09B8 \u09AF\u09CB\u0997 \u0995\u09B0\u09C1\u09A8\u0964</p>
        <div class="page-actions">
          <a class="button-link" href="/admin/modules/subjects/bangla/9-10/first-paper">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
        </div>
      </header>
      ${successMessage ? `<div class="alert">${escapeValue(successMessage)}</div>` : ""}
      ${errorMessage ? `<div class="alert alert--error">${escapeValue(errorMessage)}</div>` : ""}
      ${renderSwapPrompt(swapPrompt)}
      <section class="page-section">
        <div class="page-actions">
          ${addButton}
        </div>
        <ul class="item-list">
          ${items.length ? items.map((item) => {
    const editBody = `
                      <form class="form-card form-grid" method="post" action="/admin/modules/subjects/bangla/9-10/first-paper/sahapath/edit">
                        <input type="hidden" name="id" value="${item.id}" />
                        <label class="form-field">
                          <span>\u09A7\u09B0\u09A3</span>
                          <select name="category" required>
                            ${Object.entries(sahapathLabels).map(
      ([key, label]) => `<option value="${key}" ${item.category === key ? "selected" : ""}>${label}</option>`
    ).join("")}
                          </select>
                        </label>
                        <label class="form-field">
                          <span>\u09A8\u09BE\u09AE</span>
                          <input name="title" type="text" value="${escapeValue(item.title)}" required />
                        </label>
                        <div class="form-actions">
                          <button type="submit" class="button-link button-link--primary">\u09B9\u09BE\u09B2\u09A8\u09BE\u0997\u09BE\u09A6</button>
                        </div>
                      </form>
                    `;
    return `
                      <li class="item-card">
                        <div class="item-card__main">
                          <a class="item-card__title" href="/admin/modules/subjects/bangla/9-10/first-paper/sahapath/${item.id}">${escapeValue(item.title)}</a>
                          <span class="item-card__meta">${sahapathLabels[item.category] ?? item.category}</span>
                        </div>
                        <div class="item-card__actions">
                          ${renderModal(
      `edit-sahapath-${item.id}`,
      "\u09B8\u09B9\u09AA\u09BE\u09A0 \u09B8\u09AE\u09CD\u09AA\u09BE\u09A6\u09A8\u09BE",
      editBody,
      "\u270E",
      "button-link"
    )}
                          <div class="confirm-delete">
                            <input class="confirm-delete__toggle" type="checkbox" id="delete-sahapath-${item.id}" />
                            <label class="button-link" for="delete-sahapath-${item.id}">\u{1F5D1}\uFE0F</label>
                            <div class="confirm-delete__modal" role="dialog" aria-modal="true" aria-labelledby="delete-sahapath-${item.id}-title">
                              <div class="confirm-delete__panel">
                                <div class="confirm-delete__header">
                                  <h3 id="delete-sahapath-${item.id}-title" class="confirm-delete__title">\u09AE\u09C1\u099B\u09C7 \u09AB\u09C7\u09B2\u09C1\u09A8?</h3>
                                  <label class="confirm-delete__close" for="delete-sahapath-${item.id}" aria-label="Close">\xD7</label>
                                </div>
                                <form class="confirm-delete__form" method="post" action="/admin/modules/subjects/bangla/9-10/first-paper/sahapath/delete">
                                  <input type="hidden" name="id" value="${item.id}" />
                                  <div class="confirm-delete__actions">
                                    <label class="button-link" for="delete-sahapath-${item.id}">\u09AC\u09BE\u09A4\u09BF\u09B2</label>
                                    <button type="submit" class="button-link button-link--danger">\u09AE\u09C1\u099B\u09C7 \u09AB\u09C7\u09B2\u09C1\u09A8</button>
                                  </div>
                                </form>
                              </div>
                            </div>
                          </div>
                        </div>
                      </li>
                    `;
  }).join("") : `<li class="item-card"><span class="helper-text">\u0995\u09CB\u09A8\u09CB \u09B8\u09B9\u09AA\u09BE\u09A0 \u09AF\u09CB\u0997 \u0995\u09B0\u09BE \u09B9\u09DF\u09A8\u09BF\u0964</span></li>`}
        </ul>
      </section>
    </section>
  `;
}, "renderBanglaNineTenSahapathList");
var renderBanglaNineTenSahapathDetail = /* @__PURE__ */ __name((item) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">${escapeValue(item.title)}</h1>
      <p class="page-subtitle">${sahapathLabels[item.category] ?? item.category}</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/bangla/9-10/first-paper/sahapath">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
      </div>
    </header>
  </section>
`, "renderBanglaNineTenSahapathDetail");
var renderBanglaNineTenLiteratureList = /* @__PURE__ */ __name((props) => {
  const { items, category, successMessage, errorMessage } = props;
  const title = literatureLabels[category] ?? category;
  const addBody = `
    <form class="form-card form-grid" method="post" action="/admin/modules/subjects/bangla/9-10/first-paper/literature/${category}/new">
      <label class="form-field">
        <span>\u09A8\u09BE\u09AE \u09B2\u09BF\u0996\u09C1\u09A8</span>
        <input name="title" type="text" required />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">\u09B8\u0982\u09B0\u0995\u09CD\u09B7\u09A3 \u0995\u09B0\u09C1\u09A8</button>
      </div>
    </form>
  `;
  return `
    <section class="page">
      <header class="page-header">
        <h1 class="page-title">${title} (\u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09EF-\u09E7\u09E6)</h1>
        <p class="page-subtitle">\u09A8\u09A4\u09C1\u09A8 \u0985\u09A7\u09CD\u09AF\u09BE\u09DF \u09AF\u09CB\u0997 \u0995\u09B0\u09C1\u09A8\u0964</p>
        <div class="page-actions">
          <a class="button-link" href="/admin/modules/subjects/bangla/9-10/first-paper/literature">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
        </div>
      </header>
      ${successMessage ? `<div class="alert">${escapeValue(successMessage)}</div>` : ""}
      ${errorMessage ? `<div class="alert alert--error">${escapeValue(errorMessage)}</div>` : ""}
      <section class="page-section">
        <div class="page-actions">
          ${renderModal(`add-${category}-9-10`, `${title} \u09AF\u09CB\u0997 \u0995\u09B0\u09C1\u09A8`, addBody, "Add", "button-link button-link--primary")}
        </div>
        <ul class="item-list">
          ${items.length ? items.map((item) => {
    const editBody = `
                      <form class="form-card form-grid" method="post" action="/admin/modules/subjects/bangla/9-10/first-paper/literature/${category}/edit">
                        <input type="hidden" name="id" value="${item.id}" />
                        <label class="form-field">
                          <span>\u09A8\u09BE\u09AE</span>
                          <input name="title" type="text" value="${escapeValue(item.title)}" required />
                        </label>
                        <div class="form-actions">
                          <button type="submit" class="button-link button-link--primary">\u09B9\u09BE\u09B2\u09A8\u09BE\u0997\u09BE\u09A6</button>
                        </div>
                      </form>
                    `;
    return `
                      <li class="item-card">
                        <div class="item-card__main">
                          <a class="item-card__title" href="/admin/modules/subjects/bangla/9-10/first-paper/literature/${category}/${item.id}">${escapeValue(item.title)}</a>
                        </div>
                        <div class="item-card__actions">
                          ${renderModal(`edit-${category}-${item.id}`, "\u09B8\u09AE\u09CD\u09AA\u09BE\u09A6\u09A8\u09BE", editBody, "\u270E", "button-link")}
                          <div class="confirm-delete">
                            <input class="confirm-delete__toggle" type="checkbox" id="delete-${category}-${item.id}" />
                            <label class="button-link" for="delete-${category}-${item.id}">\u{1F5D1}\uFE0F</label>
                            <div class="confirm-delete__modal" role="dialog" aria-modal="true" aria-labelledby="delete-${category}-${item.id}-title">
                              <div class="confirm-delete__panel">
                                <div class="confirm-delete__header">
                                  <h3 id="delete-${category}-${item.id}-title" class="confirm-delete__title">\u09AE\u09C1\u099B\u09C7 \u09AB\u09C7\u09B2\u09C1\u09A8?</h3>
                                  <label class="confirm-delete__close" for="delete-${category}-${item.id}" aria-label="Close">\xD7</label>
                                </div>
                                <form class="confirm-delete__form" method="post" action="/admin/modules/subjects/bangla/9-10/first-paper/literature/${category}/delete">
                                  <input type="hidden" name="id" value="${item.id}" />
                                  <div class="confirm-delete__actions">
                                    <label class="button-link" for="delete-${category}-${item.id}">\u09AC\u09BE\u09A4\u09BF\u09B2</label>
                                    <button type="submit" class="button-link button-link--danger">\u09AE\u09C1\u099B\u09C7 \u09AB\u09C7\u09B2\u09C1\u09A8</button>
                                  </div>
                                </form>
                              </div>
                            </div>
                          </div>
                        </div>
                      </li>
                    `;
  }).join("") : `<li class="item-card"><span class="helper-text">\u0995\u09CB\u09A8\u09CB ${title} \u09AF\u09CB\u0997 \u0995\u09B0\u09BE \u09B9\u09DF\u09A8\u09BF\u0964</span></li>`}
        </ul>
      </section>
    </section>
  `;
}, "renderBanglaNineTenLiteratureList");
var renderBanglaNineTenLiteratureDetail = /* @__PURE__ */ __name((item) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">${escapeValue(item.title)}</h1>
      <p class="page-subtitle">${literatureLabels[item.category] ?? item.category}</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/bangla/9-10/first-paper/literature/${item.category}">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
      </div>
    </header>
  </section>
`, "renderBanglaNineTenLiteratureDetail");

// src/modules/subjects/bangla/9-10/first-paper/routes.ts
var sahapathCategories = ["natok", "uponnash"];
var parseNumberParam = /* @__PURE__ */ __name((value) => {
  if (!value) {
    return null;
  }
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : null;
}, "parseNumberParam");
var renderContent = /* @__PURE__ */ __name((context, content) => {
  const csrfToken = createCSRFToken();
  const layoutProps = { device: context.device, content, session: context.session, csrfToken };
  if (context.nonce) {
    layoutProps.nonce = context.nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderContent");
var buildSwapPrompt = /* @__PURE__ */ __name((url) => {
  if (url.searchParams.get("swap") !== "1") {
    return null;
  }
  const entryId = parseNumberParam(url.searchParams.get("entryId"));
  const targetId = parseNumberParam(url.searchParams.get("targetId"));
  const entryTitle = url.searchParams.get("title");
  const entryCategory = url.searchParams.get("entryCategory");
  const targetCategory = url.searchParams.get("targetCategory");
  if (!entryId || !targetId || !entryTitle || !entryCategory || !targetCategory) {
    return null;
  }
  return { entryId, targetId, entryTitle, entryCategory, targetCategory };
}, "buildSwapPrompt");
var handleBanglaNineTenFirstPaperRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const url = new URL(request.url);
  const basePath = "/admin/modules/subjects/bangla/9-10/first-paper";
  if (!url.pathname.startsWith(basePath)) {
    return null;
  }
  if (!context.session) {
    return redirectResponse("/login");
  }
  if (url.pathname === basePath) {
    if (request.method === "GET") {
      return renderContent(context, renderBanglaNineTenHome());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === `${basePath}/literature`) {
    if (request.method === "GET") {
      return renderContent(context, renderBanglaNineTenLiteratureHome());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === `${basePath}/sahapath`) {
    if (request.method === "GET") {
      const items = await listBanglaNineTenSahapathItems(env.DB);
      const usedCategories = new Set(items.map((item) => item.category));
      const availableCategories = sahapathCategories.filter((category) => !usedCategories.has(category));
      const successMessage = url.searchParams.get("updated") === "1" ? "\u09A4\u09A5\u09CD\u09AF \u09B8\u0982\u09B0\u0995\u09CD\u09B7\u09A3 \u09B9\u09DF\u09C7\u099B\u09C7\u0964" : void 0;
      const errorMessage = url.searchParams.get("error") === "invalid" ? "\u0985\u09A8\u09C1\u0997\u09CD\u09B0\u09B9 \u0995\u09B0\u09C7 \u09A4\u09A5\u09CD\u09AF \u09AA\u09C2\u09B0\u09A3 \u0995\u09B0\u09C1\u09A8\u0964" : void 0;
      const swapPrompt = buildSwapPrompt(url);
      return renderContent(
        context,
        renderBanglaNineTenSahapathList({
          items,
          availableCategories,
          ...successMessage ? { successMessage } : {},
          ...errorMessage ? { errorMessage } : {},
          ...swapPrompt ? { swapPrompt } : {}
        })
      );
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === `${basePath}/sahapath/new`) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const category = formData.get("category");
    const title = formData.get("title");
    if (typeof category !== "string" || typeof title !== "string") {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const trimmedTitle = title.trim();
    if (!trimmedTitle) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const items = await listBanglaNineTenSahapathItems(env.DB);
    if (items.some((item) => item.category === category)) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    await createBanglaNineTenSahapathItem(env.DB, { category, title: trimmedTitle });
    return redirectResponse(`${basePath}/sahapath?updated=1`);
  }
  if (url.pathname === `${basePath}/sahapath/edit`) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const itemId = parseNumberParam(formData.get("id")?.toString() ?? null);
    const category = formData.get("category");
    const title = formData.get("title");
    if (!itemId || typeof category !== "string" || typeof title !== "string") {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const trimmedTitle = title.trim();
    if (!trimmedTitle) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const items = await listBanglaNineTenSahapathItems(env.DB);
    const currentItem = items.find((item) => item.id === itemId);
    if (!currentItem) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const conflictingItem = items.find((item) => item.category === category && item.id !== itemId);
    if (conflictingItem) {
      const params = new URLSearchParams({
        swap: "1",
        entryId: String(itemId),
        targetId: String(conflictingItem.id),
        title: trimmedTitle,
        entryCategory: currentItem.category,
        targetCategory: conflictingItem.category
      });
      return redirectResponse(`${basePath}/sahapath?${params.toString()}`);
    }
    await updateBanglaNineTenSahapathItem(env.DB, { id: itemId, category, title: trimmedTitle });
    return redirectResponse(`${basePath}/sahapath?updated=1`);
  }
  if (url.pathname === `${basePath}/sahapath/switch`) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const entryId = parseNumberParam(formData.get("entryId")?.toString() ?? null);
    const targetId = parseNumberParam(formData.get("targetId")?.toString() ?? null);
    const entryTitle = formData.get("entryTitle");
    const entryCategory = formData.get("entryCategory");
    const targetCategory = formData.get("targetCategory");
    if (!entryId || !targetId || typeof entryTitle !== "string" || typeof entryCategory !== "string" || typeof targetCategory !== "string") {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const trimmedTitle = entryTitle.trim();
    if (!trimmedTitle) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    await updateBanglaNineTenSahapathItemTitle(env.DB, { id: entryId, title: trimmedTitle });
    await updateBanglaNineTenSahapathItemCategory(env.DB, { id: entryId, category: targetCategory });
    await updateBanglaNineTenSahapathItemCategory(env.DB, { id: targetId, category: entryCategory });
    return redirectResponse(`${basePath}/sahapath?updated=1`);
  }
  if (url.pathname === `${basePath}/sahapath/delete`) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const itemId = parseNumberParam(formData.get("id")?.toString() ?? null);
    if (!itemId) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    await deleteBanglaNineTenSahapathItem(env.DB, itemId);
    return redirectResponse(`${basePath}/sahapath?updated=1`);
  }
  const sahapathDetailMatch = url.pathname.match(/^\/admin\/modules\/subjects\/bangla\/9-10\/sahapath\/(\d+)$/);
  if (sahapathDetailMatch) {
    if (request.method !== "GET") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const itemId = Number(sahapathDetailMatch[1]);
    const item = await getBanglaNineTenSahapathItem(env.DB, itemId);
    if (!item) {
      return redirectResponse(`${basePath}/sahapath`);
    }
    return renderContent(context, renderBanglaNineTenSahapathDetail(item));
  }
  const literatureDetailMatch = url.pathname.match(
    /^\/admin\/modules\/subjects\/bangla\/9-10\/literature\/(goddo|poddo)\/(\d+)$/
  );
  if (literatureDetailMatch) {
    if (request.method !== "GET") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const itemId = Number(literatureDetailMatch[2]);
    const item = await getBanglaNineTenLiteratureItem(env.DB, itemId);
    if (!item) {
      return redirectResponse(`${basePath}/literature`);
    }
    return renderContent(context, renderBanglaNineTenLiteratureDetail(item));
  }
  const literatureCategoryMatch = url.pathname.match(
    /^\/admin\/modules\/subjects\/bangla\/9-10\/literature\/(goddo|poddo)$/
  );
  if (literatureCategoryMatch) {
    const category = literatureCategoryMatch[1];
    if (request.method === "GET") {
      const items = await listBanglaNineTenLiteratureItems(env.DB, category);
      const successMessage = url.searchParams.get("updated") === "1" ? "\u09A4\u09A5\u09CD\u09AF \u09B8\u0982\u09B0\u0995\u09CD\u09B7\u09A3 \u09B9\u09DF\u09C7\u099B\u09C7\u0964" : void 0;
      const errorMessage = url.searchParams.get("error") === "invalid" ? "\u0985\u09A8\u09C1\u0997\u09CD\u09B0\u09B9 \u0995\u09B0\u09C7 \u09A4\u09A5\u09CD\u09AF \u09AA\u09C2\u09B0\u09A3 \u0995\u09B0\u09C1\u09A8\u0964" : void 0;
      return renderContent(
        context,
        renderBanglaNineTenLiteratureList({
          items,
          category,
          ...successMessage ? { successMessage } : {},
          ...errorMessage ? { errorMessage } : {}
        })
      );
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  const literatureActionMatch = url.pathname.match(
    /^\/admin\/modules\/subjects\/bangla\/9-10\/literature\/(goddo|poddo)\/(new|edit|delete)$/
  );
  if (literatureActionMatch) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const category = literatureActionMatch[1];
    const action = literatureActionMatch[2];
    const formData = await request.formData();
    if (action === "new") {
      const title = formData.get("title");
      if (typeof title !== "string" || !title.trim()) {
        return redirectResponse(`${basePath}/literature/${category}?error=invalid`);
      }
      await createBanglaNineTenLiteratureItem(env.DB, { category, title: title.trim() });
      return redirectResponse(`${basePath}/literature/${category}?updated=1`);
    }
    if (action === "edit") {
      const itemId = parseNumberParam(formData.get("id")?.toString() ?? null);
      const title = formData.get("title");
      if (!itemId || typeof title !== "string" || !title.trim()) {
        return redirectResponse(`${basePath}/literature/${category}?error=invalid`);
      }
      await updateBanglaNineTenLiteratureItem(env.DB, { id: itemId, title: title.trim() });
      return redirectResponse(`${basePath}/literature/${category}?updated=1`);
    }
    if (action === "delete") {
      const itemId = parseNumberParam(formData.get("id")?.toString() ?? null);
      if (!itemId) {
        return redirectResponse(`${basePath}/literature/${category}?error=invalid`);
      }
      await deleteBanglaNineTenLiteratureItem(env.DB, itemId);
      return redirectResponse(`${basePath}/literature/${category}?updated=1`);
    }
  }
  return jsonResponse({ error: "Not found" }, 404);
}, "handleBanglaNineTenFirstPaperRoutes");

// src/modules/subjects/bangla/9-10/second-paper/views.ts
var renderBanglaNineTenSecondPaperHome = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09E8\u09AF\u09BC \u09AA\u09A4\u09CD\u09B0 (\u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09EF-\u09E7\u09E6)</h1>
      <p class="page-subtitle">\u098F\u0987 \u0985\u0982\u09B6\u099F\u09BF \u09AA\u09B0\u09C7 \u09AF\u09CB\u0997 \u09B9\u09AC\u09C7\u0964</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/bangla/9-10">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
      </div>
    </header>
  </section>
`, "renderBanglaNineTenSecondPaperHome");

// src/modules/subjects/bangla/9-10/second-paper/routes.ts
var renderContent2 = /* @__PURE__ */ __name((context, content) => {
  const csrfToken = createCSRFToken();
  const layoutProps = { device: context.device, content, session: context.session, csrfToken };
  if (context.nonce) {
    layoutProps.nonce = context.nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderContent");
var handleBanglaNineTenSecondPaperRoutes = /* @__PURE__ */ __name(async (request, _env, context) => {
  const url = new URL(request.url);
  const basePath = "/admin/modules/subjects/bangla/9-10/second-paper";
  if (!url.pathname.startsWith(basePath)) {
    return null;
  }
  if (!context.session) {
    return redirectResponse("/login");
  }
  if (url.pathname === basePath) {
    if (request.method === "GET") {
      return renderContent2(context, renderBanglaNineTenSecondPaperHome());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  return null;
}, "handleBanglaNineTenSecondPaperRoutes");

// src/modules/subjects/bangla/9-10/views.ts
var renderBanglaNineTenPaperSelection = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">\u09AC\u09BE\u0982\u09B2\u09BE (\u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09EF-\u09E7\u09E6)</h1>
      <p class="page-subtitle">\u098F\u0995\u099F\u09BF \u09AA\u09A4\u09CD\u09B0 \u09A8\u09BF\u09B0\u09CD\u09AC\u09BE\u099A\u09A8 \u0995\u09B0\u09C1\u09A8\u0964</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects">Subjects</a>
      </div>
    </header>
    <section class="page-section">
      <div class="card-grid">
        <a class="card-link" href="/admin/modules/subjects/bangla/9-10/first-paper">
          <span class="card-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09E7\u09AE \u09AA\u09A4\u09CD\u09B0</span>
        </a>
        <a class="card-link" href="/admin/modules/subjects/bangla/9-10/second-paper">
          <span class="card-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09E8\u09AF\u09BC \u09AA\u09A4\u09CD\u09B0</span>
        </a>
      </div>
    </section>
  </section>
`, "renderBanglaNineTenPaperSelection");

// src/modules/subjects/bangla/9-10/routes.ts
var renderContent3 = /* @__PURE__ */ __name((context, content) => {
  const csrfToken = createCSRFToken();
  const layoutProps = { device: context.device, content, session: context.session, csrfToken };
  if (context.nonce) {
    layoutProps.nonce = context.nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderContent");
var handleBanglaNineTenRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const url = new URL(request.url);
  const basePath = "/admin/modules/subjects/bangla/9-10";
  if (!url.pathname.startsWith(basePath)) {
    return null;
  }
  if (!context.session) {
    return redirectResponse("/login");
  }
  if (url.pathname === basePath) {
    if (request.method === "GET") {
      return renderContent3(context, renderBanglaNineTenPaperSelection());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname.startsWith(`${basePath}/first-paper`)) {
    return handleBanglaNineTenFirstPaperRoutes(request, env, context);
  }
  if (url.pathname.startsWith(`${basePath}/second-paper`)) {
    return handleBanglaNineTenSecondPaperRoutes(request, env, context);
  }
  return null;
}, "handleBanglaNineTenRoutes");

// src/modules/subjects/bangla/11-12/first-paper/data.ts
var sahapathTable2 = "bangla_11_12_sahapath_items";
var literatureTable2 = "bangla_11_12_literature_items";
var listBanglaElevenTwelveSahapathItems = /* @__PURE__ */ __name(async (db) => {
  const result = await db.prepare(`SELECT id, category, title, created_at as createdAt FROM ${sahapathTable2} ORDER BY createdAt ASC`).all();
  return result.results;
}, "listBanglaElevenTwelveSahapathItems");
var getBanglaElevenTwelveSahapathItem = /* @__PURE__ */ __name(async (db, itemId) => {
  const result = await db.prepare(`SELECT id, category, title, created_at as createdAt FROM ${sahapathTable2} WHERE id = ?`).bind(itemId).all();
  return result.results[0] ?? null;
}, "getBanglaElevenTwelveSahapathItem");
var createBanglaElevenTwelveSahapathItem = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(`INSERT INTO ${sahapathTable2} (category, title, created_at) VALUES (?, ?, ?)`).bind(payload.category, payload.title, createdAt).run();
}, "createBanglaElevenTwelveSahapathItem");
var updateBanglaElevenTwelveSahapathItem = /* @__PURE__ */ __name(async (db, payload) => {
  await db.prepare(`UPDATE ${sahapathTable2} SET category = ?, title = ? WHERE id = ?`).bind(payload.category, payload.title, payload.id).run();
}, "updateBanglaElevenTwelveSahapathItem");
var updateBanglaElevenTwelveSahapathItemTitle = /* @__PURE__ */ __name(async (db, payload) => {
  await db.prepare(`UPDATE ${sahapathTable2} SET title = ? WHERE id = ?`).bind(payload.title, payload.id).run();
}, "updateBanglaElevenTwelveSahapathItemTitle");
var updateBanglaElevenTwelveSahapathItemCategory = /* @__PURE__ */ __name(async (db, payload) => {
  await db.prepare(`UPDATE ${sahapathTable2} SET category = ? WHERE id = ?`).bind(payload.category, payload.id).run();
}, "updateBanglaElevenTwelveSahapathItemCategory");
var deleteBanglaElevenTwelveSahapathItem = /* @__PURE__ */ __name(async (db, itemId) => {
  await db.prepare(`DELETE FROM ${sahapathTable2} WHERE id = ?`).bind(itemId).run();
}, "deleteBanglaElevenTwelveSahapathItem");
var listBanglaElevenTwelveLiteratureItems = /* @__PURE__ */ __name(async (db, category) => {
  const result = await db.prepare(
    `SELECT id, category, title, created_at as createdAt FROM ${literatureTable2} WHERE category = ? ORDER BY createdAt ASC`
  ).bind(category).all();
  return result.results;
}, "listBanglaElevenTwelveLiteratureItems");
var getBanglaElevenTwelveLiteratureItem = /* @__PURE__ */ __name(async (db, itemId) => {
  const result = await db.prepare(`SELECT id, category, title, created_at as createdAt FROM ${literatureTable2} WHERE id = ?`).bind(itemId).all();
  return result.results[0] ?? null;
}, "getBanglaElevenTwelveLiteratureItem");
var createBanglaElevenTwelveLiteratureItem = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(`INSERT INTO ${literatureTable2} (category, title, created_at) VALUES (?, ?, ?)`).bind(payload.category, payload.title, createdAt).run();
}, "createBanglaElevenTwelveLiteratureItem");
var updateBanglaElevenTwelveLiteratureItem = /* @__PURE__ */ __name(async (db, payload) => {
  await db.prepare(`UPDATE ${literatureTable2} SET title = ? WHERE id = ?`).bind(payload.title, payload.id).run();
}, "updateBanglaElevenTwelveLiteratureItem");
var deleteBanglaElevenTwelveLiteratureItem = /* @__PURE__ */ __name(async (db, itemId) => {
  await db.prepare(`DELETE FROM ${literatureTable2} WHERE id = ?`).bind(itemId).run();
}, "deleteBanglaElevenTwelveLiteratureItem");

// src/modules/subjects/bangla/11-12/first-paper/views.ts
var escapeValue2 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var sahapathLabels2 = {
  natok: "\u09A8\u09BE\u099F\u0995",
  uponnash: "\u0989\u09AA\u09A8\u09CD\u09AF\u09BE\u09B8"
};
var literatureLabels2 = {
  goddo: "\u0997\u09A6\u09CD\u09AF",
  poddo: "\u09AA\u09A6\u09CD\u09AF"
};
var renderModal2 = /* @__PURE__ */ __name((id, title, body, triggerLabel, triggerClass = "button-link") => `
  <div class="modal">
    <input class="modal__toggle" type="checkbox" id="${id}" />
    <label class="${triggerClass}" for="${id}">${triggerLabel}</label>
    <div class="modal__overlay" role="dialog" aria-modal="true" aria-labelledby="${id}-title">
      <div class="modal__panel">
        <div class="modal__header">
          <h3 id="${id}-title" class="modal__title">${title}</h3>
          <label class="modal__close" for="${id}" aria-label="Close">\xD7</label>
        </div>
        ${body}
      </div>
    </div>
  </div>
`, "renderModal");
var renderSwapPrompt2 = /* @__PURE__ */ __name((prompt) => {
  if (!prompt) {
    return "";
  }
  return `
    <section class="form-card">
      <p class="helper-text">"${escapeValue2(prompt.entryTitle)}" \u098F\u09B0 \u09A7\u09B0\u09A8 \u09AA\u09B0\u09BF\u09AC\u09B0\u09CD\u09A4\u09A8 \u0995\u09B0\u09BE \u09B9\u09B2\u09C7 \u09AC\u09BF\u09A6\u09CD\u09AF\u09AE\u09BE\u09A8 \u09A7\u09B0\u09A8 \u09AC\u09A6\u09B2\u09BE\u09AC\u09C7\u0964 \u0986\u09AA\u09A8\u09BF \u0995\u09BF \u09A7\u09B0\u09A8 \u0985\u09A6\u09B2\u09AC\u09A6\u09B2 \u0995\u09B0\u09A4\u09C7 \u099A\u09BE\u09A8?</p>
      <div class="form-actions">
        <form method="post" action="/admin/modules/subjects/bangla/11-12/first-paper/sahapath/switch">
          <input type="hidden" name="entryId" value="${prompt.entryId}" />
          <input type="hidden" name="targetId" value="${prompt.targetId}" />
          <input type="hidden" name="entryTitle" value="${escapeValue2(prompt.entryTitle)}" />
          <input type="hidden" name="entryCategory" value="${escapeValue2(prompt.entryCategory)}" />
          <input type="hidden" name="targetCategory" value="${escapeValue2(prompt.targetCategory)}" />
          <button type="submit" class="button-link button-link--primary">\u0985\u09A6\u09B2\u09AC\u09A6\u09B2 \u09A8\u09BF\u09B6\u09CD\u099A\u09BF\u09A4 \u0995\u09B0\u09C1\u09A8</button>
        </form>
        <a class="button-link" href="/admin/modules/subjects/bangla/11-12/first-paper/sahapath">\u09AC\u09BE\u09A4\u09BF\u09B2</a>
      </div>
    </section>
  `;
}, "renderSwapPrompt");
var renderBanglaElevenTwelveHome = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09E7\u09AE \u09AA\u09A4\u09CD\u09B0 (\u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09E7\u09E7-\u09E7\u09E8)</h1>
      <p class="page-subtitle">\u098F\u0995\u099F\u09BF \u09AC\u09BF\u09AD\u09BE\u0997 \u09A8\u09BF\u09B0\u09CD\u09AC\u09BE\u099A\u09A8 \u0995\u09B0\u09C1\u09A8\u0964</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/bangla/11-12">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
        <a class="button-link" href="/admin/modules/subjects">Subjects</a>
      </div>
    </header>
    <section class="page-section">
      <div class="card-grid">
        <a class="card-link" href="/admin/modules/subjects/bangla/11-12/first-paper/sahapath">
          <span class="card-title">\u09B8\u09B9\u09AA\u09BE\u09A0</span>
        </a>
        <a class="card-link" href="/admin/modules/subjects/bangla/11-12/first-paper/literature">
          <span class="card-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09B8\u09BE\u09B9\u09BF\u09A4\u09CD\u09AF</span>
        </a>
      </div>
    </section>
  </section>
`, "renderBanglaElevenTwelveHome");
var renderBanglaElevenTwelveLiteratureHome = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09B8\u09BE\u09B9\u09BF\u09A4\u09CD\u09AF (\u09E7\u09AE \u09AA\u09A4\u09CD\u09B0, \u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09E7\u09E7-\u09E7\u09E8)</h1>
      <p class="page-subtitle">\u098F\u0995\u099F\u09BF \u09A7\u09B0\u09A3 \u09A8\u09BF\u09B0\u09CD\u09AC\u09BE\u099A\u09A8 \u0995\u09B0\u09C1\u09A8\u0964</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/bangla/11-12/first-paper">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
      </div>
    </header>
    <section class="page-section">
      <div class="card-grid">
        <a class="card-link" href="/admin/modules/subjects/bangla/11-12/first-paper/literature/goddo">
          <span class="card-title">\u0997\u09A6\u09CD\u09AF</span>
        </a>
        <a class="card-link" href="/admin/modules/subjects/bangla/11-12/first-paper/literature/poddo">
          <span class="card-title">\u09AA\u09A6\u09CD\u09AF</span>
        </a>
      </div>
    </section>
  </section>
`, "renderBanglaElevenTwelveLiteratureHome");
var renderBanglaElevenTwelveSahapathList = /* @__PURE__ */ __name((props) => {
  const { items, availableCategories, successMessage, errorMessage, swapPrompt } = props;
  const addDisabled = availableCategories.length === 0;
  const modalBody = `
    <form class="form-card form-grid" method="post" action="/admin/modules/subjects/bangla/11-12/first-paper/sahapath/new">
      <label class="form-field">
        <span>\u09A7\u09B0\u09A3 \u09A8\u09BF\u09B0\u09CD\u09AC\u09BE\u099A\u09A8 \u0995\u09B0\u09C1\u09A8</span>
        <select name="category" required>
          ${availableCategories.map((category) => `<option value="${category}">${sahapathLabels2[category] ?? category}</option>`).join("")}
        </select>
      </label>
      <label class="form-field">
        <span>\u09A8\u09BE\u09AE \u09B2\u09BF\u0996\u09C1\u09A8</span>
        <input name="title" type="text" required />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">\u09B8\u0982\u09B0\u0995\u09CD\u09B7\u09A3 \u0995\u09B0\u09C1\u09A8</button>
      </div>
    </form>
  `;
  const addButton = addDisabled ? `<button class="button-link" type="button" disabled>\u09AF\u09CB\u0997 \u0995\u09B0\u09BE \u09B9\u09DF\u09C7\u099B\u09C7</button>` : renderModal2("add-sahapath-11-12", "\u09B8\u09B9\u09AA\u09BE\u09A0 \u09AF\u09CB\u0997 \u0995\u09B0\u09C1\u09A8", modalBody, "Add", "button-link button-link--primary");
  return `
    <section class="page">
      <header class="page-header">
        <h1 class="page-title">\u09B8\u09B9\u09AA\u09BE\u09A0 (\u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09E7\u09E7-\u09E7\u09E8)</h1>
        <p class="page-subtitle">\u09A8\u09BE\u099F\u0995 \u0993 \u0989\u09AA\u09A8\u09CD\u09AF\u09BE\u09B8 \u09AF\u09CB\u0997 \u0995\u09B0\u09C1\u09A8\u0964</p>
        <div class="page-actions">
          <a class="button-link" href="/admin/modules/subjects/bangla/11-12/first-paper">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
        </div>
      </header>
      ${successMessage ? `<div class="alert">${escapeValue2(successMessage)}</div>` : ""}
      ${errorMessage ? `<div class="alert alert--error">${escapeValue2(errorMessage)}</div>` : ""}
      ${renderSwapPrompt2(swapPrompt)}
      <section class="page-section">
        <div class="page-actions">
          ${addButton}
        </div>
        <ul class="item-list">
          ${items.length ? items.map((item) => {
    const editBody = `
                      <form class="form-card form-grid" method="post" action="/admin/modules/subjects/bangla/11-12/first-paper/sahapath/edit">
                        <input type="hidden" name="id" value="${item.id}" />
                        <label class="form-field">
                          <span>\u09A7\u09B0\u09A3</span>
                          <select name="category" required>
                            ${Object.entries(sahapathLabels2).map(
      ([key, label]) => `<option value="${key}" ${item.category === key ? "selected" : ""}>${label}</option>`
    ).join("")}
                          </select>
                        </label>
                        <label class="form-field">
                          <span>\u09A8\u09BE\u09AE</span>
                          <input name="title" type="text" value="${escapeValue2(item.title)}" required />
                        </label>
                        <div class="form-actions">
                          <button type="submit" class="button-link button-link--primary">\u09B9\u09BE\u09B2\u09A8\u09BE\u0997\u09BE\u09A6</button>
                        </div>
                      </form>
                    `;
    return `
                      <li class="item-card">
                        <div class="item-card__main">
                          <a class="item-card__title" href="/admin/modules/subjects/bangla/11-12/first-paper/sahapath/${item.id}">${escapeValue2(item.title)}</a>
                          <span class="item-card__meta">${sahapathLabels2[item.category] ?? item.category}</span>
                        </div>
                        <div class="item-card__actions">
                          ${renderModal2(
      `edit-sahapath-${item.id}-11-12`,
      "\u09B8\u09B9\u09AA\u09BE\u09A0 \u09B8\u09AE\u09CD\u09AA\u09BE\u09A6\u09A8\u09BE",
      editBody,
      "\u270E",
      "button-link"
    )}
                          <div class="confirm-delete">
                            <input class="confirm-delete__toggle" type="checkbox" id="delete-sahapath-${item.id}-11-12" />
                            <label class="button-link" for="delete-sahapath-${item.id}-11-12">\u{1F5D1}\uFE0F</label>
                            <div class="confirm-delete__modal" role="dialog" aria-modal="true" aria-labelledby="delete-sahapath-${item.id}-11-12-title">
                              <div class="confirm-delete__panel">
                                <div class="confirm-delete__header">
                                  <h3 id="delete-sahapath-${item.id}-11-12-title" class="confirm-delete__title">\u09AE\u09C1\u099B\u09C7 \u09AB\u09C7\u09B2\u09C1\u09A8?</h3>
                                  <label class="confirm-delete__close" for="delete-sahapath-${item.id}-11-12" aria-label="Close">\xD7</label>
                                </div>
                                <form class="confirm-delete__form" method="post" action="/admin/modules/subjects/bangla/11-12/first-paper/sahapath/delete">
                                  <input type="hidden" name="id" value="${item.id}" />
                                  <div class="confirm-delete__actions">
                                    <label class="button-link" for="delete-sahapath-${item.id}-11-12">\u09AC\u09BE\u09A4\u09BF\u09B2</label>
                                    <button type="submit" class="button-link button-link--danger">\u09AE\u09C1\u099B\u09C7 \u09AB\u09C7\u09B2\u09C1\u09A8</button>
                                  </div>
                                </form>
                              </div>
                            </div>
                          </div>
                        </div>
                      </li>
                    `;
  }).join("") : `<li class="item-card"><span class="helper-text">\u0995\u09CB\u09A8\u09CB \u09B8\u09B9\u09AA\u09BE\u09A0 \u09AF\u09CB\u0997 \u0995\u09B0\u09BE \u09B9\u09DF\u09A8\u09BF\u0964</span></li>`}
        </ul>
      </section>
    </section>
  `;
}, "renderBanglaElevenTwelveSahapathList");
var renderBanglaElevenTwelveSahapathDetail = /* @__PURE__ */ __name((item) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">${escapeValue2(item.title)}</h1>
      <p class="page-subtitle">${sahapathLabels2[item.category] ?? item.category}</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/bangla/11-12/first-paper/sahapath">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
      </div>
    </header>
  </section>
`, "renderBanglaElevenTwelveSahapathDetail");
var renderBanglaElevenTwelveLiteratureList = /* @__PURE__ */ __name((props) => {
  const { items, category, successMessage, errorMessage } = props;
  const title = literatureLabels2[category] ?? category;
  const addBody = `
    <form class="form-card form-grid" method="post" action="/admin/modules/subjects/bangla/11-12/first-paper/literature/${category}/new">
      <label class="form-field">
        <span>\u09A8\u09BE\u09AE \u09B2\u09BF\u0996\u09C1\u09A8</span>
        <input name="title" type="text" required />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">\u09B8\u0982\u09B0\u0995\u09CD\u09B7\u09A3 \u0995\u09B0\u09C1\u09A8</button>
      </div>
    </form>
  `;
  return `
    <section class="page">
      <header class="page-header">
        <h1 class="page-title">${title} (\u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09E7\u09E7-\u09E7\u09E8)</h1>
        <p class="page-subtitle">\u09A8\u09A4\u09C1\u09A8 \u0985\u09A7\u09CD\u09AF\u09BE\u09DF \u09AF\u09CB\u0997 \u0995\u09B0\u09C1\u09A8\u0964</p>
        <div class="page-actions">
          <a class="button-link" href="/admin/modules/subjects/bangla/11-12/first-paper/literature">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
        </div>
      </header>
      ${successMessage ? `<div class="alert">${escapeValue2(successMessage)}</div>` : ""}
      ${errorMessage ? `<div class="alert alert--error">${escapeValue2(errorMessage)}</div>` : ""}
      <section class="page-section">
        <div class="page-actions">
          ${renderModal2(`add-${category}-11-12`, `${title} \u09AF\u09CB\u0997 \u0995\u09B0\u09C1\u09A8`, addBody, "Add", "button-link button-link--primary")}
        </div>
        <ul class="item-list">
          ${items.length ? items.map((item) => {
    const editBody = `
                      <form class="form-card form-grid" method="post" action="/admin/modules/subjects/bangla/11-12/first-paper/literature/${category}/edit">
                        <input type="hidden" name="id" value="${item.id}" />
                        <label class="form-field">
                          <span>\u09A8\u09BE\u09AE</span>
                          <input name="title" type="text" value="${escapeValue2(item.title)}" required />
                        </label>
                        <div class="form-actions">
                          <button type="submit" class="button-link button-link--primary">\u09B9\u09BE\u09B2\u09A8\u09BE\u0997\u09BE\u09A6</button>
                        </div>
                      </form>
                    `;
    return `
                      <li class="item-card">
                        <div class="item-card__main">
                          <a class="item-card__title" href="/admin/modules/subjects/bangla/11-12/first-paper/literature/${category}/${item.id}">${escapeValue2(item.title)}</a>
                        </div>
                        <div class="item-card__actions">
                          ${renderModal2(`edit-${category}-${item.id}-11-12`, "\u09B8\u09AE\u09CD\u09AA\u09BE\u09A6\u09A8\u09BE", editBody, "\u270E", "button-link")}
                          <div class="confirm-delete">
                            <input class="confirm-delete__toggle" type="checkbox" id="delete-${category}-${item.id}-11-12" />
                            <label class="button-link" for="delete-${category}-${item.id}-11-12">\u{1F5D1}\uFE0F</label>
                            <div class="confirm-delete__modal" role="dialog" aria-modal="true" aria-labelledby="delete-${category}-${item.id}-11-12-title">
                              <div class="confirm-delete__panel">
                                <div class="confirm-delete__header">
                                  <h3 id="delete-${category}-${item.id}-11-12-title" class="confirm-delete__title">\u09AE\u09C1\u099B\u09C7 \u09AB\u09C7\u09B2\u09C1\u09A8?</h3>
                                  <label class="confirm-delete__close" for="delete-${category}-${item.id}-11-12" aria-label="Close">\xD7</label>
                                </div>
                                <form class="confirm-delete__form" method="post" action="/admin/modules/subjects/bangla/11-12/first-paper/literature/${category}/delete">
                                  <input type="hidden" name="id" value="${item.id}" />
                                  <div class="confirm-delete__actions">
                                    <label class="button-link" for="delete-${category}-${item.id}-11-12">\u09AC\u09BE\u09A4\u09BF\u09B2</label>
                                    <button type="submit" class="button-link button-link--danger">\u09AE\u09C1\u099B\u09C7 \u09AB\u09C7\u09B2\u09C1\u09A8</button>
                                  </div>
                                </form>
                              </div>
                            </div>
                          </div>
                        </div>
                      </li>
                    `;
  }).join("") : `<li class="item-card"><span class="helper-text">\u0995\u09CB\u09A8\u09CB ${title} \u09AF\u09CB\u0997 \u0995\u09B0\u09BE \u09B9\u09DF\u09A8\u09BF\u0964</span></li>`}
        </ul>
      </section>
    </section>
  `;
}, "renderBanglaElevenTwelveLiteratureList");
var renderBanglaElevenTwelveLiteratureDetail = /* @__PURE__ */ __name((item) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">${escapeValue2(item.title)}</h1>
      <p class="page-subtitle">${literatureLabels2[item.category] ?? item.category}</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/bangla/11-12/first-paper/literature/${item.category}">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
      </div>
    </header>
  </section>
`, "renderBanglaElevenTwelveLiteratureDetail");

// src/modules/subjects/bangla/11-12/first-paper/routes.ts
var sahapathCategories2 = ["natok", "uponnash"];
var parseNumberParam2 = /* @__PURE__ */ __name((value) => {
  if (!value) {
    return null;
  }
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : null;
}, "parseNumberParam");
var renderContent4 = /* @__PURE__ */ __name((context, content) => {
  const csrfToken = createCSRFToken();
  const layoutProps = { device: context.device, content, session: context.session, csrfToken };
  if (context.nonce) {
    layoutProps.nonce = context.nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderContent");
var buildSwapPrompt2 = /* @__PURE__ */ __name((url) => {
  if (url.searchParams.get("swap") !== "1") {
    return null;
  }
  const entryId = parseNumberParam2(url.searchParams.get("entryId"));
  const targetId = parseNumberParam2(url.searchParams.get("targetId"));
  const entryTitle = url.searchParams.get("title");
  const entryCategory = url.searchParams.get("entryCategory");
  const targetCategory = url.searchParams.get("targetCategory");
  if (!entryId || !targetId || !entryTitle || !entryCategory || !targetCategory) {
    return null;
  }
  return { entryId, targetId, entryTitle, entryCategory, targetCategory };
}, "buildSwapPrompt");
var handleBanglaElevenTwelveFirstPaperRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const url = new URL(request.url);
  const basePath = "/admin/modules/subjects/bangla/11-12/first-paper";
  if (!url.pathname.startsWith(basePath)) {
    return null;
  }
  if (!context.session) {
    return redirectResponse("/login");
  }
  if (url.pathname === basePath) {
    if (request.method === "GET") {
      return renderContent4(context, renderBanglaElevenTwelveHome());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === `${basePath}/literature`) {
    if (request.method === "GET") {
      return renderContent4(context, renderBanglaElevenTwelveLiteratureHome());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === `${basePath}/sahapath`) {
    if (request.method === "GET") {
      const items = await listBanglaElevenTwelveSahapathItems(env.DB);
      const usedCategories = new Set(items.map((item) => item.category));
      const availableCategories = sahapathCategories2.filter((category) => !usedCategories.has(category));
      const successMessage = url.searchParams.get("updated") === "1" ? "\u09A4\u09A5\u09CD\u09AF \u09B8\u0982\u09B0\u0995\u09CD\u09B7\u09A3 \u09B9\u09DF\u09C7\u099B\u09C7\u0964" : void 0;
      const errorMessage = url.searchParams.get("error") === "invalid" ? "\u0985\u09A8\u09C1\u0997\u09CD\u09B0\u09B9 \u0995\u09B0\u09C7 \u09A4\u09A5\u09CD\u09AF \u09AA\u09C2\u09B0\u09A3 \u0995\u09B0\u09C1\u09A8\u0964" : void 0;
      const swapPrompt = buildSwapPrompt2(url);
      return renderContent4(
        context,
        renderBanglaElevenTwelveSahapathList({
          items,
          availableCategories,
          ...successMessage ? { successMessage } : {},
          ...errorMessage ? { errorMessage } : {},
          ...swapPrompt ? { swapPrompt } : {}
        })
      );
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === `${basePath}/sahapath/new`) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const category = formData.get("category");
    const title = formData.get("title");
    if (typeof category !== "string" || typeof title !== "string") {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const trimmedTitle = title.trim();
    if (!trimmedTitle) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const items = await listBanglaElevenTwelveSahapathItems(env.DB);
    if (items.some((item) => item.category === category)) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    await createBanglaElevenTwelveSahapathItem(env.DB, { category, title: trimmedTitle });
    return redirectResponse(`${basePath}/sahapath?updated=1`);
  }
  if (url.pathname === `${basePath}/sahapath/edit`) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const itemId = parseNumberParam2(formData.get("id")?.toString() ?? null);
    const category = formData.get("category");
    const title = formData.get("title");
    if (!itemId || typeof category !== "string" || typeof title !== "string") {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const trimmedTitle = title.trim();
    if (!trimmedTitle) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const items = await listBanglaElevenTwelveSahapathItems(env.DB);
    const currentItem = items.find((item) => item.id === itemId);
    if (!currentItem) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const conflictingItem = items.find((item) => item.category === category && item.id !== itemId);
    if (conflictingItem) {
      const params = new URLSearchParams({
        swap: "1",
        entryId: String(itemId),
        targetId: String(conflictingItem.id),
        title: trimmedTitle,
        entryCategory: currentItem.category,
        targetCategory: conflictingItem.category
      });
      return redirectResponse(`${basePath}/sahapath?${params.toString()}`);
    }
    await updateBanglaElevenTwelveSahapathItem(env.DB, { id: itemId, category, title: trimmedTitle });
    return redirectResponse(`${basePath}/sahapath?updated=1`);
  }
  if (url.pathname === `${basePath}/sahapath/switch`) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const entryId = parseNumberParam2(formData.get("entryId")?.toString() ?? null);
    const targetId = parseNumberParam2(formData.get("targetId")?.toString() ?? null);
    const entryTitle = formData.get("entryTitle");
    const entryCategory = formData.get("entryCategory");
    const targetCategory = formData.get("targetCategory");
    if (!entryId || !targetId || typeof entryTitle !== "string" || typeof entryCategory !== "string" || typeof targetCategory !== "string") {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    const trimmedTitle = entryTitle.trim();
    if (!trimmedTitle) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    await updateBanglaElevenTwelveSahapathItemTitle(env.DB, { id: entryId, title: trimmedTitle });
    await updateBanglaElevenTwelveSahapathItemCategory(env.DB, { id: entryId, category: targetCategory });
    await updateBanglaElevenTwelveSahapathItemCategory(env.DB, { id: targetId, category: entryCategory });
    return redirectResponse(`${basePath}/sahapath?updated=1`);
  }
  if (url.pathname === `${basePath}/sahapath/delete`) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const itemId = parseNumberParam2(formData.get("id")?.toString() ?? null);
    if (!itemId) {
      return redirectResponse(`${basePath}/sahapath?error=invalid`);
    }
    await deleteBanglaElevenTwelveSahapathItem(env.DB, itemId);
    return redirectResponse(`${basePath}/sahapath?updated=1`);
  }
  const sahapathDetailMatch = url.pathname.match(/^\/admin\/modules\/subjects\/bangla\/11-12\/sahapath\/(\d+)$/);
  if (sahapathDetailMatch) {
    if (request.method !== "GET") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const itemId = Number(sahapathDetailMatch[1]);
    const item = await getBanglaElevenTwelveSahapathItem(env.DB, itemId);
    if (!item) {
      return redirectResponse(`${basePath}/sahapath`);
    }
    return renderContent4(context, renderBanglaElevenTwelveSahapathDetail(item));
  }
  const literatureDetailMatch = url.pathname.match(
    /^\/admin\/modules\/subjects\/bangla\/11-12\/literature\/(goddo|poddo)\/(\d+)$/
  );
  if (literatureDetailMatch) {
    if (request.method !== "GET") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const itemId = Number(literatureDetailMatch[2]);
    const item = await getBanglaElevenTwelveLiteratureItem(env.DB, itemId);
    if (!item) {
      return redirectResponse(`${basePath}/literature`);
    }
    return renderContent4(context, renderBanglaElevenTwelveLiteratureDetail(item));
  }
  const literatureCategoryMatch = url.pathname.match(
    /^\/admin\/modules\/subjects\/bangla\/11-12\/literature\/(goddo|poddo)$/
  );
  if (literatureCategoryMatch) {
    const category = literatureCategoryMatch[1];
    if (request.method === "GET") {
      const items = await listBanglaElevenTwelveLiteratureItems(env.DB, category);
      const successMessage = url.searchParams.get("updated") === "1" ? "\u09A4\u09A5\u09CD\u09AF \u09B8\u0982\u09B0\u0995\u09CD\u09B7\u09A3 \u09B9\u09DF\u09C7\u099B\u09C7\u0964" : void 0;
      const errorMessage = url.searchParams.get("error") === "invalid" ? "\u0985\u09A8\u09C1\u0997\u09CD\u09B0\u09B9 \u0995\u09B0\u09C7 \u09A4\u09A5\u09CD\u09AF \u09AA\u09C2\u09B0\u09A3 \u0995\u09B0\u09C1\u09A8\u0964" : void 0;
      return renderContent4(
        context,
        renderBanglaElevenTwelveLiteratureList({
          items,
          category,
          ...successMessage ? { successMessage } : {},
          ...errorMessage ? { errorMessage } : {}
        })
      );
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  const literatureActionMatch = url.pathname.match(
    /^\/admin\/modules\/subjects\/bangla\/11-12\/literature\/(goddo|poddo)\/(new|edit|delete)$/
  );
  if (literatureActionMatch) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const category = literatureActionMatch[1];
    const action = literatureActionMatch[2];
    const formData = await request.formData();
    if (action === "new") {
      const title = formData.get("title");
      if (typeof title !== "string" || !title.trim()) {
        return redirectResponse(`${basePath}/literature/${category}?error=invalid`);
      }
      await createBanglaElevenTwelveLiteratureItem(env.DB, { category, title: title.trim() });
      return redirectResponse(`${basePath}/literature/${category}?updated=1`);
    }
    if (action === "edit") {
      const itemId = parseNumberParam2(formData.get("id")?.toString() ?? null);
      const title = formData.get("title");
      if (!itemId || typeof title !== "string" || !title.trim()) {
        return redirectResponse(`${basePath}/literature/${category}?error=invalid`);
      }
      await updateBanglaElevenTwelveLiteratureItem(env.DB, { id: itemId, title: title.trim() });
      return redirectResponse(`${basePath}/literature/${category}?updated=1`);
    }
    if (action === "delete") {
      const itemId = parseNumberParam2(formData.get("id")?.toString() ?? null);
      if (!itemId) {
        return redirectResponse(`${basePath}/literature/${category}?error=invalid`);
      }
      await deleteBanglaElevenTwelveLiteratureItem(env.DB, itemId);
      return redirectResponse(`${basePath}/literature/${category}?updated=1`);
    }
  }
  return jsonResponse({ error: "Not found" }, 404);
}, "handleBanglaElevenTwelveFirstPaperRoutes");

// src/modules/subjects/bangla/11-12/second-paper/views.ts
var renderBanglaElevenTwelveSecondPaperHome = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09E8\u09AF\u09BC \u09AA\u09A4\u09CD\u09B0 (\u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09E7\u09E7-\u09E7\u09E8)</h1>
      <p class="page-subtitle">\u098F\u0987 \u0985\u0982\u09B6\u099F\u09BF \u09AA\u09B0\u09C7 \u09AF\u09CB\u0997 \u09B9\u09AC\u09C7\u0964</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/bangla/11-12">\u09AA\u09C7\u099B\u09A8\u09C7 \u09AF\u09BE\u09A8</a>
      </div>
    </header>
  </section>
`, "renderBanglaElevenTwelveSecondPaperHome");

// src/modules/subjects/bangla/11-12/second-paper/routes.ts
var renderContent5 = /* @__PURE__ */ __name((context, content) => {
  const csrfToken = createCSRFToken();
  const layoutProps = { device: context.device, content, session: context.session, csrfToken };
  if (context.nonce) {
    layoutProps.nonce = context.nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderContent");
var handleBanglaElevenTwelveSecondPaperRoutes = /* @__PURE__ */ __name(async (request, _env, context) => {
  const url = new URL(request.url);
  const basePath = "/admin/modules/subjects/bangla/11-12/second-paper";
  if (!url.pathname.startsWith(basePath)) {
    return null;
  }
  if (!context.session) {
    return redirectResponse("/login");
  }
  if (url.pathname === basePath) {
    if (request.method === "GET") {
      return renderContent5(context, renderBanglaElevenTwelveSecondPaperHome());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  return null;
}, "handleBanglaElevenTwelveSecondPaperRoutes");

// src/modules/subjects/bangla/11-12/views.ts
var renderBanglaElevenTwelvePaperSelection = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">\u09AC\u09BE\u0982\u09B2\u09BE (\u09B6\u09CD\u09B0\u09C7\u09A3\u09BF \u09E7\u09E7-\u09E7\u09E8)</h1>
      <p class="page-subtitle">\u098F\u0995\u099F\u09BF \u09AA\u09A4\u09CD\u09B0 \u09A8\u09BF\u09B0\u09CD\u09AC\u09BE\u099A\u09A8 \u0995\u09B0\u09C1\u09A8\u0964</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects">Subjects</a>
      </div>
    </header>
    <section class="page-section">
      <div class="card-grid">
        <a class="card-link" href="/admin/modules/subjects/bangla/11-12/first-paper">
          <span class="card-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09E7\u09AE \u09AA\u09A4\u09CD\u09B0</span>
        </a>
        <a class="card-link" href="/admin/modules/subjects/bangla/11-12/second-paper">
          <span class="card-title">\u09AC\u09BE\u0982\u09B2\u09BE \u09E8\u09AF\u09BC \u09AA\u09A4\u09CD\u09B0</span>
        </a>
      </div>
    </section>
  </section>
`, "renderBanglaElevenTwelvePaperSelection");

// src/modules/subjects/bangla/11-12/routes.ts
var renderContent6 = /* @__PURE__ */ __name((context, content) => {
  const csrfToken = createCSRFToken();
  const layoutProps = { device: context.device, content, session: context.session, csrfToken };
  if (context.nonce) {
    layoutProps.nonce = context.nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderContent");
var handleBanglaElevenTwelveRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const url = new URL(request.url);
  const basePath = "/admin/modules/subjects/bangla/11-12";
  if (!url.pathname.startsWith(basePath)) {
    return null;
  }
  if (!context.session) {
    return redirectResponse("/login");
  }
  if (url.pathname === basePath) {
    if (request.method === "GET") {
      return renderContent6(context, renderBanglaElevenTwelvePaperSelection());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname.startsWith(`${basePath}/first-paper`)) {
    return handleBanglaElevenTwelveFirstPaperRoutes(request, env, context);
  }
  if (url.pathname.startsWith(`${basePath}/second-paper`)) {
    return handleBanglaElevenTwelveSecondPaperRoutes(request, env, context);
  }
  return null;
}, "handleBanglaElevenTwelveRoutes");

// src/modules/subjects/mathematics/9-10/data.ts
var chapterTable = "mathematics_9_10_chapters";
var listMathematicsNineTenChapters = /* @__PURE__ */ __name(async (db) => {
  const result = await db.prepare(`SELECT id, title, created_at as createdAt FROM ${chapterTable} ORDER BY createdAt ASC`).all();
  return result.results;
}, "listMathematicsNineTenChapters");
var getMathematicsNineTenChapter = /* @__PURE__ */ __name(async (db, chapterId) => {
  const result = await db.prepare(`SELECT id, title, created_at as createdAt FROM ${chapterTable} WHERE id = ?`).bind(chapterId).all();
  return result.results[0] ?? null;
}, "getMathematicsNineTenChapter");
var createMathematicsNineTenChapter = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(`INSERT INTO ${chapterTable} (title, created_at) VALUES (?, ?)`).bind(payload.title, createdAt).run();
}, "createMathematicsNineTenChapter");

// src/modules/subjects/mathematics/9-10/views.ts
var escapeValue3 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var renderModal3 = /* @__PURE__ */ __name((id, title, body, triggerLabel, triggerClass = "button-link") => `
  <div class="modal">
    <input class="modal__toggle" type="checkbox" id="${id}" />
    <label class="${triggerClass}" for="${id}">${triggerLabel}</label>
    <div class="modal__overlay" role="dialog" aria-modal="true" aria-labelledby="${id}-title">
      <div class="modal__panel">
        <div class="modal__header">
          <h3 id="${id}-title" class="modal__title">${title}</h3>
          <label class="modal__close" for="${id}" aria-label="Close">\xD7</label>
        </div>
        ${body}
      </div>
    </div>
  </div>
`, "renderModal");
var renderMathematicsNineTenList = /* @__PURE__ */ __name((props) => {
  const { items, successMessage, errorMessage } = props;
  const addBody = `
    <form class="form-card form-grid" method="post" action="/admin/modules/subjects/mathematics/9-10/new">
      <label class="form-field">
        <span>Chapter name</span>
        <input name="title" type="text" required />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">Save</button>
      </div>
    </form>
  `;
  return `
    <section class="page">
      <header class="page-header">
        <h1 class="page-title">Mathematics (Class 9-10)</h1>
        <p class="page-subtitle">Add chapters for this class group.</p>
        <div class="page-actions">
          <a class="button-link" href="/admin/modules/subjects/mathematics">Back</a>
        </div>
      </header>
      ${successMessage ? `<div class="alert">${escapeValue3(successMessage)}</div>` : ""}
      ${errorMessage ? `<div class="alert alert--error">${escapeValue3(errorMessage)}</div>` : ""}
      <section class="page-section">
        <div class="page-actions">
          ${renderModal3("add-mathematics-9-10", "Add chapter", addBody, "Add", "button-link button-link--primary")}
        </div>
        <ul class="item-list">
          ${items.length ? items.map(
    (item) => `
                      <li class="item-card">
                        <div class="item-card__main">
                          <a class="item-card__title" href="/admin/modules/subjects/mathematics/9-10/${item.id}">${escapeValue3(item.title)}</a>
                        </div>
                      </li>
                    `
  ).join("") : `<li class="item-card"><span class="helper-text">No chapters added yet.</span></li>`}
        </ul>
      </section>
    </section>
  `;
}, "renderMathematicsNineTenList");
var renderMathematicsNineTenDetail = /* @__PURE__ */ __name((item) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">${escapeValue3(item.title)}</h1>
      <p class="page-subtitle">No content added yet.</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/mathematics/9-10">Back</a>
      </div>
    </header>
  </section>
`, "renderMathematicsNineTenDetail");

// src/modules/subjects/mathematics/views.ts
var renderMathematicsHome = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Mathematics</h1>
      <p class="page-subtitle">Select a class to continue.</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects">Subjects</a>
      </div>
    </header>
    <section class="page-section">
      <div class="card-grid">
        <a class="card-link" href="/admin/modules/subjects/mathematics/9-10">
          <span class="card-title">Class 9-10</span>
        </a>
      </div>
    </section>
  </section>
`, "renderMathematicsHome");

// src/modules/subjects/mathematics/routes.ts
var parseNumberParam3 = /* @__PURE__ */ __name((value) => {
  if (!value) {
    return null;
  }
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : null;
}, "parseNumberParam");
var renderContent7 = /* @__PURE__ */ __name((context, content) => {
  const csrfToken = createCSRFToken();
  const layoutProps = { device: context.device, content, session: context.session, csrfToken };
  if (context.nonce) {
    layoutProps.nonce = context.nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderContent");
var handleMathematicsRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const url = new URL(request.url);
  const basePath = "/admin/modules/subjects/mathematics";
  if (!url.pathname.startsWith(basePath)) {
    return null;
  }
  if (!context.session) {
    return redirectResponse("/login");
  }
  if (url.pathname === basePath) {
    if (request.method === "GET") {
      return renderContent7(context, renderMathematicsHome());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === `${basePath}/9-10`) {
    if (request.method === "GET") {
      const successMessage = url.searchParams.get("updated") === "1" ? "Chapter saved." : void 0;
      const errorMessage = url.searchParams.get("error") === "invalid" ? "Please enter a chapter name." : void 0;
      const items = await listMathematicsNineTenChapters(env.DB);
      return renderContent7(context, renderMathematicsNineTenList({ items, successMessage, errorMessage }));
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === `${basePath}/9-10/new`) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const title = formData.get("title");
    if (typeof title !== "string" || !title.trim()) {
      return redirectResponse(`${basePath}/9-10?error=invalid`);
    }
    await createMathematicsNineTenChapter(env.DB, { title: title.trim() });
    return redirectResponse(`${basePath}/9-10?updated=1`);
  }
  const chapterMatch = url.pathname.match(/^\/admin\/modules\/subjects\/mathematics\/9-10\/(\d+)$/);
  if (chapterMatch) {
    if (request.method !== "GET") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const chapterId = parseNumberParam3(chapterMatch[1]);
    if (!chapterId) {
      return redirectResponse(`${basePath}/9-10`);
    }
    const item = await getMathematicsNineTenChapter(env.DB, chapterId);
    if (!item) {
      return redirectResponse(`${basePath}/9-10`);
    }
    return renderContent7(context, renderMathematicsNineTenDetail(item));
  }
  return jsonResponse({ error: "Not found" }, 404);
}, "handleMathematicsRoutes");

// src/modules/subjects/higher-mathematics/9-10/data.ts
var chapterTable2 = "higher_mathematics_9_10_chapters";
var listHigherMathematicsNineTenChapters = /* @__PURE__ */ __name(async (db) => {
  const result = await db.prepare(`SELECT id, title, created_at as createdAt FROM ${chapterTable2} ORDER BY createdAt ASC`).all();
  return result.results;
}, "listHigherMathematicsNineTenChapters");
var getHigherMathematicsNineTenChapter = /* @__PURE__ */ __name(async (db, chapterId) => {
  const result = await db.prepare(`SELECT id, title, created_at as createdAt FROM ${chapterTable2} WHERE id = ?`).bind(chapterId).all();
  return result.results[0] ?? null;
}, "getHigherMathematicsNineTenChapter");
var createHigherMathematicsNineTenChapter = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(`INSERT INTO ${chapterTable2} (title, created_at) VALUES (?, ?)`).bind(payload.title, createdAt).run();
}, "createHigherMathematicsNineTenChapter");

// src/modules/subjects/higher-mathematics/9-10/views.ts
var escapeValue4 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var renderModal4 = /* @__PURE__ */ __name((id, title, body, triggerLabel, triggerClass = "button-link") => `
  <div class="modal">
    <input class="modal__toggle" type="checkbox" id="${id}" />
    <label class="${triggerClass}" for="${id}">${triggerLabel}</label>
    <div class="modal__overlay" role="dialog" aria-modal="true" aria-labelledby="${id}-title">
      <div class="modal__panel">
        <div class="modal__header">
          <h3 id="${id}-title" class="modal__title">${title}</h3>
          <label class="modal__close" for="${id}" aria-label="Close">\xD7</label>
        </div>
        ${body}
      </div>
    </div>
  </div>
`, "renderModal");
var renderHigherMathematicsNineTenList = /* @__PURE__ */ __name((props) => {
  const { items, successMessage, errorMessage } = props;
  const addBody = `
    <form class="form-card form-grid" method="post" action="/admin/modules/subjects/higher-mathematics/9-10/new">
      <label class="form-field">
        <span>Chapter name</span>
        <input name="title" type="text" required />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">Save</button>
      </div>
    </form>
  `;
  return `
    <section class="page">
      <header class="page-header">
        <h1 class="page-title">Higher Mathematics (Class 9-10)</h1>
        <p class="page-subtitle">Add chapters for this class group.</p>
        <div class="page-actions">
          <a class="button-link" href="/admin/modules/subjects/higher-mathematics">Back</a>
        </div>
      </header>
      ${successMessage ? `<div class="alert">${escapeValue4(successMessage)}</div>` : ""}
      ${errorMessage ? `<div class="alert alert--error">${escapeValue4(errorMessage)}</div>` : ""}
      <section class="page-section">
        <div class="page-actions">
          ${renderModal4("add-higher-math-9-10", "Add chapter", addBody, "Add", "button-link button-link--primary")}
        </div>
        <ul class="item-list">
          ${items.length ? items.map(
    (item) => `
                      <li class="item-card">
                        <div class="item-card__main">
                          <a class="item-card__title" href="/admin/modules/subjects/higher-mathematics/9-10/${item.id}">${escapeValue4(item.title)}</a>
                        </div>
                      </li>
                    `
  ).join("") : `<li class="item-card"><span class="helper-text">No chapters added yet.</span></li>`}
        </ul>
      </section>
    </section>
  `;
}, "renderHigherMathematicsNineTenList");
var renderHigherMathematicsNineTenDetail = /* @__PURE__ */ __name((item) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">${escapeValue4(item.title)}</h1>
      <p class="page-subtitle">No content added yet.</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/higher-mathematics/9-10">Back</a>
      </div>
    </header>
  </section>
`, "renderHigherMathematicsNineTenDetail");

// src/modules/subjects/higher-mathematics/11-12/data.ts
var chapterTableByPaper = {
  first: "higher_mathematics_11_12_first_chapters",
  second: "higher_mathematics_11_12_second_chapters"
};
var getChapterTable = /* @__PURE__ */ __name((paper) => chapterTableByPaper[paper] ?? chapterTableByPaper.first, "getChapterTable");
var listHigherMathematicsElevenTwelveChapters = /* @__PURE__ */ __name(async (db, paper) => {
  const chapterTable3 = getChapterTable(paper);
  const result = await db.prepare(`SELECT id, title, created_at as createdAt FROM ${chapterTable3} ORDER BY createdAt ASC`).all();
  return result.results.map((item) => ({ ...item, paper }));
}, "listHigherMathematicsElevenTwelveChapters");
var getHigherMathematicsElevenTwelveChapter = /* @__PURE__ */ __name(async (db, chapterId, paper) => {
  const chapterTable3 = getChapterTable(paper);
  const result = await db.prepare(`SELECT id, title, created_at as createdAt FROM ${chapterTable3} WHERE id = ?`).bind(chapterId).all();
  const item = result.results[0];
  if (!item) {
    return null;
  }
  return { ...item, paper };
}, "getHigherMathematicsElevenTwelveChapter");
var createHigherMathematicsElevenTwelveChapter = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  const chapterTable3 = getChapterTable(payload.paper);
  await db.prepare(`INSERT INTO ${chapterTable3} (title, created_at) VALUES (?, ?)`).bind(payload.title, createdAt).run();
}, "createHigherMathematicsElevenTwelveChapter");

// src/modules/subjects/higher-mathematics/11-12/views.ts
var escapeValue5 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var renderModal5 = /* @__PURE__ */ __name((id, title, body, triggerLabel, triggerClass = "button-link") => `
  <div class="modal">
    <input class="modal__toggle" type="checkbox" id="${id}" />
    <label class="${triggerClass}" for="${id}">${triggerLabel}</label>
    <div class="modal__overlay" role="dialog" aria-modal="true" aria-labelledby="${id}-title">
      <div class="modal__panel">
        <div class="modal__header">
          <h3 id="${id}-title" class="modal__title">${title}</h3>
          <label class="modal__close" for="${id}" aria-label="Close">\xD7</label>
        </div>
        ${body}
      </div>
    </div>
  </div>
`, "renderModal");
var paperLabels = {
  first: "Higher Mathematics 1st",
  second: "Higher Mathematics 2nd"
};
var renderHigherMathematicsElevenTwelveHome = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Higher Mathematics (Class 11-12)</h1>
      <p class="page-subtitle">Select a paper to continue.</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects/higher-mathematics">Back</a>
      </div>
    </header>
    <section class="page-section">
      <div class="card-grid">
        <a class="card-link" href="/admin/modules/subjects/higher-mathematics/11-12/first">
          <span class="card-title">Higher Mathematics 1st</span>
        </a>
        <a class="card-link" href="/admin/modules/subjects/higher-mathematics/11-12/second">
          <span class="card-title">Higher Mathematics 2nd</span>
        </a>
      </div>
    </section>
  </section>
`, "renderHigherMathematicsElevenTwelveHome");
var renderHigherMathematicsElevenTwelveList = /* @__PURE__ */ __name((props) => {
  const { items, paper, successMessage, errorMessage } = props;
  const paperLabel = paperLabels[paper] ?? paper;
  const addBody = `
    <form class="form-card form-grid" method="post" action="/admin/modules/subjects/higher-mathematics/11-12/${paper}/new">
      <label class="form-field">
        <span>Chapter name</span>
        <input name="title" type="text" required />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">Save</button>
      </div>
    </form>
  `;
  return `
    <section class="page">
      <header class="page-header">
        <h1 class="page-title">${paperLabel} (Class 11-12)</h1>
        <p class="page-subtitle">Add chapters for this paper.</p>
        <div class="page-actions">
          <a class="button-link" href="/admin/modules/subjects/higher-mathematics/11-12">Back</a>
        </div>
      </header>
      ${successMessage ? `<div class="alert">${escapeValue5(successMessage)}</div>` : ""}
      ${errorMessage ? `<div class="alert alert--error">${escapeValue5(errorMessage)}</div>` : ""}
      <section class="page-section">
        <div class="page-actions">
          ${renderModal5(
    `add-higher-math-11-12-${paper}`,
    `Add chapter (${paperLabel})`,
    addBody,
    "Add",
    "button-link button-link--primary"
  )}
        </div>
        <ul class="item-list">
          ${items.length ? items.map(
    (item) => `
                      <li class="item-card">
                        <div class="item-card__main">
                          <a class="item-card__title" href="/admin/modules/subjects/higher-mathematics/11-12/${paper}/${item.id}">${escapeValue5(item.title)}</a>
                        </div>
                      </li>
                    `
  ).join("") : `<li class="item-card"><span class="helper-text">No chapters added yet.</span></li>`}
        </ul>
      </section>
    </section>
  `;
}, "renderHigherMathematicsElevenTwelveList");
var renderHigherMathematicsElevenTwelveDetail = /* @__PURE__ */ __name((item) => {
  const paperLabel = paperLabels[item.paper] ?? item.paper;
  return `
    <section class="page">
      <header class="page-header">
        <h1 class="page-title">${escapeValue5(item.title)}</h1>
        <p class="page-subtitle">${paperLabel}</p>
        <div class="page-actions">
          <a class="button-link" href="/admin/modules/subjects/higher-mathematics/11-12/${escapeValue5(item.paper)}">Back</a>
        </div>
      </header>
    </section>
  `;
}, "renderHigherMathematicsElevenTwelveDetail");

// src/modules/subjects/higher-mathematics/views.ts
var renderHigherMathematicsHome = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Higher Mathematics</h1>
      <p class="page-subtitle">Select a class to continue.</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects">Subjects</a>
      </div>
    </header>
    <section class="page-section">
      <div class="card-grid">
        <a class="card-link" href="/admin/modules/subjects/higher-mathematics/9-10">
          <span class="card-title">Class 9-10</span>
        </a>
        <a class="card-link" href="/admin/modules/subjects/higher-mathematics/11-12">
          <span class="card-title">Class 11-12</span>
        </a>
      </div>
    </section>
  </section>
`, "renderHigherMathematicsHome");

// src/modules/subjects/higher-mathematics/routes.ts
var validPapers = /* @__PURE__ */ new Set(["first", "second"]);
var parseNumberParam4 = /* @__PURE__ */ __name((value) => {
  if (!value) {
    return null;
  }
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : null;
}, "parseNumberParam");
var renderContent8 = /* @__PURE__ */ __name((context, content) => {
  const csrfToken = createCSRFToken();
  const layoutProps = { device: context.device, content, session: context.session, csrfToken };
  if (context.nonce) {
    layoutProps.nonce = context.nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderContent");
var handleHigherMathematicsRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const url = new URL(request.url);
  const basePath = "/admin/modules/subjects/higher-mathematics";
  if (!url.pathname.startsWith(basePath)) {
    return null;
  }
  if (!context.session) {
    return redirectResponse("/login");
  }
  if (url.pathname === basePath) {
    if (request.method === "GET") {
      return renderContent8(context, renderHigherMathematicsHome());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === `${basePath}/9-10`) {
    if (request.method === "GET") {
      const successMessage = url.searchParams.get("updated") === "1" ? "Chapter saved." : void 0;
      const errorMessage = url.searchParams.get("error") === "invalid" ? "Please enter a chapter name." : void 0;
      const items = await listHigherMathematicsNineTenChapters(env.DB);
      return renderContent8(context, renderHigherMathematicsNineTenList({ items, successMessage, errorMessage }));
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === `${basePath}/9-10/new`) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const title = formData.get("title");
    if (typeof title !== "string" || !title.trim()) {
      return redirectResponse(`${basePath}/9-10?error=invalid`);
    }
    await createHigherMathematicsNineTenChapter(env.DB, { title: title.trim() });
    return redirectResponse(`${basePath}/9-10?updated=1`);
  }
  const nineTenChapterMatch = url.pathname.match(/^\/admin\/modules\/subjects\/higher-mathematics\/9-10\/(\d+)$/);
  if (nineTenChapterMatch) {
    if (request.method !== "GET") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const chapterId = parseNumberParam4(nineTenChapterMatch[1]);
    if (!chapterId) {
      return redirectResponse(`${basePath}/9-10`);
    }
    const item = await getHigherMathematicsNineTenChapter(env.DB, chapterId);
    if (!item) {
      return redirectResponse(`${basePath}/9-10`);
    }
    return renderContent8(context, renderHigherMathematicsNineTenDetail(item));
  }
  if (url.pathname === `${basePath}/11-12`) {
    if (request.method === "GET") {
      return renderContent8(context, renderHigherMathematicsElevenTwelveHome());
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  const paperMatch = url.pathname.match(/^\/admin\/modules\/subjects\/higher-mathematics\/11-12\/(first|second)$/);
  if (paperMatch) {
    if (request.method === "GET") {
      const paper = paperMatch[1];
      const successMessage = url.searchParams.get("updated") === "1" ? "Chapter saved." : void 0;
      const errorMessage = url.searchParams.get("error") === "invalid" ? "Please enter a chapter name." : void 0;
      const items = await listHigherMathematicsElevenTwelveChapters(env.DB, paper);
      return renderContent8(
        context,
        renderHigherMathematicsElevenTwelveList({ items, paper, successMessage, errorMessage })
      );
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  const paperNewMatch = url.pathname.match(/^\/admin\/modules\/subjects\/higher-mathematics\/11-12\/(first|second)\/new$/);
  if (paperNewMatch) {
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const paper = paperNewMatch[1];
    if (!validPapers.has(paper)) {
      return redirectResponse(`${basePath}/11-12`);
    }
    const formData = await request.formData();
    const title = formData.get("title");
    if (typeof title !== "string" || !title.trim()) {
      return redirectResponse(`${basePath}/11-12/${paper}?error=invalid`);
    }
    await createHigherMathematicsElevenTwelveChapter(env.DB, { paper, title: title.trim() });
    return redirectResponse(`${basePath}/11-12/${paper}?updated=1`);
  }
  const paperChapterMatch = url.pathname.match(
    /^\/admin\/modules\/subjects\/higher-mathematics\/11-12\/(first|second)\/(\d+)$/
  );
  if (paperChapterMatch) {
    if (request.method !== "GET") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const paper = paperChapterMatch[1];
    const chapterId = parseNumberParam4(paperChapterMatch[2]);
    if (!validPapers.has(paper) || !chapterId) {
      return redirectResponse(`${basePath}/11-12`);
    }
    const item = await getHigherMathematicsElevenTwelveChapter(env.DB, chapterId, paper);
    if (!item) {
      return redirectResponse(`${basePath}/11-12/${paper}`);
    }
    return renderContent8(context, renderHigherMathematicsElevenTwelveDetail(item));
  }
  return jsonResponse({ error: "Not found" }, 404);
}, "handleHigherMathematicsRoutes");

// src/modules/subjects/types.ts
var defineSubjectModule = /* @__PURE__ */ __name((subjectModule) => subjectModule, "defineSubjectModule");

// src/modules/subjects/registry.ts
var handleBanglaRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const nineTen = await handleBanglaNineTenRoutes(request, env, context);
  if (nineTen) {
    return nineTen;
  }
  return handleBanglaElevenTwelveRoutes(request, env, context);
}, "handleBanglaRoutes");
var subjectModules = [
  defineSubjectModule({ template: banglaTemplate, adminRoutes: handleBanglaRoutes }),
  defineSubjectModule({ template: englishTemplate }),
  defineSubjectModule({ template: mathematicsTemplate, adminRoutes: handleMathematicsRoutes }),
  defineSubjectModule({ template: bangladesh_and_global_studiesTemplate }),
  defineSubjectModule({ template: information_and_communication_technologyTemplate }),
  defineSubjectModule({ template: islam_and_moral_educationTemplate }),
  defineSubjectModule({ template: hindu_religion_and_moral_educationTemplate }),
  defineSubjectModule({ template: buddhist_religion_and_moral_educationTemplate }),
  defineSubjectModule({ template: christian_religion_and_moral_educationTemplate }),
  defineSubjectModule({ template: physicsTemplate }),
  defineSubjectModule({ template: chemistryTemplate }),
  defineSubjectModule({ template: biologyTemplate }),
  defineSubjectModule({ template: higher_mathematicsTemplate, adminRoutes: handleHigherMathematicsRoutes }),
  defineSubjectModule({ template: accountingTemplate }),
  defineSubjectModule({ template: finance_and_bankingTemplate }),
  defineSubjectModule({ template: business_entrepreneurshipTemplate }),
  defineSubjectModule({ template: business_organization_and_managementTemplate }),
  defineSubjectModule({ template: production_management_and_marketingTemplate }),
  defineSubjectModule({ template: finance_banking_and_insuranceTemplate }),
  defineSubjectModule({ template: geography_and_environmentTemplate }),
  defineSubjectModule({ template: civics_and_citizenshipTemplate }),
  defineSubjectModule({ template: economicsTemplate }),
  defineSubjectModule({ template: history_of_bangladesh_and_world_civilizationTemplate }),
  defineSubjectModule({ template: agriculture_studiesTemplate }),
  defineSubjectModule({ template: home_scienceTemplate }),
  defineSubjectModule({ template: civics_and_good_governanceTemplate }),
  defineSubjectModule({ template: historyTemplate }),
  defineSubjectModule({ template: islamic_history_and_cultureTemplate }),
  defineSubjectModule({ template: sociologyTemplate }),
  defineSubjectModule({ template: social_workTemplate }),
  defineSubjectModule({ template: geographyTemplate }),
  defineSubjectModule({ template: logicTemplate }),
  defineSubjectModule({ template: psychologyTemplate }),
  defineSubjectModule({ template: statisticsTemplate })
];
var listSubjectTemplates = /* @__PURE__ */ __name(() => subjectModules.map((subjectModule) => subjectModule.template), "listSubjectTemplates");
var getSubjectTemplate = /* @__PURE__ */ __name((slug) => {
  if (!slug) {
    return null;
  }
  return subjectModules.find((subjectModule) => subjectModule.template.slug === slug)?.template ?? null;
}, "getSubjectTemplate");
var handleSubjectAdminRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  for (const subjectModule of subjectModules) {
    if (!subjectModule.adminRoutes) {
      continue;
    }
    const response = await subjectModule.adminRoutes(request, env, context);
    if (response) {
      return response;
    }
  }
  return null;
}, "handleSubjectAdminRoutes");

// src/core/db/schema.ts
var adminTableSchema = {
  name: "admins",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "name", type: "TEXT", notNull: true },
    { name: "email", type: "TEXT", notNull: true, unique: true },
    { name: "password_hash", type: "TEXT", notNull: true },
    { name: "date_of_birth", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var studentTableSchema = {
  name: "students",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "name", type: "TEXT", notNull: true },
    { name: "email", type: "TEXT", notNull: true, unique: true },
    { name: "password_hash", type: "TEXT", notNull: true },
    { name: "date_of_birth", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true },
    { name: "verified_at", type: "TEXT" }
  ]
};
var studentVerificationTableSchema = {
  name: "student_verifications",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "student_id", type: "INTEGER", notNull: true },
    { name: "code_hash", type: "TEXT", notNull: true },
    { name: "attempts", type: "INTEGER", notNull: true, defaultValue: "0" },
    { name: "expires_at", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true },
    { name: "used_at", type: "TEXT" }
  ]
};
var teacherTableSchema = {
  name: "teachers",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "name", type: "TEXT", notNull: true },
    { name: "email", type: "TEXT", notNull: true, unique: true },
    { name: "password_hash", type: "TEXT", notNull: true },
    { name: "date_of_birth", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var moduleTableSchema = {
  name: "modules",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "name", type: "TEXT", notNull: true },
    { name: "slug", type: "TEXT", notNull: true, unique: true },
    { name: "description", type: "TEXT" },
    { name: "is_active", type: "INTEGER", notNull: true, defaultValue: "1" },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var classGroupTableSchema = {
  name: "class_groups",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "name", type: "TEXT", notNull: true },
    { name: "slug", type: "TEXT", notNull: true, unique: true },
    { name: "description", type: "TEXT" },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var subjectTableSchema = {
  name: "subjects",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "name", type: "TEXT", notNull: true },
    { name: "slug", type: "TEXT", notNull: true, unique: true },
    { name: "template_slug", type: "TEXT" },
    { name: "description", type: "TEXT" },
    { name: "is_two_paper", type: "INTEGER", notNull: true, defaultValue: "0" },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var classSubjectTableSchema = {
  name: "class_subjects",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "class_group_id", type: "INTEGER", notNull: true },
    { name: "subject_id", type: "INTEGER", notNull: true },
    { name: "stream", type: "TEXT", notNull: true },
    { name: "is_optional", type: "INTEGER", notNull: true, defaultValue: "0" },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var chapterTableSchema = {
  name: "chapters",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "class_subject_id", type: "INTEGER", notNull: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "slug", type: "TEXT", notNull: true },
    { name: "position", type: "INTEGER", notNull: true, defaultValue: "0" },
    { name: "summary", type: "TEXT" },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var chapterTopicTableSchema = {
  name: "chapter_topics",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "chapter_id", type: "INTEGER", notNull: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "slug", type: "TEXT", notNull: true },
    { name: "position", type: "INTEGER", notNull: true, defaultValue: "0" },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var contentItemTableSchema = {
  name: "content_items",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "chapter_id", type: "INTEGER" },
    { name: "topic_id", type: "INTEGER" },
    { name: "content_type", type: "TEXT", notNull: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "body", type: "TEXT" },
    { name: "resource_url", type: "TEXT" },
    { name: "position", type: "INTEGER", notNull: true, defaultValue: "0" },
    { name: "metadata_json", type: "TEXT" },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var subjectExamTableSchema = {
  name: "subject_exams",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "class_subject_id", type: "INTEGER", notNull: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "exam_type", type: "TEXT", notNull: true },
    { name: "instructions", type: "TEXT" },
    { name: "total_marks", type: "INTEGER", notNull: true, defaultValue: "0" },
    { name: "duration_minutes", type: "INTEGER", notNull: true, defaultValue: "0" },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var subjectExamQuestionTableSchema = {
  name: "subject_exam_questions",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "exam_id", type: "INTEGER", notNull: true },
    { name: "question", type: "TEXT", notNull: true },
    { name: "answer", type: "TEXT" },
    { name: "choices_json", type: "TEXT" },
    { name: "position", type: "INTEGER", notNull: true, defaultValue: "0" },
    { name: "explanation", type: "TEXT" }
  ]
};
var banglaNineTenSahapathTableSchema = {
  name: "bangla_9_10_sahapath_items",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "category", type: "TEXT", notNull: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var banglaNineTenLiteratureTableSchema = {
  name: "bangla_9_10_literature_items",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "category", type: "TEXT", notNull: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var banglaElevenTwelveSahapathTableSchema = {
  name: "bangla_11_12_sahapath_items",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "category", type: "TEXT", notNull: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var banglaElevenTwelveLiteratureTableSchema = {
  name: "bangla_11_12_literature_items",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "category", type: "TEXT", notNull: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var mathematicsNineTenChapterTableSchema = {
  name: "mathematics_9_10_chapters",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var higherMathematicsNineTenChapterTableSchema = {
  name: "higher_mathematics_9_10_chapters",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var higherMathematicsElevenTwelveFirstChapterTableSchema = {
  name: "higher_mathematics_11_12_first_chapters",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var higherMathematicsElevenTwelveSecondChapterTableSchema = {
  name: "higher_mathematics_11_12_second_chapters",
  columns: [
    { name: "id", type: "INTEGER", primaryKey: true },
    { name: "title", type: "TEXT", notNull: true },
    { name: "created_at", type: "TEXT", notNull: true }
  ]
};
var expectedSchema = [
  adminTableSchema,
  studentTableSchema,
  studentVerificationTableSchema,
  teacherTableSchema,
  moduleTableSchema,
  classGroupTableSchema,
  subjectTableSchema,
  classSubjectTableSchema,
  chapterTableSchema,
  chapterTopicTableSchema,
  contentItemTableSchema,
  subjectExamTableSchema,
  subjectExamQuestionTableSchema,
  banglaNineTenSahapathTableSchema,
  banglaNineTenLiteratureTableSchema,
  banglaElevenTwelveSahapathTableSchema,
  banglaElevenTwelveLiteratureTableSchema,
  mathematicsNineTenChapterTableSchema,
  higherMathematicsNineTenChapterTableSchema,
  higherMathematicsElevenTwelveFirstChapterTableSchema,
  higherMathematicsElevenTwelveSecondChapterTableSchema
];
var buildColumnSql = /* @__PURE__ */ __name((column) => {
  const parts = [column.name, column.type];
  if (column.primaryKey) {
    parts.push("PRIMARY KEY");
  }
  if (column.notNull) {
    parts.push("NOT NULL");
  }
  if (column.unique) {
    parts.push("UNIQUE");
  }
  if (column.defaultValue) {
    parts.push(`DEFAULT ${column.defaultValue}`);
  }
  return parts.join(" ");
}, "buildColumnSql");
var buildCreateTableSql = /* @__PURE__ */ __name((table) => {
  const columnSql = table.columns.map(buildColumnSql).join(", ");
  return `CREATE TABLE IF NOT EXISTS ${table.name} (${columnSql})`;
}, "buildCreateTableSql");

// src/core/db/manager.ts
var getExistingTables = /* @__PURE__ */ __name(async (db) => {
  const result = await db.prepare("SELECT name FROM sqlite_master WHERE type = 'table' AND name NOT LIKE 'sqlite_%'").all();
  return result.results.map((row) => row.name);
}, "getExistingTables");
var getTableColumns = /* @__PURE__ */ __name(async (db, table) => {
  const result = await db.prepare(`PRAGMA table_info(${table})`).all();
  return result.results;
}, "getTableColumns");
var hasMatchingColumn = /* @__PURE__ */ __name((expected, existing) => {
  const expectedType = expected.type.toUpperCase();
  const existingType = existing.type.toUpperCase();
  if (expectedType !== existingType) {
    return false;
  }
  if (expected.primaryKey && existing.pk !== 1) {
    return false;
  }
  if (expected.notNull && existing.notnull !== 1) {
    return false;
  }
  if (!expected.notNull && existing.notnull === 1) {
    return false;
  }
  return true;
}, "hasMatchingColumn");
var planTableUpdate = /* @__PURE__ */ __name((expected, existingColumns) => {
  const existingMap = new Map(existingColumns.map((column) => [column.name, column]));
  const expectedNames = new Set(expected.columns.map((column) => column.name));
  let recreate = false;
  const addColumns = [];
  for (const existing of existingColumns) {
    if (!expectedNames.has(existing.name)) {
      recreate = true;
      break;
    }
  }
  if (!recreate) {
    for (const expectedColumn of expected.columns) {
      const existing = existingMap.get(expectedColumn.name);
      if (!existing) {
        if (expectedColumn.primaryKey || expectedColumn.unique) {
          recreate = true;
          break;
        }
        addColumns.push(expectedColumn);
        continue;
      }
      if (!hasMatchingColumn(expectedColumn, existing)) {
        recreate = true;
        break;
      }
    }
  }
  return {
    table: expected.name,
    recreate,
    addColumns
  };
}, "planTableUpdate");
var dropTable = /* @__PURE__ */ __name(async (db, table) => {
  await db.prepare(`DROP TABLE IF EXISTS ${table}`).run();
}, "dropTable");
var createTable = /* @__PURE__ */ __name(async (db, table) => {
  await db.prepare(buildCreateTableSql(table)).run();
}, "createTable");
var addColumn = /* @__PURE__ */ __name(async (db, table, column) => {
  const columnSql = buildColumnSql({ ...column, primaryKey: false, unique: false });
  await db.prepare(`ALTER TABLE ${table} ADD COLUMN ${columnSql}`).run();
}, "addColumn");
var isAuthorizationError = /* @__PURE__ */ __name((error) => {
  if (!(error instanceof Error)) {
    return false;
  }
  return error.message.includes("SQLITE_AUTH") || error.message.includes("not authorized");
}, "isAuthorizationError");
var createMinimalSchema = /* @__PURE__ */ __name(async (db) => {
  for (const table of expectedSchema) {
    await createTable(db, table);
  }
}, "createMinimalSchema");
var ensureSeedData = /* @__PURE__ */ __name(async (db) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(
    `INSERT INTO modules (name, slug, description, is_active, created_at)
       SELECT ?, ?, ?, ?, ?
       WHERE NOT EXISTS (SELECT 1 FROM modules WHERE slug = ?)`
  ).bind("Subjects", "subjects", "Manage class subjects, chapters, and content.", 1, createdAt, "subjects").run();
  const classGroups = [
    { name: "Class 9-10", slug: "9-10", description: "Combined curriculum for class 9 and 10." },
    { name: "Class 11-12", slug: "11-12", description: "Combined curriculum for class 11 and 12." }
  ];
  for (const group of classGroups) {
    await db.prepare(
      `INSERT INTO class_groups (name, slug, description, created_at)
         SELECT ?, ?, ?, ?
         WHERE NOT EXISTS (SELECT 1 FROM class_groups WHERE slug = ?)`
    ).bind(group.name, group.slug, group.description, createdAt, group.slug).run();
  }
  const templateSubjects = listSubjectTemplates();
  const classGroupRows = await db.prepare("SELECT id, slug FROM class_groups").all();
  const classGroupMap = new Map(classGroupRows.results.map((group) => [group.slug, group.id]));
  for (const template of templateSubjects) {
    await db.prepare(
      `INSERT INTO subjects (name, slug, template_slug, description, is_two_paper, created_at)
         SELECT ?, ?, ?, ?, ?, ?
         WHERE NOT EXISTS (SELECT 1 FROM subjects WHERE slug = ?)`
    ).bind(template.name, template.slug, template.slug, null, 0, createdAt, template.slug).run();
    const subjectResult = await db.prepare("SELECT id FROM subjects WHERE slug = ?").bind(template.slug).all();
    const subjectId = subjectResult.results[0]?.id;
    if (!subjectId) {
      continue;
    }
    for (const group of template.classGroups) {
      const classGroupId = classGroupMap.get(group.slug);
      if (!classGroupId) {
        continue;
      }
      await db.prepare(
        `INSERT INTO class_subjects (class_group_id, subject_id, stream, is_optional, created_at)
           SELECT ?, ?, ?, ?, ?
           WHERE NOT EXISTS (
             SELECT 1 FROM class_subjects WHERE class_group_id = ? AND subject_id = ?
           )`
      ).bind(
        classGroupId,
        subjectId,
        group.stream,
        group.isOptional ? 1 : 0,
        createdAt,
        classGroupId,
        subjectId
      ).run();
    }
  }
}, "ensureSeedData");
var ensureSchema = /* @__PURE__ */ __name(async (db) => {
  try {
    const existingTables = await getExistingTables(db);
    const expectedTableNames = new Set(expectedSchema.map((table) => table.name));
    for (const table of existingTables) {
      if (!expectedTableNames.has(table)) {
        await dropTable(db, table);
      }
    }
    for (const table of expectedSchema) {
      if (!existingTables.includes(table.name)) {
        await createTable(db, table);
        continue;
      }
      const columns = await getTableColumns(db, table.name);
      const plan = planTableUpdate(table, columns);
      if (plan.recreate) {
        await dropTable(db, table.name);
        await createTable(db, table);
        continue;
      }
      for (const column of plan.addColumns) {
        await addColumn(db, table.name, column);
      }
    }
    await ensureSeedData(db);
  } catch (error) {
    if (isAuthorizationError(error)) {
      await createMinimalSchema(db);
      await ensureSeedData(db);
      return;
    }
    throw error;
  }
}, "ensureSchema");

// src/core/security/password.ts
var encoder = new TextEncoder();
var toBase64 = /* @__PURE__ */ __name((buffer) => btoa(String.fromCharCode(...new Uint8Array(buffer))), "toBase64");
var fromBase64 = /* @__PURE__ */ __name((value) => Uint8Array.from(atob(value), (character) => character.charCodeAt(0)), "fromBase64");
var PBKDF2_ITERATIONS = 6e4;
var hashPassword = /* @__PURE__ */ __name(async (password) => {
  const salt = crypto.getRandomValues(new Uint8Array(16));
  const keyMaterial = await crypto.subtle.importKey("raw", encoder.encode(password), "PBKDF2", false, ["deriveBits"]);
  const derivedBits = await crypto.subtle.deriveBits(
    {
      name: "PBKDF2",
      hash: "SHA-256",
      salt,
      iterations: PBKDF2_ITERATIONS
    },
    keyMaterial,
    256
  );
  return `${toBase64(salt)}:${toBase64(derivedBits)}`;
}, "hashPassword");
var verifyPassword = /* @__PURE__ */ __name(async (password, stored) => {
  const [saltBase64, hashBase64] = stored.split(":");
  if (!saltBase64 || !hashBase64) {
    return false;
  }
  const salt = fromBase64(saltBase64);
  const expectedHash = fromBase64(hashBase64);
  const keyMaterial = await crypto.subtle.importKey("raw", encoder.encode(password), "PBKDF2", false, ["deriveBits"]);
  const derivedBits = await crypto.subtle.deriveBits(
    {
      name: "PBKDF2",
      hash: "SHA-256",
      salt,
      iterations: PBKDF2_ITERATIONS
    },
    keyMaterial,
    256
  );
  const derivedBytes = new Uint8Array(derivedBits);
  if (derivedBytes.length !== expectedHash.length) {
    return false;
  }
  let mismatch = 0;
  for (let index = 0; index < derivedBytes.length; index += 1) {
    mismatch |= derivedBytes[index] ^ expectedHash[index];
  }
  return mismatch === 0;
}, "verifyPassword");

// src/domains/auth/adminSetup.ts
var ADMIN_COUNT_QUERY = "SELECT COUNT(*) as count FROM admins";
var adminExists = /* @__PURE__ */ __name(async (db) => {
  const result = await db.prepare(ADMIN_COUNT_QUERY).all();
  const count = result.results[0]?.count ?? 0;
  return count > 0;
}, "adminExists");
var createAdmin = /* @__PURE__ */ __name(async (db, payload) => {
  const passwordHash = await hashPassword(payload.password);
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(
    "INSERT INTO admins (name, email, password_hash, date_of_birth, created_at) VALUES (?, ?, ?, ?, ?)"
  ).bind(payload.name, payload.email, passwordHash, payload.dateOfBirth, createdAt).run();
}, "createAdmin");

// src/domains/auth/adminLogin.ts
var getAdminByEmail = /* @__PURE__ */ __name(async (db, email) => {
  const result = await db.prepare("SELECT name, email, password_hash as passwordHash FROM admins WHERE email = ? LIMIT 1").bind(email).all();
  const admin = result.results[0];
  return admin ?? null;
}, "getAdminByEmail");
var verifyAdminLogin = /* @__PURE__ */ __name(async (db, email, password) => {
  const admin = await getAdminByEmail(db, email);
  if (!admin) {
    return null;
  }
  const valid = await verifyPassword(password, admin.passwordHash);
  if (!valid) {
    return null;
  }
  return { name: admin.name, email: admin.email };
}, "verifyAdminLogin");

// src/domains/admin/userManagement.ts
var roleTables = {
  admin: "admins",
  teacher: "teachers",
  student: "students"
};
var normalizeUserRole = /* @__PURE__ */ __name((value) => {
  if (value === "admin" || value === "teacher" || value === "student") {
    return value;
  }
  return null;
}, "normalizeUserRole");
var buildSearchFilter = /* @__PURE__ */ __name((query) => {
  const trimmed = query?.trim();
  if (!trimmed) {
    return { clause: "", params: [] };
  }
  const like = `%${trimmed}%`;
  return { clause: "WHERE (name LIKE ? OR email LIKE ?)", params: [like, like] };
}, "buildSearchFilter");
var runAllQuery = /* @__PURE__ */ __name(async (db, query, params) => {
  if (params.length === 0) {
    const result2 = await db.prepare(query).all();
    return result2.results;
  }
  const result = await db.prepare(query).bind(...params).all();
  return result.results;
}, "runAllQuery");
var selectUserSql = /* @__PURE__ */ __name((table, role, searchClause) => `SELECT '${role}' as role, name, email, created_at as createdAt FROM ${table} ${searchClause}`, "selectUserSql");
var listUsers = /* @__PURE__ */ __name(async (db, options) => {
  const search = buildSearchFilter(options.query);
  if (options.role) {
    const table = roleTables[options.role];
    const query = `${selectUserSql(table, options.role, search.clause)} ORDER BY createdAt DESC`;
    return runAllQuery(db, query, search.params);
  }
  const selects = Object.entries(roleTables).map(([role, table]) => selectUserSql(table, role, search.clause));
  const unionQuery = `${selects.join(" UNION ALL ")} ORDER BY createdAt DESC`;
  const params = selects.flatMap(() => search.params);
  return runAllQuery(db, unionQuery, params);
}, "listUsers");
var findUserRoleByEmail = /* @__PURE__ */ __name(async (db, email) => {
  const query = `
    SELECT 'admin' as role FROM admins WHERE email = ?
    UNION ALL
    SELECT 'teacher' as role FROM teachers WHERE email = ?
    UNION ALL
    SELECT 'student' as role FROM students WHERE email = ?
    LIMIT 1
  `;
  const result = await db.prepare(query).bind(email, email, email).all();
  return result.results[0]?.role ?? null;
}, "findUserRoleByEmail");
var createUserAccount = /* @__PURE__ */ __name(async (db, payload) => {
  const passwordHash = await hashPassword(payload.password);
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  if (payload.role === "student") {
    await db.prepare(
      "INSERT INTO students (name, email, password_hash, date_of_birth, created_at, verified_at) VALUES (?, ?, ?, ?, ?, ?)"
    ).bind(payload.name, payload.email, passwordHash, payload.dateOfBirth, createdAt, createdAt).run();
    return;
  }
  if (payload.role === "teacher") {
    await db.prepare(
      "INSERT INTO teachers (name, email, password_hash, date_of_birth, created_at) VALUES (?, ?, ?, ?, ?)"
    ).bind(payload.name, payload.email, passwordHash, payload.dateOfBirth, createdAt).run();
    return;
  }
  await db.prepare("INSERT INTO admins (name, email, password_hash, date_of_birth, created_at) VALUES (?, ?, ?, ?, ?)").bind(payload.name, payload.email, passwordHash, payload.dateOfBirth, createdAt).run();
}, "createUserAccount");
var deleteUserAccount = /* @__PURE__ */ __name(async (db, payload) => {
  if (payload.role === "student") {
    await db.prepare("DELETE FROM student_verifications WHERE student_id IN (SELECT id FROM students WHERE email = ?)").bind(payload.email).run();
    await db.prepare("DELETE FROM students WHERE email = ?").bind(payload.email).run();
    return;
  }
  if (payload.role === "teacher") {
    await db.prepare("DELETE FROM teachers WHERE email = ?").bind(payload.email).run();
    return;
  }
  await db.prepare("DELETE FROM admins WHERE email = ?").bind(payload.email).run();
}, "deleteUserAccount");

// src/domains/admin/modules.ts
var listModules = /* @__PURE__ */ __name(async (db) => {
  const result = await db.prepare(
    "SELECT id, name, slug, description, is_active as isActive, created_at as createdAt FROM modules ORDER BY createdAt DESC"
  ).all();
  return result.results;
}, "listModules");
var ensureModulesSeed = /* @__PURE__ */ __name(async (db) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(
    `INSERT INTO modules (name, slug, description, is_active, created_at)
       SELECT ?, ?, ?, ?, ?
       WHERE NOT EXISTS (SELECT 1 FROM modules WHERE slug = ?)`
  ).bind("Subjects", "subjects", "Manage class subjects, chapters, and content.", 1, createdAt, "subjects").run();
}, "ensureModulesSeed");
var ensureDefaultClassGroups = /* @__PURE__ */ __name(async (db) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  const classGroups = [
    { name: "Class 9-10", slug: "9-10", description: "Combined curriculum for class 9 and 10." },
    { name: "Class 11-12", slug: "11-12", description: "Combined curriculum for class 11 and 12." }
  ];
  for (const group of classGroups) {
    await db.prepare(
      `INSERT INTO class_groups (name, slug, description, created_at)
         SELECT ?, ?, ?, ?
         WHERE NOT EXISTS (SELECT 1 FROM class_groups WHERE slug = ?)`
    ).bind(group.name, group.slug, group.description, createdAt, group.slug).run();
  }
}, "ensureDefaultClassGroups");
var syncSubjectTemplates = /* @__PURE__ */ __name(async (db, templates) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  const classGroupRows = await db.prepare("SELECT id, slug FROM class_groups").all();
  const classGroupMap = new Map(classGroupRows.results.map((group) => [group.slug, group.id]));
  for (const template of templates) {
    await db.prepare(
      `INSERT INTO subjects (name, slug, template_slug, description, is_two_paper, created_at)
         SELECT ?, ?, ?, ?, ?, ?
         WHERE NOT EXISTS (SELECT 1 FROM subjects WHERE slug = ?)`
    ).bind(template.name, template.slug, template.slug, null, 0, createdAt, template.slug).run();
    const subjectResult = await db.prepare("SELECT id FROM subjects WHERE slug = ?").bind(template.slug).all();
    const subjectId = subjectResult.results[0]?.id;
    if (!subjectId) {
      continue;
    }
    for (const group of template.classGroups) {
      const classGroupId = classGroupMap.get(group.slug);
      if (!classGroupId) {
        continue;
      }
      await db.prepare(
        `INSERT INTO class_subjects (class_group_id, subject_id, stream, is_optional, created_at)
           SELECT ?, ?, ?, ?, ?
           WHERE NOT EXISTS (
             SELECT 1 FROM class_subjects WHERE class_group_id = ? AND subject_id = ?
           )`
      ).bind(
        classGroupId,
        subjectId,
        group.stream,
        group.isOptional ? 1 : 0,
        createdAt,
        classGroupId,
        subjectId
      ).run();
    }
  }
}, "syncSubjectTemplates");
var listSubjects = /* @__PURE__ */ __name(async (db) => {
  const result = await db.prepare(
    `SELECT subjects.id,
        subjects.name,
        subjects.slug,
        subjects.template_slug as templateSlug,
        subjects.description,
        subjects.is_two_paper as isTwoPaper,
        subjects.created_at as createdAt
      FROM subjects
      ORDER BY subjects.name ASC`
  ).all();
  return result.results;
}, "listSubjects");
var getSubjectById = /* @__PURE__ */ __name(async (db, subjectId) => {
  const result = await db.prepare("SELECT id, name, slug, template_slug as templateSlug, description, is_two_paper as isTwoPaper FROM subjects WHERE id = ?").bind(subjectId).all();
  return result.results[0] ?? null;
}, "getSubjectById");
var listSubjectClassGroups = /* @__PURE__ */ __name(async (db, subjectId) => {
  const result = await db.prepare(
    `SELECT class_subjects.id as classSubjectId,
        class_subjects.class_group_id as classGroupId,
        class_subjects.stream as stream,
        class_subjects.is_optional as isOptional,
        class_groups.name as classGroupName,
        class_groups.slug as classGroupSlug
      FROM class_subjects
      JOIN class_groups ON class_groups.id = class_subjects.class_group_id
      WHERE class_subjects.subject_id = ?
      ORDER BY class_groups.slug ASC`
  ).bind(subjectId).all();
  return result.results;
}, "listSubjectClassGroups");
var deleteSubject = /* @__PURE__ */ __name(async (db, subjectId) => {
  await db.prepare(
    "DELETE FROM content_items WHERE topic_id IN (SELECT id FROM chapter_topics WHERE chapter_id IN (SELECT id FROM chapters WHERE class_subject_id IN (SELECT id FROM class_subjects WHERE subject_id = ?)))"
  ).bind(subjectId).run();
  await db.prepare(
    "DELETE FROM content_items WHERE chapter_id IN (SELECT id FROM chapters WHERE class_subject_id IN (SELECT id FROM class_subjects WHERE subject_id = ?))"
  ).bind(subjectId).run();
  await db.prepare(
    "DELETE FROM chapter_topics WHERE chapter_id IN (SELECT id FROM chapters WHERE class_subject_id IN (SELECT id FROM class_subjects WHERE subject_id = ?))"
  ).bind(subjectId).run();
  await db.prepare("DELETE FROM chapters WHERE class_subject_id IN (SELECT id FROM class_subjects WHERE subject_id = ?)").bind(subjectId).run();
  await db.prepare("DELETE FROM class_subjects WHERE subject_id = ?").bind(subjectId).run();
  await db.prepare("DELETE FROM subjects WHERE id = ?").bind(subjectId).run();
}, "deleteSubject");
var createChapter = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(
    "INSERT INTO chapters (class_subject_id, title, slug, position, summary, created_at) VALUES (?, ?, ?, ?, ?, ?)"
  ).bind(payload.classSubjectId, payload.title, payload.slug, payload.position, payload.summary ?? null, createdAt).run();
}, "createChapter");
var deleteChapter = /* @__PURE__ */ __name(async (db, chapterId) => {
  await db.prepare("DELETE FROM content_items WHERE topic_id IN (SELECT id FROM chapter_topics WHERE chapter_id = ?)").bind(chapterId).run();
  await db.prepare("DELETE FROM content_items WHERE chapter_id = ?").bind(chapterId).run();
  await db.prepare("DELETE FROM chapter_topics WHERE chapter_id = ?").bind(chapterId).run();
  await db.prepare("DELETE FROM chapters WHERE id = ?").bind(chapterId).run();
}, "deleteChapter");
var createTopic = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare("INSERT INTO chapter_topics (chapter_id, title, slug, position, created_at) VALUES (?, ?, ?, ?, ?)").bind(payload.chapterId, payload.title, payload.slug, payload.position, createdAt).run();
}, "createTopic");
var deleteTopic = /* @__PURE__ */ __name(async (db, topicId) => {
  await db.prepare("DELETE FROM content_items WHERE topic_id = ?").bind(topicId).run();
  await db.prepare("DELETE FROM chapter_topics WHERE id = ?").bind(topicId).run();
}, "deleteTopic");
var createContentItem = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(
    "INSERT INTO content_items (chapter_id, topic_id, content_type, title, body, resource_url, position, metadata_json, created_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)"
  ).bind(
    payload.chapterId ?? null,
    payload.topicId ?? null,
    payload.contentType,
    payload.title,
    payload.body ?? null,
    payload.resourceUrl ?? null,
    payload.position,
    payload.metadataJson ?? null,
    createdAt
  ).run();
}, "createContentItem");
var deleteContentItem = /* @__PURE__ */ __name(async (db, contentItemId) => {
  await db.prepare("DELETE FROM content_items WHERE id = ?").bind(contentItemId).run();
}, "deleteContentItem");

// src/ui/pages/admin-setup/content.ts
var adminSetupStyles = `
  ${themeStyles}
  * { box-sizing: border-box; }
  body { margin: 0; padding: 24px; min-height: 100vh; display: grid; place-items: center; }
  main { width: min(560px, 100%); background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-lg); padding: 24px; box-shadow: var(--shadow-md); }
  h1 { margin-top: 0; font-family: var(--font-display); }
  label { display: block; margin-top: 16px; color: var(--color-text); }
  input { width: 100%; margin-top: 6px; }
  button { margin-top: 20px; }
  .note { margin-top: 12px; font-size: 14px; color: var(--color-text-muted); }
`;
var renderAdminSetupPage = /* @__PURE__ */ __name((csrfToken, requiresSetupToken) => `<!DOCTYPE html>
<html lang="en" data-theme="light">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="${csrfToken}" />
    <title>Admin Setup</title>
    <style>${adminSetupStyles}</style>
  </head>
  <body>
    <main>
      <h1>First Admin Setup</h1>
      <p>Set up the first admin account. This form is available only once.</p>
      <form method="post" action="/setup-admin">
        <input type="hidden" name="csrf_token" value="${csrfToken}" />
        <label>
          Full name
          <input name="name" autocomplete="name" required />
        </label>
        <label>
          Gmail address
          <input type="email" name="email" autocomplete="email" required />
        </label>
        <label>
          Password
          <input type="password" name="password" autocomplete="new-password" required minlength="8" />
        </label>
        <label>
          Date of birth
          <input type="date" name="dateOfBirth" required />
        </label>
        ${requiresSetupToken ? `
        <label>
          Setup key
          <input type="password" name="setupToken" autocomplete="one-time-code" required />
        </label>` : ""}
        <button type="submit">Create admin</button>
      </form>
      ${requiresSetupToken ? `<p class="note">Enter the setup key configured in your deployment secrets.</p>` : ""}
      <p class="note">Once the admin is created, this setup screen will be disabled.</p>
    </main>
  </body>
</html>`, "renderAdminSetupPage");

// src/ui/pages/login/content.ts
var escapeValue6 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var renderLoginContent = /* @__PURE__ */ __name(({ errorMessage, csrfToken } = {}) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Sign in</h1>
      <p class="page-subtitle">Use your Gmail account to access your freeducation profile.</p>
    </header>
    ${errorMessage ? `<div class="alert alert--error">${escapeValue6(errorMessage)}</div>` : ""}
    <form class="form-card form-grid" method="POST" action="/login">
      ${csrfToken ? `<input type="hidden" name="csrf_token" value="${escapeValue6(csrfToken)}" />` : ""}
      <label class="form-field">
        <span>Email</span>
        <input type="email" name="email" placeholder="you@gmail.com" autocomplete="email" required />
      </label>
      <label class="form-field">
        <span>Password</span>
        <input type="password" name="password" autocomplete="current-password" required />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">Login</button>
      </div>
      <p class="helper-text">We currently support Gmail-only access for the admin account.</p>
      <p class="helper-text">New student? <a href="/signup">Create your account</a>.</p>
    </form>
  </section>
`, "renderLoginContent");

// src/ui/pages/modules/content.ts
var escapeValue7 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var renderModuleRows = /* @__PURE__ */ __name((modules) => {
  if (!modules.length) {
    return `
      <tr>
        <td colspan="3">No modules have been registered yet.</td>
      </tr>`;
  }
  return modules.map(
    (module) => `
      <tr>
        <td>${escapeValue7(module.name)}</td>
        <td>${module.isActive ? "Active" : "Inactive"}</td>
        <td>
          ${module.isActive ? `<a class="button-link" href="/admin/modules/${escapeValue7(module.slug)}">Open</a>` : ""}
        </td>
      </tr>`
  ).join("");
}, "renderModuleRows");
var renderModulesContent = /* @__PURE__ */ __name(({ modules, successMessage, errorMessage }) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Modules</h1>
      <p class="page-subtitle">Modules are defined in code and synced automatically.</p>
    </header>
    ${successMessage ? `<div class="alert">${escapeValue7(successMessage)}</div>` : ""}
    ${errorMessage ? `<div class="alert alert--error">${escapeValue7(errorMessage)}</div>` : ""}
    <section class="page-section">
      <div class="table-scroll">
        <table class="data-table">
          <thead>
            <tr>
              <th>Name</th>
              <th>Status</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            ${renderModuleRows(modules)}
          </tbody>
        </table>
      </div>
    </section>
  </section>
`, "renderModulesContent");

// src/ui/pages/modules/subjects/detail.ts
var escapeValue8 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var expandClassOptions = /* @__PURE__ */ __name((classGroups, splitCombined) => {
  const order = ["9-10", "11-12"];
  const sorted = [...classGroups].sort((a, b) => {
    const aIndex = order.indexOf(a.classGroupSlug);
    const bIndex = order.indexOf(b.classGroupSlug);
    if (aIndex === -1 && bIndex === -1) {
      return a.classGroupName.localeCompare(b.classGroupName);
    }
    if (aIndex === -1) return 1;
    if (bIndex === -1) return -1;
    return aIndex - bIndex;
  });
  return sorted.flatMap((group) => {
    if (splitCombined && group.classGroupSlug === "9-10") {
      return [
        { label: "Class 9", classSubjectId: group.classSubjectId, classGroupSlug: group.classGroupSlug },
        { label: "Class 10", classSubjectId: group.classSubjectId, classGroupSlug: group.classGroupSlug }
      ];
    }
    if (splitCombined && group.classGroupSlug === "11-12") {
      return [
        { label: "Class 11", classSubjectId: group.classSubjectId, classGroupSlug: group.classGroupSlug },
        { label: "Class 12", classSubjectId: group.classSubjectId, classGroupSlug: group.classGroupSlug }
      ];
    }
    return [{ label: group.classGroupName, classSubjectId: group.classSubjectId, classGroupSlug: group.classGroupSlug }];
  });
}, "expandClassOptions");
var renderClassGroupLinks = /* @__PURE__ */ __name((subjectId, classGroups, subjectSlug, selected) => {
  if (!classGroups.length) {
    return '<p class="helper-text">No class groups assigned to this subject.</p>';
  }
  const options = expandClassOptions(classGroups, subjectSlug !== "bangla");
  return `
    <div class="card-grid">
      ${options.map((option) => {
    const active = option.classSubjectId === selected;
    const path = subjectSlug === "bangla" && (option.classGroupSlug === "9-10" || option.classGroupSlug === "11-12") ? `/admin/modules/subjects/bangla/${option.classGroupSlug}` : `/admin/modules/subjects/${subjectId}?classSubjectId=${option.classSubjectId}`;
    return `
            <a class="card-link ${active ? "card-link--active" : ""}" href="${path}">
              <span class="card-title">${escapeValue8(option.label)}</span>
            </a>`;
  }).join("")}
    </div>`;
}, "renderClassGroupLinks");
var renderSubjectDetailContent = /* @__PURE__ */ __name(({
  subject,
  classGroups,
  selectedClassSubjectId,
  successMessage,
  errorMessage
}) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">${escapeValue8(subject.name)}</h1>
      <p class="page-subtitle">Select a class to continue.</p>
      <div class="page-actions">
        <a class="button-link" href="/admin/modules/subjects">Back to subjects</a>
      </div>
    </header>
    ${successMessage ? `<div class="alert">${escapeValue8(successMessage)}</div>` : ""}
    ${errorMessage ? `<div class="alert alert--error">${escapeValue8(errorMessage)}</div>` : ""}
    <section class="page-section">
      <h2 class="section-title">Class selection</h2>
      ${renderClassGroupLinks(subject.id, classGroups, subject.slug, selectedClassSubjectId)}
    </section>
  </section>
`, "renderSubjectDetailContent");

// src/ui/pages/modules/subjects/content.ts
var escapeValue9 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var renderSubjectRows = /* @__PURE__ */ __name((subjects) => {
  if (!subjects.length) {
    return `
      <tr>
        <td colspan="1">No subject templates have been registered yet.</td>
      </tr>`;
  }
  return subjects.map((subject) => {
    return `
        <tr>
          <td>
            <a class="table-row-link" href="${escapeValue9(subject.manageUrl)}">
              <div style="font-weight: 500; margin-bottom: 2px;">${escapeValue9(subject.name)}</div>
              <div class="table-meta">${escapeValue9(subject.manageLabel)}</div>
            </a>
          </td>
        </tr>`;
  }).join("");
}, "renderSubjectRows");
var renderSubjectsModuleContent = /* @__PURE__ */ __name(({
  subjects,
  successMessage,
  errorMessage
}) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Subjects</h1>
      <p class="page-subtitle">Select a subject to manage class-specific content.</p>
    </header>
    ${successMessage ? `<div class="alert">${escapeValue9(successMessage)}</div>` : ""}
    ${errorMessage ? `<div class="alert alert--error">${escapeValue9(errorMessage)}</div>` : ""}
    <section class="page-section">
      <div class="table-scroll">
        <table class="data-table">
          <thead>
            <tr>
              <th>Subject</th>
            </tr>
          </thead>
          <tbody>
            ${renderSubjectRows(subjects)}
          </tbody>
        </table>
      </div>
    </section>
  </section>
`, "renderSubjectsModuleContent");

// src/ui/pages/user-management/content.ts
var escapeValue10 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var formatRoleLabel = /* @__PURE__ */ __name((role) => {
  if (role === "admin") {
    return "Admin";
  }
  if (role === "teacher") {
    return "Teacher";
  }
  return "Student";
}, "formatRoleLabel");
var renderUserManagementContent = /* @__PURE__ */ __name(({
  users,
  filters,
  successMessage,
  errorMessage
}) => {
  const queryValue = filters.query ? escapeValue10(filters.query) : "";
  const roleValue = filters.role ?? "";
  const rows = users.length ? users.map(
    (user, index) => `
        <tr>
          <td>${formatRoleLabel(user.role)}</td>
          <td title="${escapeValue10(user.name)}">${escapeValue10(user.name)}</td>
          <td title="${escapeValue10(user.email)}">${escapeValue10(user.email)}</td>
          <td title="${escapeValue10(new Date(user.createdAt).toLocaleDateString())}">${escapeValue10(new Date(user.createdAt).toLocaleDateString())}</td>
          <td>
            <div class="confirm-delete">
              <input class="confirm-delete__toggle" type="checkbox" id="confirm-delete-${index}" />
              <label class="button-link button-link--danger" for="confirm-delete-${index}">Delete</label>
              <div class="confirm-delete__modal" role="dialog" aria-modal="true" aria-labelledby="confirm-delete-${index}-title">
                <div class="confirm-delete__panel">
                  <div class="confirm-delete__header">
                    <h3 id="confirm-delete-${index}-title" class="confirm-delete__title">Confirm delete</h3>
                    <label class="confirm-delete__close" for="confirm-delete-${index}" aria-label="Close">\xD7</label>
                  </div>
                  <form class="confirm-delete__form" method="post" action="/admin/users/delete">
                    <input type="hidden" name="role" value="${escapeValue10(user.role)}" />
                    <input type="hidden" name="email" value="${escapeValue10(user.email)}" />
                    <div class="confirm-delete__field">
                      <span>Admin password</span>
                      <input type="password" name="adminPassword" required autocomplete="current-password" />
                    </div>
                    <div class="confirm-delete__actions">
                      <label class="button-link" for="confirm-delete-${index}">Cancel</label>
                      <button type="submit" class="button-link button-link--danger">Confirm delete</button>
                    </div>
                  </form>
                </div>
              </div>
            </div>
          </td>
        </tr>`
  ).join("") : `
        <tr>
          <td colspan="5" style="text-align: center; padding: 24px; color: var(--color-text-muted); font-style: italic;">No users found for this filter.</td>
        </tr>`;
  return `
    <header class="page-header">
      <h1 class="page-title">User management</h1>
      <p class="page-subtitle">Review and create student, teacher, and admin accounts.</p>
    </header>
    ${successMessage ? `<div class="alert">${escapeValue10(successMessage)}</div>` : ""}
    ${errorMessage ? `<div class="alert alert--error">${escapeValue10(errorMessage)}</div>` : ""}
    <section class="page-section">
      <div class="user-management-toolbar">
        <form class="filter-form" method="get" action="/admin/users">
          <div class="filter-fields">
            <label class="filter-field">
              <span>Role</span>
              <select name="role">
                <option value="" ${roleValue === "" ? "selected" : ""}>All roles</option>
                <option value="admin" ${roleValue === "admin" ? "selected" : ""}>Admin</option>
                <option value="teacher" ${roleValue === "teacher" ? "selected" : ""}>Teacher</option>
                <option value="student" ${roleValue === "student" ? "selected" : ""}>Student</option>
              </select>
            </label>
            <label class="filter-field">
              <span>Search</span>
              <input name="q" placeholder="Name or email" value="${queryValue}" autocomplete="off" />
            </label>
          </div>
          <div class="filter-actions">
            <button type="submit" class="button-link">Filter</button>
            <a class="button-link" href="/admin/users">Reset</a>
          </div>
        </form>
        <div class="toolbar-actions">
          <a class="button-link button-link--primary" href="/admin/users/new">Add user</a>
        </div>
      </div>
    </section>
    <section class="page-section">
      <div class="table-scroll">
        <table class="data-table">
          <thead>
            <tr>
              <th>Role</th>
              <th>Name</th>
              <th>Email</th>
              <th>Created</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            ${rows}
          </tbody>
        </table>
      </div>
    </section>
  `;
}, "renderUserManagementContent");

// src/ui/pages/user-management/create/content.ts
var escapeValue11 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var formatRoleLabel2 = /* @__PURE__ */ __name((role) => {
  if (role === "admin") {
    return "Admin";
  }
  if (role === "teacher") {
    return "Teacher";
  }
  return "Student";
}, "formatRoleLabel");
var renderCreateUserContent = /* @__PURE__ */ __name(({ role, errorMessage, values }) => {
  if (!role) {
    return `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Add a user</h1>
      <p class="page-subtitle">Select who you are creating before entering details.</p>
    </header>
    <form class="form-card form-grid" method="get" action="/admin/users/new">
      <label class="form-field">
        <span>User role</span>
        <select name="role" required>
          <option value="" selected disabled>Select a role</option>
          <option value="admin">Admin</option>
          <option value="teacher">Teacher</option>
          <option value="student">Student</option>
        </select>
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">Continue</button>
        <a class="button-link" href="/admin/users">Back to users</a>
      </div>
    </form>
  </section>
`;
  }
  return `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Add ${formatRoleLabel2(role)}</h1>
      <p class="page-subtitle">Create a new ${formatRoleLabel2(role).toLowerCase()} account with a Gmail address.</p>
    </header>
    ${errorMessage ? `<div class="alert alert--error">${escapeValue11(errorMessage)}</div>` : ""}
    <form class="form-card form-grid" method="post" action="/admin/users/new">
      <input type="hidden" name="role" value="${role}" />
      <label class="form-field">
        <span>Full name</span>
        <input name="name" autocomplete="name" required value="${values?.name ? escapeValue11(values.name) : ""}" />
      </label>
      <label class="form-field">
        <span>Date of birth</span>
        <input type="date" name="dateOfBirth" required value="${values?.dateOfBirth ? escapeValue11(values.dateOfBirth) : ""}" />
      </label>
      <label class="form-field">
        <span>Gmail address</span>
        <input type="email" name="email" placeholder="user@gmail.com" autocomplete="email" required value="${values?.email ? escapeValue11(values.email) : ""}" />
      </label>
      <label class="form-field">
        <span>Temporary password</span>
        <input type="password" name="password" autocomplete="new-password" required minlength="8" />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">Create account</button>
        <a class="button-link" href="/admin/users">Back to users</a>
      </div>
      <p class="helper-text">Admins add accounts directly, so no email verification is required.</p>
    </form>
  </section>
`;
}, "renderCreateUserContent");

// src/core/security/session.ts
var encoder2 = new TextEncoder();
var toBase64Url = /* @__PURE__ */ __name((buffer) => btoa(String.fromCharCode(...new Uint8Array(buffer))).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/g, ""), "toBase64Url");
var fromBase64Url = /* @__PURE__ */ __name((value) => {
  const padded = value.replace(/-/g, "+").replace(/_/g, "/");
  const padLength = (4 - padded.length % 4) % 4;
  const normalized = `${padded}${"=".repeat(padLength)}`;
  return Uint8Array.from(atob(normalized), (character) => character.charCodeAt(0));
}, "fromBase64Url");
var signValue = /* @__PURE__ */ __name(async (value, secret) => {
  const key = await crypto.subtle.importKey(
    "raw",
    encoder2.encode(secret),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"]
  );
  const signature = await crypto.subtle.sign("HMAC", key, encoder2.encode(value));
  return toBase64Url(signature);
}, "signValue");
var timingSafeEqual2 = /* @__PURE__ */ __name((a, b) => {
  if (a.length !== b.length) {
    return false;
  }
  let mismatch = 0;
  for (let index = 0; index < a.length; index += 1) {
    mismatch |= a[index] ^ b[index];
  }
  return mismatch === 0;
}, "timingSafeEqual");
var createAdminSessionToken = /* @__PURE__ */ __name(async (session, secret) => {
  const issuedAt = Math.floor(Date.now() / 1e3);
  const payloadData = {
    ...session,
    iat: issuedAt,
    exp: issuedAt + SESSION_CONFIG.MAX_AGE
  };
  const payload = toBase64Url(encoder2.encode(JSON.stringify(payloadData)));
  const signature = await signValue(payload, secret);
  return `${payload}.${signature}`;
}, "createAdminSessionToken");
var readAdminSessionToken = /* @__PURE__ */ __name(async (token, secret) => {
  const [payload, signature] = token.split(".");
  if (!payload || !signature) {
    return null;
  }
  const expectedSignature = await signValue(payload, secret);
  const signatureBytes = fromBase64Url(signature);
  const expectedBytes = fromBase64Url(expectedSignature);
  if (!timingSafeEqual2(signatureBytes, expectedBytes)) {
    return null;
  }
  try {
    const decoded = new TextDecoder().decode(fromBase64Url(payload));
    const parsed = JSON.parse(decoded);
    if (!parsed?.email || !parsed?.name || !parsed?.exp) {
      return null;
    }
    const now = Math.floor(Date.now() / 1e3);
    if (parsed.exp <= now) {
      return null;
    }
    return { name: parsed.name, email: parsed.email };
  } catch {
    return null;
  }
}, "readAdminSessionToken");
var serializeAdminSessionCookie = /* @__PURE__ */ __name((token) => `${SESSION_CONFIG.COOKIE_NAME}=${token}; HttpOnly; Secure; SameSite=Strict; Path=${SESSION_CONFIG.PATH}; Max-Age=${SESSION_CONFIG.MAX_AGE}`, "serializeAdminSessionCookie");
var clearAdminSessionCookie = /* @__PURE__ */ __name(() => `${SESSION_CONFIG.COOKIE_NAME}=; HttpOnly; Secure; SameSite=Strict; Path=${SESSION_CONFIG.PATH}; Max-Age=0`, "clearAdminSessionCookie");
var getCookieValue = /* @__PURE__ */ __name((cookieHeader, name) => {
  if (!cookieHeader) {
    return null;
  }
  const cookies = cookieHeader.split(";");
  for (const cookie of cookies) {
    const [key, ...rest] = cookie.trim().split("=");
    if (key === name) {
      return rest.join("=");
    }
  }
  return null;
}, "getCookieValue");

// src/api/admin.ts
var isValidPassword = /* @__PURE__ */ __name((password) => password.length >= 8 && /[a-z]/.test(password) && /[A-Z]/.test(password) && /\d/.test(password), "isValidPassword");
var renderAdminPage = /* @__PURE__ */ __name((context, content) => {
  const csrfToken = createCSRFToken();
  const layoutProps = { device: context.device, content, session: context.session, csrfToken };
  if (context.nonce) {
    layoutProps.nonce = context.nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderAdminPage");
var getAdminSetup = /* @__PURE__ */ __name(async (env) => {
  if (await adminExists(env.DB)) {
    return redirectResponse("/");
  }
  const csrfToken = createCSRFToken();
  const requiresSetupToken = Boolean(env.ADMIN_SETUP_TOKEN);
  return htmlResponse(renderAdminSetupPage(csrfToken, requiresSetupToken), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "getAdminSetup");
var postAdminSetup = /* @__PURE__ */ __name(async (request, env) => {
  if (await adminExists(env.DB)) {
    return redirectResponse("/");
  }
  const formData = await request.formData();
  const name = formData.get("name");
  const email = formData.get("email");
  const password = formData.get("password");
  const dateOfBirth = formData.get("dateOfBirth");
  const setupToken = formData.get("setupToken");
  if (typeof name !== "string" || typeof email !== "string" || typeof password !== "string" || typeof dateOfBirth !== "string") {
    return badRequest("Missing required fields.");
  }
  if (!email.endsWith("@gmail.com")) {
    return badRequest("Only Gmail addresses are allowed for the first admin.");
  }
  if (env.ADMIN_SETUP_TOKEN) {
    if (typeof setupToken !== "string" || setupToken !== env.ADMIN_SETUP_TOKEN) {
      return badRequest("Invalid setup key.");
    }
  }
  const trimmedName = name.trim();
  const normalizedEmail = email.trim().toLowerCase();
  await createAdmin(env.DB, {
    name: trimmedName,
    email: normalizedEmail,
    password,
    dateOfBirth
  });
  const token = await createAdminSessionToken({ name: trimmedName, email: normalizedEmail }, env.JWT_SECRET);
  return redirectResponse("/", {
    "Set-Cookie": serializeAdminSessionCookie(token)
  });
}, "postAdminSetup");
var renderLogin = /* @__PURE__ */ __name((context, errorMessage) => {
  const csrfToken = createCSRFToken();
  const content = renderLoginContent(errorMessage ? { errorMessage, csrfToken } : { csrfToken });
  const layoutProps = { device: context.device, content, session: context.session, csrfToken };
  if (context.nonce) {
    layoutProps.nonce = context.nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderLogin");
var renderUserList = /* @__PURE__ */ __name(async (env, context, role, query, successMessage, errorMessage) => {
  const users = await listUsers(env.DB, { role, ...query ? { query } : {} });
  const content = renderUserManagementContent({
    users,
    filters: { role: role ?? null, ...query ? { query } : {} },
    ...successMessage ? { successMessage } : {},
    ...errorMessage ? { errorMessage } : {}
  });
  return renderAdminPage(context, content);
}, "renderUserList");
var renderCreateUser = /* @__PURE__ */ __name((context, role, errorMessage, values) => {
  const content = renderCreateUserContent({
    role,
    ...values ? { values } : {},
    ...errorMessage ? { errorMessage } : {}
  });
  return renderAdminPage(context, content);
}, "renderCreateUser");
var parseNumberParam5 = /* @__PURE__ */ __name((value) => {
  if (!value) {
    return null;
  }
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : null;
}, "parseNumberParam");
var renderModules = /* @__PURE__ */ __name(async (env, context) => {
  await ensureModulesSeed(env.DB);
  let modules = await listModules(env.DB);
  if (!modules.length) {
    modules = [
      {
        id: 0,
        name: "Subjects",
        slug: "subjects",
        description: "Manage class subjects, chapters, and content.",
        isActive: 1,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      }
    ];
  }
  const content = renderModulesContent({ modules });
  return renderAdminPage(context, content);
}, "renderModules");
var renderSubjectsModule = /* @__PURE__ */ __name(async (env, context, successMessage, errorMessage) => {
  await ensureDefaultClassGroups(env.DB);
  const subjectTemplates = listSubjectTemplates();
  let subjects = await listSubjects(env.DB);
  const existingSlugs = new Set(subjects.map((subject) => subject.slug));
  const missingTemplates = subjectTemplates.filter((template) => !existingSlugs.has(template.slug));
  if (missingTemplates.length) {
    await syncSubjectTemplates(env.DB, missingTemplates);
    subjects = await listSubjects(env.DB);
  }
  const subjectBySlug = new Map(subjects.map((subject) => [subject.slug, subject]));
  const subjectTemplateRows = subjectTemplates.map((template) => {
    const subject = subjectBySlug.get(template.slug);
    const customManageUrl = template.slug === "mathematics" ? "/admin/modules/subjects/mathematics" : template.slug === "higher-mathematics" ? "/admin/modules/subjects/higher-mathematics" : null;
    return {
      name: template.name,
      manageUrl: subject ? customManageUrl ?? `/admin/modules/subjects/${subject.id}` : "/admin/modules/subjects",
      manageLabel: subject ? "Manage" : "Sync pending"
    };
  });
  const content = renderSubjectsModuleContent({
    subjects: subjectTemplateRows,
    ...successMessage ? { successMessage } : {},
    ...errorMessage ? { errorMessage } : {}
  });
  return renderAdminPage(context, content);
}, "renderSubjectsModule");
var renderSubjectDetail = /* @__PURE__ */ __name(async (env, context, subjectId, classSubjectId, successMessage, errorMessage) => {
  const subject = await getSubjectById(env.DB, subjectId);
  if (!subject) {
    return redirectResponse("/admin/modules/subjects");
  }
  const classGroups = await listSubjectClassGroups(env.DB, subjectId);
  const content = renderSubjectDetailContent({
    subject,
    classGroups,
    ...classSubjectId !== null ? { selectedClassSubjectId: classSubjectId } : {},
    ...successMessage ? { successMessage } : {},
    ...errorMessage ? { errorMessage } : {}
  });
  return renderAdminPage(context, content);
}, "renderSubjectDetail");
var getAdminSession = /* @__PURE__ */ __name(async (request, env) => {
  const cookieValue = getCookieValue(request.headers.get("cookie"), SESSION_CONFIG.COOKIE_NAME);
  if (!cookieValue) {
    return null;
  }
  return readAdminSessionToken(cookieValue, env.JWT_SECRET);
}, "getAdminSession");
var handleAdminRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const url = new URL(request.url);
  if (url.pathname === "/setup-admin") {
    if (request.method === "GET") {
      return getAdminSetup(env);
    }
    if (request.method === "POST") {
      return postAdminSetup(request, env);
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === "/logout") {
    return redirectResponse("/", { "Set-Cookie": clearAdminSessionCookie() });
  }
  if (url.pathname === "/login") {
    if (!context.adminReady) {
      return redirectResponse("/setup-admin");
    }
    if (request.method === "GET") {
      if (context.session) {
        return redirectResponse("/");
      }
      return renderLogin(context);
    }
    if (request.method === "POST") {
      const formData = await request.formData();
      const email = formData.get("email");
      const password = formData.get("password");
      if (typeof email !== "string" || typeof password !== "string") {
        return renderLogin(context, "Please enter your email and password.");
      }
      const normalizedEmail = email.trim().toLowerCase();
      if (!normalizedEmail.endsWith("@gmail.com")) {
        return renderLogin(context, "Please use a Gmail address to sign in.");
      }
      const adminSession = await verifyAdminLogin(env.DB, normalizedEmail, password);
      if (!adminSession) {
        return renderLogin(context, "The email or password you entered is incorrect.");
      }
      const token = await createAdminSessionToken(adminSession, env.JWT_SECRET);
      return redirectResponse("/", { "Set-Cookie": serializeAdminSessionCookie(token) });
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === "/admin/users") {
    if (!context.session) {
      return redirectResponse("/login");
    }
    if (request.method === "GET") {
      const role = normalizeUserRole(url.searchParams.get("role"));
      const query = url.searchParams.get("q")?.trim() ?? null;
      const successMessage = url.searchParams.get("created") === "1" ? "User account created." : url.searchParams.get("deleted") === "1" ? "User account deleted." : void 0;
      const errorMessage = url.searchParams.get("error") === "invalid-password" ? "Admin password did not match. Please try again." : url.searchParams.get("error") === "missing-password" ? "Enter your admin password to delete a user." : void 0;
      return renderUserList(env, context, role, query, successMessage, errorMessage);
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === "/admin/modules") {
    if (!context.session) {
      return redirectResponse("/login");
    }
    if (request.method === "GET") {
      return renderModules(env, context);
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === "/admin/modules/subjects") {
    if (!context.session) {
      return redirectResponse("/login");
    }
    if (request.method === "GET") {
      const successMessage = url.searchParams.get("deleted") === "1" ? "Subject deleted." : url.searchParams.get("updated") === "1" ? "Subject updated." : void 0;
      const errorMessage = url.searchParams.get("error") === "invalid" ? "Please complete all fields." : void 0;
      return renderSubjectsModule(env, context, successMessage, errorMessage);
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === "/admin/modules/subjects/class-groups") {
    if (!context.session) {
      return redirectResponse("/login");
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  const subjectRouteResponse = await handleSubjectAdminRoutes(request, env, context);
  if (subjectRouteResponse) {
    return subjectRouteResponse;
  }
  const subjectDetailMatch = url.pathname.match(/^\/admin\/modules\/subjects\/(\d+)$/);
  if (subjectDetailMatch) {
    if (!context.session) {
      return redirectResponse("/login");
    }
    const subjectId = Number(subjectDetailMatch[1]);
    const classSubjectId = parseNumberParam5(url.searchParams.get("classSubjectId"));
    if (request.method === "GET") {
      const successMessage = url.searchParams.get("updated") === "1" ? "Update saved." : void 0;
      const errorMessage = url.searchParams.get("error") === "invalid" ? "Please complete all fields." : void 0;
      return renderSubjectDetail(
        env,
        context,
        subjectId,
        classSubjectId,
        successMessage,
        errorMessage
      );
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  const subjectActionMatch = url.pathname.match(
    /^\/admin\/modules\/subjects\/(\d+)\/(delete|chapters|chapters\/delete|topics|topics\/delete|content)$/
  );
  if (subjectActionMatch) {
    if (!context.session) {
      return redirectResponse("/login");
    }
    const subjectId = Number(subjectActionMatch[1]);
    const action = subjectActionMatch[2];
    const subject = await getSubjectById(env.DB, subjectId);
    const template = getSubjectTemplate(subject?.templateSlug ?? subject?.slug ?? null);
    if (request.method !== "POST") {
      return jsonResponse({ error: "Method not allowed" }, 405);
    }
    const formData = await request.formData();
    const classSubjectId = parseNumberParam5(formData.get("classSubjectId")?.toString() ?? null);
    const chapterId = parseNumberParam5(formData.get("chapterId")?.toString() ?? null);
    const topicId = parseNumberParam5(formData.get("topicId")?.toString() ?? null);
    try {
      if (action === "delete") {
        await deleteSubject(env.DB, subjectId);
        return redirectResponse("/admin/modules/subjects?deleted=1");
      }
      if (action === "chapters") {
        const title = formData.get("title");
        const slug = formData.get("slug");
        const position = parseNumberParam5(formData.get("position")?.toString() ?? null) ?? 0;
        const summary = formData.get("summary");
        if (!classSubjectId || typeof title !== "string" || typeof slug !== "string") {
          return redirectResponse(`/admin/modules/subjects/${subjectId}?error=invalid`);
        }
        await createChapter(env.DB, {
          classSubjectId,
          title: title.trim(),
          slug: slug.trim().toLowerCase(),
          position,
          summary: typeof summary === "string" ? summary.trim() : void 0
        });
        return redirectResponse(`/admin/modules/subjects/${subjectId}?classSubjectId=${classSubjectId}&updated=1`);
      }
      if (action === "chapters/delete") {
        if (!classSubjectId || !chapterId) {
          return redirectResponse(`/admin/modules/subjects/${subjectId}?error=invalid`);
        }
        await deleteChapter(env.DB, chapterId);
        return redirectResponse(`/admin/modules/subjects/${subjectId}?classSubjectId=${classSubjectId}&updated=1`);
      }
      if (action === "topics") {
        if (template && !template.structure.hasTopics) {
          return redirectResponse(`/admin/modules/subjects/${subjectId}?error=invalid`);
        }
        const title = formData.get("title");
        const slug = formData.get("slug");
        const position = parseNumberParam5(formData.get("position")?.toString() ?? null) ?? 0;
        if (!classSubjectId || !chapterId || typeof title !== "string" || typeof slug !== "string") {
          return redirectResponse(`/admin/modules/subjects/${subjectId}?error=invalid`);
        }
        await createTopic(env.DB, {
          chapterId,
          title: title.trim(),
          slug: slug.trim().toLowerCase(),
          position
        });
        return redirectResponse(
          `/admin/modules/subjects/${subjectId}?classSubjectId=${classSubjectId}&chapterId=${chapterId}&updated=1`
        );
      }
      if (action === "topics/delete") {
        if (template && !template.structure.hasTopics) {
          return redirectResponse(`/admin/modules/subjects/${subjectId}?error=invalid`);
        }
        if (!classSubjectId || !chapterId || !topicId) {
          return redirectResponse(`/admin/modules/subjects/${subjectId}?error=invalid`);
        }
        await deleteTopic(env.DB, topicId);
        return redirectResponse(
          `/admin/modules/subjects/${subjectId}?classSubjectId=${classSubjectId}&chapterId=${chapterId}&updated=1`
        );
      }
      if (action === "content") {
        const contentType = formData.get("contentType");
        const title = formData.get("title");
        const body = formData.get("body");
        const resourceUrl = formData.get("resourceUrl");
        const position = parseNumberParam5(formData.get("position")?.toString() ?? null) ?? 0;
        const requiresTopic = template?.structure.hasTopics ?? true;
        if (!classSubjectId || !chapterId || requiresTopic && !topicId || typeof contentType !== "string" || typeof title !== "string") {
          return redirectResponse(`/admin/modules/subjects/${subjectId}?error=invalid`);
        }
        await createContentItem(env.DB, {
          chapterId: requiresTopic ? void 0 : chapterId,
          topicId,
          contentType: contentType.trim(),
          title: title.trim(),
          body: typeof body === "string" ? body.trim() : void 0,
          resourceUrl: typeof resourceUrl === "string" ? resourceUrl.trim() : void 0,
          position
        });
        return redirectResponse(
          requiresTopic ? `/admin/modules/subjects/${subjectId}?classSubjectId=${classSubjectId}&chapterId=${chapterId}&topicId=${topicId}&updated=1` : `/admin/modules/subjects/${subjectId}?classSubjectId=${classSubjectId}&chapterId=${chapterId}&updated=1`
        );
      }
    } catch {
      return redirectResponse(`/admin/modules/subjects/${subjectId}?error=invalid`);
    }
    return redirectResponse(`/admin/modules/subjects/${subjectId}?error=invalid`);
  }
  if (url.pathname === "/admin/modules/subjects/content/delete") {
    if (!context.session) {
      return redirectResponse("/login");
    }
    if (request.method === "POST") {
      const formData = await request.formData();
      const contentItemId = parseNumberParam5(formData.get("contentItemId")?.toString() ?? null);
      const subjectId = parseNumberParam5(formData.get("subjectId")?.toString() ?? null);
      const classSubjectId = parseNumberParam5(formData.get("classSubjectId")?.toString() ?? null);
      const chapterId = parseNumberParam5(formData.get("chapterId")?.toString() ?? null);
      const topicId = parseNumberParam5(formData.get("topicId")?.toString() ?? null);
      if (!contentItemId) {
        return redirectResponse("/admin/modules/subjects?error=invalid");
      }
      await deleteContentItem(env.DB, contentItemId);
      if (subjectId && classSubjectId && chapterId && topicId) {
        return redirectResponse(
          `/admin/modules/subjects/${subjectId}?classSubjectId=${classSubjectId}&chapterId=${chapterId}&topicId=${topicId}&updated=1`
        );
      }
      if (subjectId && classSubjectId && chapterId) {
        return redirectResponse(
          `/admin/modules/subjects/${subjectId}?classSubjectId=${classSubjectId}&chapterId=${chapterId}&updated=1`
        );
      }
      return redirectResponse("/admin/modules/subjects?updated=1");
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === "/admin/users/new") {
    if (!context.session) {
      return redirectResponse("/login");
    }
    if (request.method === "GET") {
      const role = normalizeUserRole(url.searchParams.get("role"));
      return renderCreateUser(context, role);
    }
    if (request.method === "POST") {
      const formData = await request.formData();
      const role = normalizeUserRole(formData.get("role")?.toString() ?? null);
      const name = formData.get("name");
      const email = formData.get("email");
      const password = formData.get("password");
      const dateOfBirth = formData.get("dateOfBirth");
      if (!role || typeof name !== "string" || typeof email !== "string" || typeof password !== "string" || typeof dateOfBirth !== "string") {
        return renderCreateUser(context, role, "Please complete all required fields.");
      }
      const trimmedName = name.trim();
      const normalizedEmail = email.trim().toLowerCase();
      const trimmedDob = dateOfBirth.trim();
      if (!trimmedName) {
        return renderCreateUser(context, role, "Please enter a full name.", {
          name: trimmedName,
          email: normalizedEmail,
          dateOfBirth: trimmedDob
        });
      }
      if (!normalizedEmail.endsWith("@gmail.com")) {
        return renderCreateUser(context, role, "Please use a Gmail address.", {
          name: trimmedName,
          email: normalizedEmail,
          dateOfBirth: trimmedDob
        });
      }
      if (!isValidPassword(password)) {
        return renderCreateUser(
          context,
          role,
          "Password must be at least 8 characters with upper, lower, and number.",
          {
            name: trimmedName,
            email: normalizedEmail,
            dateOfBirth: trimmedDob
          }
        );
      }
      const existingRole = await findUserRoleByEmail(env.DB, normalizedEmail);
      if (existingRole) {
        return renderCreateUser(context, role, `Email already belongs to a ${existingRole} account.`, {
          name: trimmedName,
          email: normalizedEmail,
          dateOfBirth: trimmedDob
        });
      }
      try {
        await createUserAccount(env.DB, {
          role,
          name: trimmedName,
          email: normalizedEmail,
          password,
          dateOfBirth: trimmedDob
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : "Unable to create account.";
        return renderCreateUser(context, role, message, {
          name: trimmedName,
          email: normalizedEmail,
          dateOfBirth: trimmedDob
        });
      }
      return redirectResponse("/admin/users?created=1");
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === "/admin/users/delete") {
    if (!context.session) {
      return redirectResponse("/login");
    }
    if (request.method === "POST") {
      const formData = await request.formData();
      const role = normalizeUserRole(formData.get("role")?.toString() ?? null);
      const email = formData.get("email");
      const adminPassword = formData.get("adminPassword");
      if (!role || typeof email !== "string") {
        return redirectResponse("/admin/users");
      }
      const normalizedEmail = email.trim().toLowerCase();
      if (!normalizedEmail) {
        return redirectResponse("/admin/users");
      }
      if (typeof adminPassword !== "string" || !adminPassword.trim()) {
        return redirectResponse("/admin/users?error=missing-password");
      }
      const adminSession = await verifyAdminLogin(env.DB, context.session.email, adminPassword);
      if (!adminSession) {
        return redirectResponse("/admin/users?error=invalid-password");
      }
      try {
        await deleteUserAccount(env.DB, { role, email: normalizedEmail });
      } catch {
        return redirectResponse("/admin/users");
      }
      return redirectResponse("/admin/users?deleted=1");
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  return null;
}, "handleAdminRoutes");

// src/ui/pages/home/content.ts
var renderHomeContent = /* @__PURE__ */ __name(() => `
  <section class="page home">
    <section class="home-cover">
      <div class="home-cover__shapes" aria-hidden="true">
        <span class="shape shape--one"></span>
        <span class="shape shape--two"></span>
        <span class="shape shape--three"></span>
        <span class="shape shape--four"></span>
        <span class="shape shape--five"></span>
      </div>
      <div class="home-cover__content">
        <h1 class="home-cover__title">freeducation</h1>
        <div class="home-cover__quotes" aria-live="polite">
          <ul class="home-cover__list">
            <li class="home-cover__item">"Education is the most powerful weapon which you can use to change the world." \u2014 Nelson Mandela</li>
            <li class="home-cover__item">"The roots of education are bitter, but the fruit is sweet." \u2014 Aristotle</li>
            <li class="home-cover__item">"An investment in knowledge pays the best interest." \u2014 Benjamin Franklin</li>
            <li class="home-cover__item">"Education is not the filling of a pail, but the lighting of a fire." \u2014 William Butler Yeats</li>
          </ul>
        </div>
      </div>
    </section>
  </section>
`, "renderHomeContent");

// src/docs/openapi/specification.ts
var createOpenAPIDocument = /* @__PURE__ */ __name(() => ({
  openapi: "3.0.0",
  info: {
    title: "FreeEducation API",
    version: "1.0.0",
    description: "Educational platform API for managing students, teachers, content, and assessments",
    contact: {
      name: "FreeEducation Team",
      email: "support@freeducation.org"
    },
    license: {
      name: "MIT",
      url: "https://opensource.org/licenses/MIT"
    }
  },
  servers: [
    {
      url: "https://freeducation.org",
      description: "Production server"
    },
    {
      url: "https://dev.freeducation.org",
      description: "Development server"
    },
    {
      url: "http://localhost:8787",
      description: "Local development"
    }
  ],
  tags: [
    {
      name: "Authentication",
      description: "User authentication and session management"
    },
    {
      name: "Users",
      description: "User management (students, teachers, admins)"
    },
    {
      name: "Subjects",
      description: "Educational subjects and curriculum management"
    },
    {
      name: "Content",
      description: "Educational content (chapters, topics, materials)"
    },
    {
      name: "Assessments",
      description: "Exams, quizzes, and assessments"
    }
  ],
  paths: {
    // Authentication endpoints
    "/login": {
      post: {
        tags: ["Authentication"],
        summary: "User login",
        description: "Authenticate user and create session",
        requestBody: {
          required: true,
          content: {
            "application/x-www-form-urlencoded": {
              schema: {
                type: "object",
                required: ["email", "password"],
                properties: {
                  email: {
                    type: "string",
                    format: "email",
                    description: "User email address (Gmail only)"
                  },
                  password: {
                    type: "string",
                    minLength: 8,
                    description: "User password"
                  }
                }
              }
            }
          }
        },
        responses: {
          "200": {
            description: "Login successful",
            headers: {
              "Set-Cookie": {
                description: "Session cookie",
                schema: { type: "string" }
              }
            }
          },
          "400": {
            description: "Invalid credentials",
            content: {
              "application/json": {
                schema: {
                  type: "object",
                  properties: {
                    error: { type: "string" }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/logout": {
      get: {
        tags: ["Authentication"],
        summary: "User logout",
        description: "Clear user session",
        responses: {
          "302": {
            description: "Redirect to home"
          }
        }
      }
    },
    // Student signup endpoints
    "/signup": {
      post: {
        tags: ["Authentication"],
        summary: "Student registration",
        description: "Register new student account",
        requestBody: {
          required: true,
          content: {
            "application/x-www-form-urlencoded": {
              schema: {
                type: "object",
                required: ["name", "email", "password", "dateOfBirth"],
                properties: {
                  name: {
                    type: "string",
                    minLength: 2,
                    maxLength: 100,
                    description: "Student full name"
                  },
                  email: {
                    type: "string",
                    format: "email",
                    description: "Gmail address only"
                  },
                  password: {
                    type: "string",
                    minLength: 8,
                    description: "Password with upper, lower, and number"
                  },
                  dateOfBirth: {
                    type: "string",
                    format: "date",
                    description: "Date of birth (YYYY-MM-DD)"
                  }
                }
              }
            }
          }
        },
        responses: {
          "302": {
            description: "Redirect to verification page"
          },
          "400": {
            description: "Validation error"
          }
        }
      }
    },
    "/signup/verify": {
      post: {
        tags: ["Authentication"],
        summary: "Email verification",
        description: "Verify student email with verification code",
        requestBody: {
          required: true,
          content: {
            "application/x-www-form-urlencoded": {
              schema: {
                type: "object",
                required: ["email", "code"],
                properties: {
                  email: {
                    type: "string",
                    format: "email",
                    description: "Student email address"
                  },
                  code: {
                    type: "string",
                    pattern: "^\\d{6}$",
                    description: "6-digit verification code"
                  }
                }
              }
            }
          }
        },
        responses: {
          "200": {
            description: "Email verified successfully"
          },
          "400": {
            description: "Invalid verification code"
          }
        }
      }
    },
    // Admin setup
    "/setup-admin": {
      post: {
        tags: ["Authentication"],
        summary: "Create first admin",
        description: "Setup initial admin account (only if no admin exists)",
        requestBody: {
          required: true,
          content: {
            "application/x-www-form-urlencoded": {
              schema: {
                type: "object",
                required: ["name", "email", "password", "dateOfBirth"],
                properties: {
                  name: { type: "string", minLength: 2 },
                  email: { type: "string", format: "email" },
                  password: { type: "string", minLength: 8 },
                  dateOfBirth: { type: "string", format: "date" }
                }
              }
            }
          }
        },
        responses: {
          "302": {
            description: "Admin created and redirected to home"
          },
          "400": {
            description: "Admin already exists or validation error"
          }
        }
      }
    },
    // Health check
    "/health": {
      get: {
        tags: ["System"],
        summary: "Health check",
        description: "Check API health status",
        responses: {
          "200": {
            description: "API is healthy",
            content: {
              "application/json": {
                schema: {
                  type: "object",
                  properties: {
                    status: { type: "string", example: "ok" }
                  }
                }
              }
            }
          }
        }
      }
    },
    // Admin user management
    "/admin/users": {
      get: {
        tags: ["Users"],
        summary: "List users",
        description: "Get list of all users with optional filtering",
        parameters: [
          {
            name: "role",
            in: "query",
            schema: {
              type: "string",
              enum: ["admin", "student", "teacher"]
            },
            description: "Filter by user role"
          },
          {
            name: "q",
            in: "query",
            schema: { type: "string" },
            description: "Search query"
          }
        ],
        responses: {
          "200": {
            description: "List of users",
            content: {
              "text/html": {
                schema: { type: "string" }
              }
            }
          },
          "401": {
            description: "Unauthorized"
          }
        },
        security: [{ adminSession: [] }]
      }
    },
    "/admin/users/new": {
      post: {
        tags: ["Users"],
        summary: "Create user",
        description: "Create new user account",
        requestBody: {
          required: true,
          content: {
            "application/x-www-form-urlencoded": {
              schema: {
                type: "object",
                required: ["role", "name", "email", "password", "dateOfBirth"],
                properties: {
                  role: {
                    type: "string",
                    enum: ["admin", "student", "teacher"]
                  },
                  name: { type: "string", minLength: 2 },
                  email: { type: "string", format: "email" },
                  password: { type: "string", minLength: 8 },
                  dateOfBirth: { type: "string", format: "date" }
                }
              }
            }
          }
        },
        responses: {
          "302": {
            description: "User created and redirected to user list"
          },
          "400": {
            description: "Validation error"
          },
          "401": {
            description: "Unauthorized"
          }
        },
        security: [{ adminSession: [] }]
      }
    },
    // Subject management
    "/admin/modules/subjects": {
      get: {
        tags: ["Subjects"],
        summary: "List subjects",
        description: "Get list of all subjects",
        responses: {
          "200": {
            description: "List of subjects",
            content: {
              "text/html": {
                schema: { type: "string" }
              }
            }
          },
          "401": {
            description: "Unauthorized"
          }
        },
        security: [{ adminSession: [] }]
      }
    },
    "/admin/modules/subjects/{subjectId}": {
      get: {
        tags: ["Subjects"],
        summary: "Get subject details",
        description: "Get detailed information about a subject",
        parameters: [
          {
            name: "subjectId",
            in: "path",
            required: true,
            schema: { type: "integer" }
          }
        ],
        responses: {
          "200": {
            description: "Subject details",
            content: {
              "text/html": {
                schema: { type: "string" }
              }
            }
          },
          "404": {
            description: "Subject not found"
          },
          "401": {
            description: "Unauthorized"
          }
        },
        security: [{ adminSession: [] }]
      }
    }
  },
  components: {
    securitySchemes: {
      adminSession: {
        type: "apiKey",
        in: "cookie",
        name: "admin_session",
        description: "Admin session cookie for authentication"
      }
    },
    schemas: {
      User: {
        type: "object",
        properties: {
          id: { type: "integer" },
          name: { type: "string" },
          email: { type: "string", format: "email" },
          role: { type: "string", enum: ["admin", "student", "teacher"] },
          dateOfBirth: { type: "string", format: "date" },
          createdAt: { type: "string", format: "date-time" }
        }
      },
      Subject: {
        type: "object",
        properties: {
          id: { type: "integer" },
          name: { type: "string" },
          slug: { type: "string" },
          description: { type: "string" },
          isTwoPaper: { type: "boolean" },
          createdAt: { type: "string", format: "date-time" }
        }
      },
      Chapter: {
        type: "object",
        properties: {
          id: { type: "integer" },
          title: { type: "string" },
          slug: { type: "string" },
          summary: { type: "string" },
          position: { type: "integer" },
          createdAt: { type: "string", format: "date-time" }
        }
      },
      Error: {
        type: "object",
        properties: {
          error: { type: "string" },
          message: { type: "string" }
        }
      }
    }
  }
}), "createOpenAPIDocument");

// src/docs/openapi/index.ts
var getOpenAPIYAML = /* @__PURE__ */ __name(() => {
  const doc = createOpenAPIDocument();
  const yamlString = jsonToYaml(doc);
  return yamlString;
}, "getOpenAPIYAML");
var getOpenAPIJSON = /* @__PURE__ */ __name(() => {
  const doc = createOpenAPIDocument();
  return JSON.stringify(doc, null, 2);
}, "getOpenAPIJSON");
var jsonToYaml = /* @__PURE__ */ __name((obj, indent = 0) => {
  const spaces = "  ".repeat(indent);
  let yaml = "";
  if (obj === null || obj === void 0) {
    return "null\n";
  }
  if (typeof obj === "string" || typeof obj === "number" || typeof obj === "boolean") {
    return `${obj}
`;
  }
  if (Array.isArray(obj)) {
    if (obj.length === 0) return "[]\n";
    obj.forEach((item) => {
      yaml += `${spaces}- `;
      if (typeof item === "object" && item !== null) {
        yaml += "\n" + jsonToYaml(item, indent + 1);
      } else {
        yaml += jsonToYaml(item, 0).trim() + "\n";
      }
    });
    return yaml;
  }
  if (typeof obj === "object") {
    Object.entries(obj).forEach(([key, value]) => {
      if (typeof value === "object" && value !== null && !Array.isArray(value)) {
        yaml += `${spaces}${key}:
${jsonToYaml(value, indent + 1)}`;
      } else if (Array.isArray(value)) {
        yaml += `${spaces}${key}:
${jsonToYaml(value, indent)}`;
      } else {
        yaml += `${spaces}${key}: ${jsonToYaml(value, 0).trim()}
`;
      }
    });
  }
  return yaml;
}, "jsonToYaml");

// src/api/public.ts
var getCacheStore = /* @__PURE__ */ __name(() => {
  const cacheStorage = globalThis.caches;
  return cacheStorage?.default ?? null;
}, "getCacheStore");
var renderHome = /* @__PURE__ */ __name((device, session, cacheable, nonce) => {
  const content = renderHomeContent();
  const headers = cacheable ? {
    "Cache-Control": "public, max-age=60, stale-while-revalidate=300",
    Vary: "User-Agent"
  } : void 0;
  const layoutProps = { device, content, session };
  if (nonce) {
    layoutProps.nonce = nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, headers);
}, "renderHome");
var handlePublicRoutes = /* @__PURE__ */ __name(async (request, context) => {
  const url = new URL(request.url);
  if (url.pathname === "/health") {
    return jsonResponse({ status: "ok" });
  }
  if (url.pathname === "/api-docs") {
    const format = url.searchParams.get("format") || "json";
    const content = format === "yaml" ? getOpenAPIYAML() : getOpenAPIJSON();
    const contentType = format === "yaml" ? "application/x-yaml" : "application/json";
    return new Response(content, {
      status: 200,
      headers: {
        "content-type": contentType,
        "access-control-allow-origin": "*"
      }
    });
  }
  if (url.pathname === "/" || url.pathname === "/home") {
    if (!context.adminReady) {
      return null;
    }
    const isCacheable = request.method === "GET" && !context.session;
    if (isCacheable) {
      const cache = getCacheStore();
      const cacheKey = new Request(`${url.origin}${url.pathname}?device=${context.device}`);
      const cached = await cache?.match(cacheKey);
      if (cached) {
        return cached;
      }
      const response = renderHome(context.device, context.session, true, context.nonce);
      await cache?.put(cacheKey, response.clone());
      return response;
    }
    return renderHome(context.device, context.session, false, context.nonce);
  }
  return null;
}, "handlePublicRoutes");

// src/domains/auth/studentSignup.ts
var selectStudentByEmail = /* @__PURE__ */ __name(async (db, email) => {
  const result = await db.prepare(
    "SELECT id, name, email, password_hash as passwordHash, date_of_birth as dateOfBirth, verified_at as verifiedAt FROM students WHERE email = ? LIMIT 1"
  ).bind(email).all();
  return result.results[0] ?? null;
}, "selectStudentByEmail");
var insertStudent = /* @__PURE__ */ __name(async (db, payload) => {
  const passwordHash = await hashPassword(payload.password);
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(
    "INSERT INTO students (name, email, password_hash, date_of_birth, created_at) VALUES (?, ?, ?, ?, ?)"
  ).bind(payload.name, payload.email, passwordHash, payload.dateOfBirth, createdAt).run();
  const record = await selectStudentByEmail(db, payload.email);
  if (!record) {
    throw new Error("Unable to create student.");
  }
  return record;
}, "insertStudent");
var updateStudent = /* @__PURE__ */ __name(async (db, studentId, payload) => {
  const passwordHash = await hashPassword(payload.password);
  await db.prepare("UPDATE students SET name = ?, password_hash = ?, date_of_birth = ? WHERE id = ?").bind(payload.name, passwordHash, payload.dateOfBirth, studentId).run();
}, "updateStudent");
var clearStudentVerifications = /* @__PURE__ */ __name(async (db, studentId) => {
  await db.prepare("DELETE FROM student_verifications WHERE student_id = ?").bind(studentId).run();
}, "clearStudentVerifications");
var insertStudentVerification = /* @__PURE__ */ __name(async (db, payload) => {
  const createdAt = (/* @__PURE__ */ new Date()).toISOString();
  await db.prepare(
    "INSERT INTO student_verifications (student_id, code_hash, attempts, expires_at, created_at) VALUES (?, ?, ?, ?, ?)"
  ).bind(payload.studentId, payload.codeHash, 0, payload.expiresAt, createdAt).run();
}, "insertStudentVerification");
var selectLatestVerification = /* @__PURE__ */ __name(async (db, studentId) => {
  const result = await db.prepare(
    "SELECT id, student_id as studentId, code_hash as codeHash, attempts, expires_at as expiresAt, used_at as usedAt FROM student_verifications WHERE student_id = ? ORDER BY created_at DESC LIMIT 1"
  ).bind(studentId).all();
  return result.results[0] ?? null;
}, "selectLatestVerification");
var updateVerificationAttempts = /* @__PURE__ */ __name(async (db, verificationId, attempts) => {
  await db.prepare("UPDATE student_verifications SET attempts = ? WHERE id = ?").bind(attempts, verificationId).run();
}, "updateVerificationAttempts");
var markVerificationUsed = /* @__PURE__ */ __name(async (db, verificationId) => {
  await db.prepare("UPDATE student_verifications SET used_at = ? WHERE id = ?").bind((/* @__PURE__ */ new Date()).toISOString(), verificationId).run();
}, "markVerificationUsed");
var markStudentVerified = /* @__PURE__ */ __name(async (db, studentId) => {
  await db.prepare("UPDATE students SET verified_at = ? WHERE id = ?").bind((/* @__PURE__ */ new Date()).toISOString(), studentId).run();
}, "markStudentVerified");
var createOrRefreshStudentSignup = /* @__PURE__ */ __name(async (db, payload) => {
  const existing = await selectStudentByEmail(db, payload.email);
  if (existing?.verifiedAt) {
    throw new Error("An account with this email already exists.");
  }
  let student = existing;
  if (student) {
    await updateStudent(db, student.id, {
      name: payload.name,
      password: payload.password,
      dateOfBirth: payload.dateOfBirth
    });
  } else {
    student = await insertStudent(db, {
      name: payload.name,
      email: payload.email,
      password: payload.password,
      dateOfBirth: payload.dateOfBirth
    });
  }
  await clearStudentVerifications(db, student.id);
  await insertStudentVerification(db, {
    studentId: student.id,
    codeHash: payload.codeHash,
    expiresAt: payload.expiresAt
  });
  return { student };
}, "createOrRefreshStudentSignup");
var deleteStudentSignup = /* @__PURE__ */ __name(async (db, studentId) => {
  await clearStudentVerifications(db, studentId);
  await db.prepare("DELETE FROM students WHERE id = ?").bind(studentId).run();
}, "deleteStudentSignup");
var verifyStudentSignup = /* @__PURE__ */ __name(async (db, payload) => {
  const student = await selectStudentByEmail(db, payload.email);
  if (!student) {
    throw new Error("No account was found for that email.");
  }
  if (student.verifiedAt) {
    return { student };
  }
  const verification = await selectLatestVerification(db, student.id);
  if (!verification) {
    throw new Error("No verification request was found. Please request a new code.");
  }
  if (verification.usedAt) {
    throw new Error("This verification code has already been used.");
  }
  const now = Date.now();
  if (Date.parse(verification.expiresAt) <= now) {
    throw new Error("Your verification code has expired. Please request a new one.");
  }
  if (verification.attempts >= 5) {
    throw new Error("Too many attempts. Please request a new verification code.");
  }
  const matches = await verifyPassword(payload.code, verification.codeHash);
  if (!matches) {
    await updateVerificationAttempts(db, verification.id, verification.attempts + 1);
    throw new Error("The verification code is incorrect.");
  }
  await markVerificationUsed(db, verification.id);
  await markStudentVerified(db, student.id);
  return { student };
}, "verifyStudentSignup");

// src/ui/pages/signup/content.ts
var escapeValue12 = /* @__PURE__ */ __name((value) => value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;"), "escapeValue");
var renderSignupContent = /* @__PURE__ */ __name(({ errorMessage, values } = {}) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Student signup</h1>
      <p class="page-subtitle">Create your student account with a Gmail address.</p>
    </header>
    ${errorMessage ? `<div class="alert alert--error">${escapeValue12(errorMessage)}</div>` : ""}
    <form class="form-card form-grid" method="POST" action="/signup">
      <label class="form-field">
        <span>Full name</span>
        <input name="name" autocomplete="name" required value="${values?.name ? escapeValue12(values.name) : ""}" />
      </label>
      <label class="form-field">
        <span>Date of birth</span>
        <input type="date" name="dateOfBirth" required value="${values?.dateOfBirth ? escapeValue12(values.dateOfBirth) : ""}" />
      </label>
      <label class="form-field">
        <span>Gmail address</span>
        <input type="email" name="email" placeholder="you@gmail.com" autocomplete="email" required value="${values?.email ? escapeValue12(values.email) : ""}" />
      </label>
      <label class="form-field">
        <span>Password</span>
        <input type="password" name="password" autocomplete="new-password" required minlength="8" />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">Verify</button>
      </div>
      <p class="helper-text">We will email a verification code to complete your signup.</p>
    </form>
  </section>
`, "renderSignupContent");
var renderSignupVerificationContent = /* @__PURE__ */ __name(({ errorMessage, email } = {}) => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Verify your email</h1>
      <p class="page-subtitle">Enter the 6-digit code sent to your Gmail address.</p>
    </header>
    ${errorMessage ? `<div class="alert alert--error">${escapeValue12(errorMessage)}</div>` : ""}
    <form class="form-card form-grid" method="POST" action="/signup/verify">
      <label class="form-field">
        <span>Gmail address</span>
        <input type="email" name="email" readonly required value="${email ? escapeValue12(email) : ""}" />
      </label>
      <label class="form-field">
        <span>Verification code</span>
        <input name="code" inputmode="numeric" autocomplete="one-time-code" required />
      </label>
      <div class="form-actions">
        <button type="submit" class="button-link button-link--primary">Confirm</button>
      </div>
      <p class="helper-text">If the code expires, return to signup to request a new one.</p>
    </form>
  </section>
`, "renderSignupVerificationContent");
var renderSignupConfirmationContent = /* @__PURE__ */ __name(() => `
  <section class="page">
    <header class="page-header">
      <h1 class="page-title">Signup complete</h1>
      <p class="page-subtitle">Your student account is verified. You can now sign in when student login is available.</p>
    </header>
    <div class="form-card">
      <p class="helper-text">Thank you for joining Freeducation.</p>
      <div class="form-actions">
        <a class="button-link button-link--primary" href="/login">Go to sign in</a>
      </div>
    </div>
  </section>
`, "renderSignupConfirmationContent");

// src/integrations/email/gmail.ts
var assertGmailConfig = /* @__PURE__ */ __name((env) => {
  const missing = [
    ["GMAIL_CLIENT_ID", env.GMAIL_CLIENT_ID],
    ["GMAIL_CLIENT_SECRET", env.GMAIL_CLIENT_SECRET],
    ["GMAIL_REFRESH_TOKEN", env.GMAIL_REFRESH_TOKEN]
  ].filter(([, value]) => !value).map(([key]) => key);
  if (missing.length > 0) {
    throw new Error(`Missing Gmail configuration: ${missing.join(", ")}.`);
  }
}, "assertGmailConfig");
var buildRawEmail = /* @__PURE__ */ __name((to, subject, body) => {
  const headers = [
    `To: ${to}`,
    "From: Freeducation <noreply@freeducation>",
    `Subject: ${subject}`,
    "MIME-Version: 1.0",
    "Content-Type: text/plain; charset=utf-8"
  ];
  return [...headers, "", body].join("\r\n");
}, "buildRawEmail");
var toBase64Url2 = /* @__PURE__ */ __name((value) => btoa(value).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/g, ""), "toBase64Url");
var fetchAccessToken = /* @__PURE__ */ __name(async (env) => {
  assertGmailConfig(env);
  const payload = new URLSearchParams({
    client_id: env.GMAIL_CLIENT_ID,
    client_secret: env.GMAIL_CLIENT_SECRET,
    refresh_token: env.GMAIL_REFRESH_TOKEN,
    grant_type: "refresh_token"
  });
  const response = await fetch("https://oauth2.googleapis.com/token", {
    method: "POST",
    headers: { "content-type": "application/x-www-form-urlencoded" },
    body: payload.toString()
  });
  if (!response.ok) {
    let errorDetail = response.statusText;
    try {
      const data2 = await response.json();
      errorDetail = data2.error_description || data2.error || errorDetail;
    } catch {
    }
    throw new Error(`Unable to refresh Gmail access token: ${errorDetail}.`);
  }
  const data = await response.json();
  if (!data.access_token) {
    throw new Error("Missing Gmail access token.");
  }
  return data.access_token;
}, "fetchAccessToken");
var sendStudentVerificationEmail = /* @__PURE__ */ __name(async (env, {
  to,
  name,
  code
}) => {
  const accessToken = await fetchAccessToken(env);
  const subject = "Verify your Freeducation account";
  const body = `Hi ${name},

Use the verification code below to complete your Freeducation signup:

${code}

If you did not request this, you can ignore this email.`;
  const rawMessage = buildRawEmail(to, subject, body);
  const response = await fetch("https://gmail.googleapis.com/gmail/v1/users/me/messages/send", {
    method: "POST",
    headers: {
      Authorization: `Bearer ${accessToken}`,
      "content-type": "application/json"
    },
    body: JSON.stringify({ raw: toBase64Url2(rawMessage) })
  });
  if (!response.ok) {
    let errorDetail = response.statusText;
    try {
      const data = await response.json();
      errorDetail = data.error?.message || errorDetail;
    } catch {
    }
    throw new Error(`Unable to send verification email: ${errorDetail}.`);
  }
}, "sendStudentVerificationEmail");

// src/core/security/verification.ts
var createVerificationCode = /* @__PURE__ */ __name(() => {
  const value = crypto.getRandomValues(new Uint32Array(1))[0] ?? 0;
  return String(value % 1e6).padStart(6, "0");
}, "createVerificationCode");
var hashVerificationCode = /* @__PURE__ */ __name(async (code) => hashPassword(code), "hashVerificationCode");

// src/api/student.ts
var isValidPassword2 = /* @__PURE__ */ __name((password) => password.length >= 8 && /[a-z]/.test(password) && /[A-Z]/.test(password) && /\d/.test(password), "isValidPassword");
var renderStudentPage = /* @__PURE__ */ __name((device, session, content, nonce) => {
  const csrfToken = createCSRFToken();
  const layoutProps = { device, content, session, csrfToken };
  if (nonce) {
    layoutProps.nonce = nonce;
  }
  return htmlResponse(renderPageLayout(layoutProps), 200, {
    "Set-Cookie": setCSRFCookie(csrfToken),
    "Cache-Control": "no-store"
  });
}, "renderStudentPage");
var renderSignup = /* @__PURE__ */ __name((device, session, errorMessage, values, nonce) => {
  const content = renderSignupContent({
    ...errorMessage ? { errorMessage } : {},
    ...values ? { values } : {}
  });
  return renderStudentPage(device, session, content, nonce);
}, "renderSignup");
var renderVerification = /* @__PURE__ */ __name((device, session, email, errorMessage, nonce) => {
  const content = renderSignupVerificationContent({
    ...errorMessage ? { errorMessage } : {},
    ...email ? { email } : {}
  });
  return renderStudentPage(device, session, content, nonce);
}, "renderVerification");
var renderConfirmation = /* @__PURE__ */ __name((device, session, nonce) => {
  const content = renderSignupConfirmationContent();
  return renderStudentPage(device, session, content, nonce);
}, "renderConfirmation");
var handleStudentRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const url = new URL(request.url);
  if (url.pathname === "/signup") {
    if (request.method === "GET") {
      return renderSignup(context.device, context.session, void 0, void 0, context.nonce);
    }
    if (request.method === "POST") {
      const formData = await request.formData();
      const name = formData.get("name");
      const email = formData.get("email");
      const password = formData.get("password");
      const dateOfBirth = formData.get("dateOfBirth");
      if (typeof name !== "string" || typeof email !== "string" || typeof password !== "string" || typeof dateOfBirth !== "string") {
        return renderSignup(context.device, context.session, "Please complete all required fields.", void 0, context.nonce);
      }
      const trimmedName = name.trim();
      const normalizedEmail = email.trim().toLowerCase();
      const trimmedDob = dateOfBirth.trim();
      if (!trimmedName) {
        return renderSignup(context.device, context.session, "Please enter your full name.", void 0, context.nonce);
      }
      if (!normalizedEmail.endsWith("@gmail.com")) {
        return renderSignup(context.device, context.session, "Please use a Gmail address to sign up.", {
          name: trimmedName,
          email: normalizedEmail,
          dateOfBirth: trimmedDob
        }, context.nonce);
      }
      if (!isValidPassword2(password)) {
        return renderSignup(
          context.device,
          context.session,
          "Your password must be at least 8 characters and include upper, lower, and number.",
          {
            name: trimmedName,
            email: normalizedEmail,
            dateOfBirth: trimmedDob
          },
          context.nonce
        );
      }
      const existingRole = await findUserRoleByEmail(env.DB, normalizedEmail);
      if (existingRole) {
        return renderSignup(
          context.device,
          context.session,
          `This email already belongs to a ${existingRole} account. Please log in instead.`,
          {
            name: trimmedName,
            email: normalizedEmail,
            dateOfBirth: trimmedDob
          },
          context.nonce
        );
      }
      const code = createVerificationCode();
      const codeHash = await hashVerificationCode(code);
      const expiresAt = new Date(Date.now() + 15 * 60 * 1e3).toISOString();
      try {
        const { student } = await createOrRefreshStudentSignup(env.DB, {
          name: trimmedName,
          email: normalizedEmail,
          password,
          dateOfBirth: trimmedDob,
          codeHash,
          expiresAt
        });
        try {
          await sendStudentVerificationEmail(env, { to: normalizedEmail, name: trimmedName, code });
        } catch (error) {
          await deleteStudentSignup(env.DB, student.id);
          throw error;
        }
      } catch (error) {
        const message = error instanceof Error ? error.message : "Unable to start signup.";
        return renderSignup(context.device, context.session, message, {
          name: trimmedName,
          email: normalizedEmail,
          dateOfBirth: trimmedDob
        }, context.nonce);
      }
      return renderVerification(context.device, context.session, normalizedEmail, void 0, context.nonce);
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  if (url.pathname === "/signup/verify") {
    if (request.method === "GET") {
      const email = url.searchParams.get("email")?.trim().toLowerCase();
      return renderVerification(context.device, context.session, email, email ? void 0 : "Please enter your email.", context.nonce);
    }
    if (request.method === "POST") {
      const formData = await request.formData();
      const email = formData.get("email");
      const code = formData.get("code");
      if (typeof email !== "string" || typeof code !== "string") {
        return renderVerification(context.device, context.session, void 0, "Please enter the email and code.", context.nonce);
      }
      const normalizedEmail = email.trim().toLowerCase();
      const trimmedCode = code.trim();
      if (!normalizedEmail.endsWith("@gmail.com")) {
        return renderVerification(context.device, context.session, normalizedEmail, "Please use a Gmail address.", context.nonce);
      }
      if (!/^\d{6}$/.test(trimmedCode)) {
        return renderVerification(context.device, context.session, normalizedEmail, "Enter the 6-digit code we sent.", context.nonce);
      }
      try {
        await verifyStudentSignup(env.DB, { email: normalizedEmail, code: trimmedCode });
      } catch (error) {
        const message = error instanceof Error ? error.message : "Unable to verify this code.";
        return renderVerification(context.device, context.session, normalizedEmail, message, context.nonce);
      }
      return renderConfirmation(context.device, context.session, context.nonce);
    }
    return jsonResponse({ error: "Method not allowed" }, 405);
  }
  return null;
}, "handleStudentRoutes");

// src/api/user.ts
var UserPreferencesSchema = {
  sidebarState: ["minimized", "expanded"]
};
var handleUserRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const { session } = context;
  const url = new URL(request.url);
  const path = url.pathname;
  if (!path.startsWith("/api/user/")) {
    return null;
  }
  if (request.method === "GET" && path === "/api/user/preferences") {
    if (!session) {
      return jsonResponse({ error: "Unauthorized" }, 401);
    }
    try {
      const result = await env.DB.prepare(`
        SELECT preferences FROM users WHERE email = ?
      `).bind(session.email).all();
      const preferences = result.results && result.results[0] ? JSON.parse(result.results[0].preferences) : {};
      return jsonResponse({
        sidebarState: preferences.sidebarState || "expanded",
        theme: preferences.theme || "light"
      });
    } catch (error) {
      console.error("Failed to get user preferences:", error);
      return jsonResponse({ error: "Failed to get preferences" }, 500);
    }
  }
  if (request.method === "POST" && path === "/api/user/preferences") {
    if (!session) {
      return jsonResponse({ error: "Unauthorized" }, 401);
    }
    const csrfToken = request.headers.get("X-CSRF-Token");
    if (!csrfToken) {
      return jsonResponse({ error: "CSRF token required" }, 403);
    }
    try {
      const body = await request.json();
      if (body.sidebarState && !UserPreferencesSchema.sidebarState.includes(body.sidebarState)) {
        return jsonResponse({ error: "Invalid sidebarState value" }, 400);
      }
      const currentResult = await env.DB.prepare(`
        SELECT preferences FROM users WHERE email = ?
      `).bind(session.email).all();
      const currentPreferences = currentResult.results && currentResult.results[0] ? JSON.parse(currentResult.results[0].preferences) : {};
      const updatedPreferences = {
        ...currentPreferences,
        ...body,
        updatedAt: (/* @__PURE__ */ new Date()).toISOString()
      };
      await env.DB.prepare(`
        UPDATE users SET preferences = ? WHERE email = ?
      `).bind(JSON.stringify(updatedPreferences), session.email).run();
      return jsonResponse({
        success: true,
        preferences: updatedPreferences
      });
    } catch (error) {
      console.error("Failed to update user preferences:", error);
      return jsonResponse({ error: "Failed to update preferences" }, 500);
    }
  }
  return null;
}, "handleUserRoutes");

// src/api/index.ts
var handleApiRoutes = /* @__PURE__ */ __name(async (request, env, context) => {
  const userResponse = await handleUserRoutes(request, env, context);
  if (userResponse) {
    return userResponse;
  }
  const studentResponse = await handleStudentRoutes(request, env, context);
  if (studentResponse) {
    return studentResponse;
  }
  const publicResponse = await handlePublicRoutes(request, context);
  if (publicResponse) {
    return publicResponse;
  }
  const adminResponse = await handleAdminRoutes(request, env, context);
  if (adminResponse) {
    return adminResponse;
  }
  return null;
}, "handleApiRoutes");

// src/core/middleware/rateLimit.ts
var rateLimitStore = /* @__PURE__ */ new Map();
var apiRateLimitStore = /* @__PURE__ */ new Map();
var createRateLimitMiddleware = /* @__PURE__ */ __name((config) => {
  const { windowMs, maxRequests, keyGenerator = defaultKeyGenerator, usePersistentStore = false } = config;
  const selectedStore = usePersistentStore ? apiRateLimitStore : rateLimitStore;
  return (request) => {
    const key = keyGenerator(request);
    const now = Date.now();
    if (!usePersistentStore) {
      for (const [storeKey, data] of selectedStore.entries()) {
        if (data.resetTime < now) {
          selectedStore.delete(storeKey);
        }
      }
    }
    const existing = selectedStore.get(key);
    if (!existing || existing.resetTime < now) {
      selectedStore.set(key, { count: 1, resetTime: now + windowMs });
      return null;
    }
    if (existing.count >= maxRequests) {
      return new Response("Too many requests", {
        status: 429,
        headers: {
          "Content-Type": "text/plain",
          "Retry-After": Math.ceil((existing.resetTime - now) / 1e3).toString(),
          "X-RateLimit-Limit": maxRequests.toString(),
          "X-RateLimit-Remaining": "0",
          "X-RateLimit-Reset": existing.resetTime.toString()
        }
      });
    }
    existing.count++;
    return null;
  };
}, "createRateLimitMiddleware");
var defaultKeyGenerator = /* @__PURE__ */ __name((request) => {
  const forwarded = request.headers.get("X-Forwarded-For");
  const forwardedIp = forwarded ? forwarded.split(",")[0]?.trim() : null;
  const ip = request.headers.get("CF-Connecting-IP") || forwardedIp || "unknown";
  return `${ip}:${new URL(request.url).pathname}`;
}, "defaultKeyGenerator");
var RATE_LIMITS = {
  AUTH: { windowMs: 15 * 60 * 1e3, maxRequests: 5, usePersistentStore: false },
  // In-memory for UI
  GENERAL: { windowMs: 15 * 60 * 1e3, maxRequests: 100, usePersistentStore: false },
  // In-memory for UI
  API: { windowMs: 1 * 60 * 1e3, maxRequests: 30, usePersistentStore: true }
  // Persistent for API
};

// src/core/middleware/validation.ts
var validateEmail = /* @__PURE__ */ __name((email) => {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return emailRegex.test(email);
}, "validateEmail");
var sanitizeString = /* @__PURE__ */ __name((input) => {
  return input.trim().replace(/[<>]/g, "").replace(/javascript:/gi, "").replace(/on\w+=/gi, "");
}, "sanitizeString");
var validateField = /* @__PURE__ */ __name((fieldName, value, rule) => {
  const errors = [];
  if (rule.required && (value === void 0 || value === null || value === "")) {
    errors.push(`${fieldName} is required`);
  }
  if (!rule.required && (value === void 0 || value === null || value === "")) {
    return null;
  }
  if (typeof value !== "string") {
    errors.push(`${fieldName} must be a string`);
  } else {
    const strValue = value;
    if (rule.minLength && strValue.length < rule.minLength) {
      errors.push(`${fieldName} must be at least ${rule.minLength} characters long`);
    }
    if (rule.maxLength && strValue.length > rule.maxLength) {
      errors.push(`${fieldName} must not exceed ${rule.maxLength} characters`);
    }
    if (rule.pattern && !rule.pattern.test(strValue)) {
      errors.push(`${fieldName} format is invalid`);
    }
    if (rule.email && !validateEmail(strValue)) {
      errors.push(`${fieldName} must be a valid email address`);
    }
    if (rule.numeric && !/^\d+$/.test(strValue)) {
      errors.push(`${fieldName} must contain only numbers`);
    }
    if (rule.alphanumeric && !/^[a-zA-Z0-9]+$/.test(strValue)) {
      errors.push(`${fieldName} must contain only letters and numbers`);
    }
    if (rule.sanitize) {
      const sanitized = sanitizeString(strValue);
      if (sanitized !== strValue) {
        errors.push(`${fieldName} contains invalid characters`);
      }
    }
  }
  return errors.length > 0 ? { field: fieldName, message: errors[0], value } : null;
}, "validateField");
var validateFormData = /* @__PURE__ */ __name((formData, schema) => {
  const errors = [];
  const data = formData instanceof FormData ? Array.from(formData.entries()).reduce((acc, [key, value]) => {
    acc[key] = value;
    return acc;
  }, {}) : formData;
  Object.entries(schema).forEach(([fieldName, rule]) => {
    const error = validateField(fieldName, data[fieldName], rule);
    if (error) {
      errors.push(error);
    }
  });
  return errors;
}, "validateFormData");
var COMMON_VALIDATION_SCHEMAS = {
  USER_REGISTRATION: {
    name: { required: true, minLength: 2, maxLength: 100, sanitize: true },
    email: { required: true, email: true, maxLength: 255 },
    password: { required: true, minLength: 8, maxLength: 128 },
    dateOfBirth: { required: true, pattern: /^\d{4}-\d{2}-\d{2}$/ }
  },
  USER_LOGIN: {
    email: { required: true, email: true, maxLength: 255 },
    password: { required: true, minLength: 1 }
  },
  EMAIL_VERIFICATION: {
    email: { required: true, email: true, maxLength: 255 },
    code: { required: true, numeric: true, minLength: 6, maxLength: 6 }
  },
  SUBJECT_CREATION: {
    name: { required: true, minLength: 2, maxLength: 200, sanitize: true },
    slug: { required: true, minLength: 2, maxLength: 100, pattern: /^[a-z0-9-]+$/, alphanumeric: false },
    description: { maxLength: 1e3, sanitize: true }
  },
  CHAPTER_CREATION: {
    title: { required: true, minLength: 2, maxLength: 200, sanitize: true },
    slug: { required: true, minLength: 2, maxLength: 100, pattern: /^[a-z0-9-]+$/, alphanumeric: false },
    summary: { maxLength: 1e3, sanitize: true },
    position: { required: true, numeric: true }
  }
};

// src/core/middleware/securityHeaders.ts
var SECURITY_HEADERS_CONFIG = {
  enableCSP: true,
  enableHSTS: true,
  enableXFrameOptions: true,
  enableXContentTypeOptions: true,
  enableReferrerPolicy: true,
  enablePermissionsPolicy: true
};
var getSecurityHeaders = /* @__PURE__ */ __name((config = SECURITY_HEADERS_CONFIG, nonce) => {
  const headers = {};
  if (config.enableCSP) {
    const scriptSrc = nonce ? `script-src 'self' 'nonce-${nonce}'` : "script-src 'self'";
    headers["Content-Security-Policy"] = [
      "default-src 'self'",
      scriptSrc,
      "style-src 'self' 'unsafe-inline'",
      "img-src 'self' data: https:",
      "font-src 'self' data:",
      "connect-src 'self'",
      "frame-ancestors 'none'",
      "base-uri 'self'",
      "form-action 'self'"
    ].join("; ");
  }
  if (config.enableHSTS) {
    headers["Strict-Transport-Security"] = "max-age=31536000; includeSubDomains; preload";
  }
  if (config.enableXFrameOptions) {
    headers["X-Frame-Options"] = "DENY";
  }
  if (config.enableXContentTypeOptions) {
    headers["X-Content-Type-Options"] = "nosniff";
  }
  if (config.enableReferrerPolicy) {
    headers["Referrer-Policy"] = "strict-origin-when-cross-origin";
  }
  if (config.enablePermissionsPolicy) {
    headers["Permissions-Policy"] = [
      "camera=()",
      "microphone=()",
      "geolocation=()",
      "payment=()",
      "usb=()",
      "magnetometer=()",
      "gyroscope=()",
      "accelerometer=()"
    ].join(", ");
  }
  headers["X-XSS-Protection"] = "1; mode=block";
  headers["X-Permitted-Cross-Domain-Policies"] = "none";
  headers["Cross-Origin-Embedder-Policy"] = "require-corp";
  headers["Cross-Origin-Opener-Policy"] = "same-origin";
  headers["Cross-Origin-Resource-Policy"] = "same-origin";
  return headers;
}, "getSecurityHeaders");
var applySecurityHeaders = /* @__PURE__ */ __name((response, config, nonce) => {
  const headers = getSecurityHeaders(config, nonce);
  const newHeaders = new Headers(response.headers);
  Object.entries(headers).forEach(([key, value]) => {
    newHeaders.set(key, value);
  });
  return new Response(response.body, {
    status: response.status,
    statusText: response.statusText,
    headers: newHeaders
  });
}, "applySecurityHeaders");

// src/core/middleware/index.ts
var generateNonce = /* @__PURE__ */ __name(() => {
  const array = new Uint8Array(16);
  crypto.getRandomValues(array);
  return btoa(String.fromCharCode(...array));
}, "generateNonce");
var createSecurityMiddleware = /* @__PURE__ */ __name((config = {}) => {
  const {
    enableRateLimit = true,
    enableCSRF = false,
    // Disabled by default for GET requests
    enableSecurityHeaders = true,
    enableValidation = false,
    rateLimitType = "GENERAL"
  } = config;
  const rateLimitMiddleware = enableRateLimit ? createRateLimitMiddleware(RATE_LIMITS[rateLimitType]) : null;
  return async (request, context) => {
    if (rateLimitMiddleware) {
      const rateLimitResponse = rateLimitMiddleware(request);
      if (rateLimitResponse) {
        return rateLimitResponse;
      }
    }
    if (enableCSRF && ["POST", "PUT", "DELETE", "PATCH"].includes(request.method)) {
      const csrfToken = await extractCSRFToken(request);
      const csrfCookie = getCSRFCookie(request);
      if (!csrfToken || !csrfCookie) {
        return jsonResponse({ error: "CSRF token missing" }, 403);
      }
      const isValid = validateCSRFToken(csrfToken, csrfCookie);
      if (!isValid) {
        return jsonResponse({ error: "Invalid CSRF token" }, 403);
      }
    }
    if (enableValidation && context?.schema && ["POST", "PUT", "PATCH"].includes(request.method)) {
      try {
        const formData = await request.clone().formData().catch(() => null);
        if (formData) {
          const errors = validateFormData(formData, context.schema);
          if (errors.length > 0) {
            return jsonResponse({
              error: "Validation failed",
              errors: errors.map((e) => ({ field: e.field, message: e.message }))
            }, 400);
          }
        }
      } catch (error) {
        return jsonResponse({ error: "Invalid request data" }, 400);
      }
    }
    return null;
  };
}, "createSecurityMiddleware");
var applySecurityToResponse = /* @__PURE__ */ __name((response, nonce) => {
  return applySecurityHeaders(response, void 0, nonce);
}, "applySecurityToResponse");
var SECURITY_MIDDLEWARE = {
  // For authentication endpoints
  AUTH: createSecurityMiddleware({
    enableRateLimit: true,
    rateLimitType: "AUTH",
    enableCSRF: true,
    enableValidation: true
  }),
  // For general API endpoints
  API: createSecurityMiddleware({
    enableRateLimit: true,
    rateLimitType: "API",
    enableCSRF: true,
    enableSecurityHeaders: true
  }),
  // For public endpoints
  PUBLIC: createSecurityMiddleware({
    enableRateLimit: true,
    rateLimitType: "GENERAL",
    enableSecurityHeaders: true
  }),
  // For admin endpoints
  ADMIN: createSecurityMiddleware({
    enableRateLimit: true,
    rateLimitType: "API",
    enableCSRF: true,
    enableSecurityHeaders: true,
    enableValidation: true
  })
};

// src/app/router.ts
var handleRequest = /* @__PURE__ */ __name(async (request, env) => {
  if (!env.DB) {
    return serviceError("Database binding is missing.");
  }
  if (!env.JWT_SECRET) {
    return serviceError("JWT_SECRET is not configured.");
  }
  const adminReady = await adminExists(env.DB);
  const device = getDeviceType(request.headers.get("user-agent"));
  const session = await getAdminSession(request, env);
  const nonce = generateNonce();
  const url = new URL(request.url);
  const pathname = url.pathname;
  let securityMiddleware = SECURITY_MIDDLEWARE.PUBLIC;
  let securityContext = {};
  if (pathname.startsWith("/admin")) {
    securityMiddleware = SECURITY_MIDDLEWARE.ADMIN;
  } else if (pathname.startsWith("/signup") || pathname === "/login" || pathname === "/setup-admin") {
    securityMiddleware = SECURITY_MIDDLEWARE.AUTH;
    if (pathname === "/signup") {
      securityContext = { schema: COMMON_VALIDATION_SCHEMAS.USER_REGISTRATION };
    } else if (pathname === "/signup/verify") {
      securityContext = { schema: COMMON_VALIDATION_SCHEMAS.EMAIL_VERIFICATION };
    } else if (pathname === "/setup-admin") {
      securityContext = { schema: COMMON_VALIDATION_SCHEMAS.USER_REGISTRATION };
    } else if (pathname === "/login") {
      securityContext = { schema: COMMON_VALIDATION_SCHEMAS.USER_LOGIN };
    }
  } else if (pathname.startsWith("/api")) {
    securityMiddleware = SECURITY_MIDDLEWARE.API;
  }
  const securityResponse = await securityMiddleware(request, securityContext);
  if (securityResponse) {
    return applySecurityToResponse(securityResponse, nonce);
  }
  const apiResponse = await handleApiRoutes(request, env, { adminReady, device, session, nonce });
  if (apiResponse) {
    return applySecurityToResponse(apiResponse, nonce);
  }
  if (!adminReady) {
    return applySecurityToResponse(redirectResponse("/setup-admin"), nonce);
  }
  return applySecurityToResponse(redirectResponse("/"), nonce);
}, "handleRequest");

// src/app/worker.ts
var schemaReady = null;
var runSchemaCheck = /* @__PURE__ */ __name((env) => {
  if (!env.DB) {
    return Promise.resolve();
  }
  if (!schemaReady) {
    schemaReady = ensureSchema(env.DB);
  }
  return schemaReady;
}, "runSchemaCheck");
var renderServiceError = /* @__PURE__ */ __name((error) => {
  const message = error instanceof Error ? error.message : "Unknown error";
  return new Response(`Service unavailable: ${message}`, {
    status: 500,
    headers: { "content-type": "text/plain; charset=utf-8" }
  });
}, "renderServiceError");
var worker_default = {
  async fetch(request, env) {
    try {
      await runSchemaCheck(env);
      return await handleRequest(request, env);
    } catch (error) {
      return renderServiceError(error);
    }
  }
};

// ../../../../AppData/Local/npm-cache/_npx/32026684e21afda6/node_modules/wrangler/templates/middleware/middleware-ensure-req-body-drained.ts
var drainBody = /* @__PURE__ */ __name(async (request, env, _ctx, middlewareCtx) => {
  try {
    return await middlewareCtx.next(request, env);
  } finally {
    try {
      if (request.body !== null && !request.bodyUsed) {
        const reader = request.body.getReader();
        while (!(await reader.read()).done) {
        }
      }
    } catch (e) {
      console.error("Failed to drain the unused request body.", e);
    }
  }
}, "drainBody");
var middleware_ensure_req_body_drained_default = drainBody;

// ../../../../AppData/Local/npm-cache/_npx/32026684e21afda6/node_modules/wrangler/templates/middleware/middleware-miniflare3-json-error.ts
function reduceError(e) {
  return {
    name: e?.name,
    message: e?.message ?? String(e),
    stack: e?.stack,
    cause: e?.cause === void 0 ? void 0 : reduceError(e.cause)
  };
}
__name(reduceError, "reduceError");
var jsonError = /* @__PURE__ */ __name(async (request, env, _ctx, middlewareCtx) => {
  try {
    return await middlewareCtx.next(request, env);
  } catch (e) {
    const error = reduceError(e);
    return Response.json(error, {
      status: 500,
      headers: { "MF-Experimental-Error-Stack": "true" }
    });
  }
}, "jsonError");
var middleware_miniflare3_json_error_default = jsonError;

// .wrangler/tmp/bundle-HT75SJ/middleware-insertion-facade.js
var __INTERNAL_WRANGLER_MIDDLEWARE__ = [
  middleware_ensure_req_body_drained_default,
  middleware_miniflare3_json_error_default
];
var middleware_insertion_facade_default = worker_default;

// ../../../../AppData/Local/npm-cache/_npx/32026684e21afda6/node_modules/wrangler/templates/middleware/common.ts
var __facade_middleware__ = [];
function __facade_register__(...args) {
  __facade_middleware__.push(...args.flat());
}
__name(__facade_register__, "__facade_register__");
function __facade_invokeChain__(request, env, ctx, dispatch, middlewareChain) {
  const [head, ...tail] = middlewareChain;
  const middlewareCtx = {
    dispatch,
    next(newRequest, newEnv) {
      return __facade_invokeChain__(newRequest, newEnv, ctx, dispatch, tail);
    }
  };
  return head(request, env, ctx, middlewareCtx);
}
__name(__facade_invokeChain__, "__facade_invokeChain__");
function __facade_invoke__(request, env, ctx, dispatch, finalMiddleware) {
  return __facade_invokeChain__(request, env, ctx, dispatch, [
    ...__facade_middleware__,
    finalMiddleware
  ]);
}
__name(__facade_invoke__, "__facade_invoke__");

// .wrangler/tmp/bundle-HT75SJ/middleware-loader.entry.ts
var __Facade_ScheduledController__ = class ___Facade_ScheduledController__ {
  constructor(scheduledTime, cron, noRetry) {
    this.scheduledTime = scheduledTime;
    this.cron = cron;
    this.#noRetry = noRetry;
  }
  static {
    __name(this, "__Facade_ScheduledController__");
  }
  #noRetry;
  noRetry() {
    if (!(this instanceof ___Facade_ScheduledController__)) {
      throw new TypeError("Illegal invocation");
    }
    this.#noRetry();
  }
};
function wrapExportedHandler(worker) {
  if (__INTERNAL_WRANGLER_MIDDLEWARE__ === void 0 || __INTERNAL_WRANGLER_MIDDLEWARE__.length === 0) {
    return worker;
  }
  for (const middleware of __INTERNAL_WRANGLER_MIDDLEWARE__) {
    __facade_register__(middleware);
  }
  const fetchDispatcher = /* @__PURE__ */ __name(function(request, env, ctx) {
    if (worker.fetch === void 0) {
      throw new Error("Handler does not export a fetch() function.");
    }
    return worker.fetch(request, env, ctx);
  }, "fetchDispatcher");
  return {
    ...worker,
    fetch(request, env, ctx) {
      const dispatcher = /* @__PURE__ */ __name(function(type, init) {
        if (type === "scheduled" && worker.scheduled !== void 0) {
          const controller = new __Facade_ScheduledController__(
            Date.now(),
            init.cron ?? "",
            () => {
            }
          );
          return worker.scheduled(controller, env, ctx);
        }
      }, "dispatcher");
      return __facade_invoke__(request, env, ctx, dispatcher, fetchDispatcher);
    }
  };
}
__name(wrapExportedHandler, "wrapExportedHandler");
function wrapWorkerEntrypoint(klass) {
  if (__INTERNAL_WRANGLER_MIDDLEWARE__ === void 0 || __INTERNAL_WRANGLER_MIDDLEWARE__.length === 0) {
    return klass;
  }
  for (const middleware of __INTERNAL_WRANGLER_MIDDLEWARE__) {
    __facade_register__(middleware);
  }
  return class extends klass {
    #fetchDispatcher = /* @__PURE__ */ __name((request, env, ctx) => {
      this.env = env;
      this.ctx = ctx;
      if (super.fetch === void 0) {
        throw new Error("Entrypoint class does not define a fetch() function.");
      }
      return super.fetch(request);
    }, "#fetchDispatcher");
    #dispatcher = /* @__PURE__ */ __name((type, init) => {
      if (type === "scheduled" && super.scheduled !== void 0) {
        const controller = new __Facade_ScheduledController__(
          Date.now(),
          init.cron ?? "",
          () => {
          }
        );
        return super.scheduled(controller);
      }
    }, "#dispatcher");
    fetch(request) {
      return __facade_invoke__(
        request,
        this.env,
        this.ctx,
        this.#dispatcher,
        this.#fetchDispatcher
      );
    }
  };
}
__name(wrapWorkerEntrypoint, "wrapWorkerEntrypoint");
var WRAPPED_ENTRY;
if (typeof middleware_insertion_facade_default === "object") {
  WRAPPED_ENTRY = wrapExportedHandler(middleware_insertion_facade_default);
} else if (typeof middleware_insertion_facade_default === "function") {
  WRAPPED_ENTRY = wrapWorkerEntrypoint(middleware_insertion_facade_default);
}
var middleware_loader_entry_default = WRAPPED_ENTRY;
export {
  __INTERNAL_WRANGLER_MIDDLEWARE__,
  middleware_loader_entry_default as default
};
//# sourceMappingURL=index.js.map
