# Frontend Shared UI

Shared layout and UI components used by multiple frontend modules live here.

## Add a shared component

1. Create a file under `components` with a clear name.
2. Export the component string.
3. Import the component in the module that needs it.
