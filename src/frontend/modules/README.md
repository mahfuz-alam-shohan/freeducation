# Frontend Modules

Modules are grouped into user modules and platform modules to keep features isolated.

## Add a module

1. Create a new folder under `users/` or `platforms/`.
2. Export a module entry point that returns HTML or script strings.
3. Compose the module in `layout.ts` or `app.ts`.
