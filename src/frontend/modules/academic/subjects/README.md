# Academic Subjects

Academic runtime flows live in `app/`.

Each subject has its own folder at this level (for example `bangla/`, `english/`) so subject-specific data stays isolated and easy to extend.
