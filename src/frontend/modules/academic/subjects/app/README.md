# Academic Runtime Module

This module holds the student-facing academic runtime, state, and render pipelines.

Subject-specific data and helpers live alongside the subject folders at `modules/platforms/academic/subjects`.
