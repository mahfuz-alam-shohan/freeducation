# Student API Module

This module handles student authentication and onboarding workflows.

## Adding student routes

1. Add the route implementation inside this folder.
2. Mount it in `index.ts` for the student module.
3. Keep shared logic in the local folder unless it is used platform-wide.
