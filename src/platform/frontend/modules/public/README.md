# Public Frontend Module

This module contains landing, login, and public-facing content bundles.

## Add a public page

1. Add UI pieces in `landing-parts` or a new folder.
2. Export the bundle in `landing.ts` or `auth.ts`.
3. Map the new view in routing maps when needed.
