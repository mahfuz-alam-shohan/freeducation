# App Runtime Module

This module holds the client-side runtime, state, and render pipelines.

## Extend the runtime

1. Add helpers in this folder or a nested folder that matches the feature.
2. Export the helper bundle from the relevant file.
3. Compose the bundle in `frontend/app.ts`.
