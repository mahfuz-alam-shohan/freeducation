# Admin Frontend Module

This module contains admin dashboards, settings, and admin-specific UI bundles.

## Add a new admin view

1. Add a view file under `dashboard-parts` or a new folder.
2. Export the view bundle and compose it in `dashboard.ts` or `settings.ts`.
3. Register the view name in routing maps as needed.
