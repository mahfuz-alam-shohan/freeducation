# Frontend Modules

Each module owns a feature slice and exports script or markup bundles for composition.

## Add a module

1. Create a new folder in this directory.
2. Export a module entry point that returns HTML or script strings.
3. Compose the module in `frontend/layout.ts` or `frontend/app.ts`.
