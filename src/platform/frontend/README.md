# Frontend Layer

The frontend is assembled from modular bundles and shared layout components.

## Add a new frontend module

1. Create a folder in `modules/` with a clear feature name.
2. Expose component bundles or scripts from the module.
3. Import the module where it needs to be composed (layout or app bundle).
4. Keep reusable UI in `shared`.
