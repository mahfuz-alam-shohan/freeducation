export const authPageStyles = `
.center-wrap { min-height: 100vh; display: grid; place-items: center; padding: 10px; background: #f8fafc; }
.auth-card { width: min(500px, 100%); background: var(--surface); border: 1px solid var(--line); border-radius: 4px; padding: 10px; }
.public-login-shell { padding: 8px 6px; }
.public-auth-card { width: min(420px, 100%); border-radius: 4px; }
.form-grid { display: grid; gap: 5px; margin-top: 8px; }
label { font-size: 12px; color: #334155; font-weight: 600; }
.error { color: #b91c1c; min-height: 18px; font-size: 12px; margin: 0 0 6px; }
`;
