export function teacherDashboardHtml() {
  return `
    <section class="role-dash">
      <h2>Teacher dashboard</h2>
      <p>This dashboard is ready. Only the profile menu is enabled for now.</p>
    </section>
  `;
}
